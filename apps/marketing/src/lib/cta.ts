/**
 * Public marketing CTAs. Weekend outbound does not depend on hosted signup.
 * Canonical free path: aeorank-cli@0.1.1 (12 dimensions, 8 files).
 * Hosted app.aeorank.dev is Clerk Development — label it experimental/dev only.
 */
export const CLI_SCAN_CMD = "npx aeorank-cli@0.1.1 scan https://your-site.com";

/** On-site install strip that documents CLI_SCAN_CMD. */
export const CLI_INSTALL_HREF = "/#install";

/** Published quick start that shows the same command. */
export const CLI_DOCS_HREF = "https://docs.aeorank.dev/getting-started/";

/** Hosted dashboard — experimental / Clerk Development. Never a primary "get score" CTA. */
export const HOSTED_APP_HREF = "https://app.aeorank.dev";
