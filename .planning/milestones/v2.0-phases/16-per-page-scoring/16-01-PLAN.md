---
phase: 16-per-page-scoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/scorer/index.ts
  - packages/core/src/constants.ts
  - packages/core/src/types.ts
  - packages/core/src/index.ts
  - packages/core/src/__tests__/scorer.test.ts
autonomous: true
requirements: [PAGE-01, PAGE-04]

must_haves:
  truths:
    - "scorePerPage() returns scores on 0-75 scale (not 0-100)"
    - "PageScore type includes maxScore field set to 75"
    - "PAGE_LEVEL_DIMENSIONS is exported from @aeorank/core for consumers"
    - "API scan result JSON includes pages[] with per-page dimension scores (already wired, verify intact)"
    - "Duplication gate caps at 26 (35% of 75) instead of 35"
  artifacts:
    - path: "packages/core/src/scorer/index.ts"
      provides: "scorePerPage with 0-75 normalization"
      contains: "Math.round.*75"
    - path: "packages/core/src/constants.ts"
      provides: "Exported PAGE_LEVEL_DIMENSIONS constant"
      contains: "export const PAGE_LEVEL_DIMENSIONS"
    - path: "packages/core/src/types.ts"
      provides: "PageScore.maxScore field"
      contains: "maxScore"
    - path: "packages/core/src/__tests__/scorer.test.ts"
      provides: "Tests for 0-75 scale and duplication gate"
      contains: "toBeLessThanOrEqual(75)"
  key_links:
    - from: "packages/core/src/scorer/index.ts"
      to: "packages/core/src/constants.ts"
      via: "import PAGE_LEVEL_DIMENSIONS"
      pattern: "import.*PAGE_LEVEL_DIMENSIONS.*constants"
    - from: "packages/core/src/index.ts"
      to: "packages/core/src/constants.ts"
      via: "re-export PAGE_LEVEL_DIMENSIONS"
      pattern: "export.*PAGE_LEVEL_DIMENSIONS"
---

<objective>
Change per-page scoring from 0-100 to 0-75 scale and export PAGE_LEVEL_DIMENSIONS for CLI/dashboard consumers.

Purpose: PAGE-01 requires page-level scores on 0-75 scale (since site-level dimensions like topic-coherence, cross-page-duplication don't apply per-page). PAGE-04 requires API to return per-page scores (already wired but needs scale fix and maxScore field).
Output: Updated scorePerPage() returning 0-75 scores, exported constant, updated types, passing tests.
</objective>

<execution_context>
@/Users/vin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-per-page-scoring/16-CONTEXT.md

@packages/core/src/scorer/index.ts
@packages/core/src/constants.ts
@packages/core/src/types.ts
@packages/core/src/index.ts
@packages/core/src/__tests__/scorer.test.ts

<interfaces>
From packages/core/src/types.ts:
```typescript
export interface PageScore {
  url: string;
  title: string;
  score: number;
  grade: string;
  dimensions: Pick<DimensionScore, "id" | "score" | "status">[];
}
```

From packages/core/src/scorer/index.ts:
```typescript
export function scorePerPage(pages: ScannedPage[], meta: ScanMeta): PageScore[];
// PAGE_LEVEL_DIMENSIONS is currently a local const (25 items, 67% weight sum)
// Current normalization: (weightedSum / totalPageWeight) * 100 -> 0-100
```

From packages/core/src/constants.ts:
```typescript
export const DIMENSION_DEFS: DimensionDef[];  // 36 entries, weights sum to 100
export const PILLAR_GROUPS: PillarGroup[];
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Move PAGE_LEVEL_DIMENSIONS to constants.ts, add PageScore.maxScore, update scorePerPage to 0-75</name>
  <files>packages/core/src/constants.ts, packages/core/src/types.ts, packages/core/src/scorer/index.ts, packages/core/src/index.ts</files>
  <read_first>
    - packages/core/src/scorer/index.ts (lines 68-132 — PAGE_LEVEL_DIMENSIONS and scorePerPage)
    - packages/core/src/constants.ts (full — DIMENSION_DEFS, PILLAR_GROUPS)
    - packages/core/src/types.ts (lines 92-100 — PageScore interface)
    - packages/core/src/index.ts (exports section)
  </read_first>
  <behavior>
    - Test: scorePerPage returns score <= 75 for any page (not <= 100)
    - Test: scorePerPage returns score >= 0 for any page
    - Test: PageScore objects include maxScore property equal to 75
    - Test: duplication gate caps at 26 (Math.round(75 * 0.35) = 26) not 35
    - Test: page with 0 dupes can score above 26
    - Test: PAGE_LEVEL_DIMENSIONS is importable from @aeorank/core
  </behavior>
  <action>
1. **constants.ts**: Move the `PAGE_LEVEL_DIMENSIONS` array from `scorer/index.ts` to `constants.ts` as an exported const. Keep the exact same 25 dimension IDs. Add `export const PAGE_SCORE_MAX = 75;` constant.

2. **types.ts**: Add `maxScore: number;` field to `PageScore` interface (after `score`).

3. **scorer/index.ts**:
   - Remove the local `PAGE_LEVEL_DIMENSIONS` array
   - Import `PAGE_LEVEL_DIMENSIONS` and `PAGE_SCORE_MAX` from `../constants.js`
   - Change normalization from `(weightedSum / totalPageWeight) * 100` to `(weightedSum / totalPageWeight) * PAGE_SCORE_MAX`
   - Change duplication gate from `Math.min(score, 35)` to `Math.min(score, Math.round(PAGE_SCORE_MAX * 0.35))` (= 26)
   - Add `maxScore: PAGE_SCORE_MAX` to the returned PageScore object
   - Update grade call: `getGrade(Math.round(score * (100 / PAGE_SCORE_MAX)))` — convert to 100-scale for grading since grade thresholds are 0-100

4. **index.ts**: Add `PAGE_LEVEL_DIMENSIONS, PAGE_SCORE_MAX` to the re-exports from `./constants.js`
  </action>
  <verify>
    <automated>cd packages/core && npx vitest run src/__tests__/scorer.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "export const PAGE_LEVEL_DIMENSIONS" packages/core/src/constants.ts
    - grep -q "export const PAGE_SCORE_MAX = 75" packages/core/src/constants.ts
    - grep -q "maxScore: number" packages/core/src/types.ts (in PageScore interface)
    - grep -q "PAGE_SCORE_MAX" packages/core/src/scorer/index.ts
    - grep -q "PAGE_LEVEL_DIMENSIONS" packages/core/src/index.ts
  </acceptance_criteria>
  <done>scorePerPage returns 0-75 scores with maxScore:75, duplication gate caps at 26, PAGE_LEVEL_DIMENSIONS exported from @aeorank/core</done>
</task>

<task type="auto">
  <name>Task 2: Update existing scorer tests for 0-75 scale</name>
  <files>packages/core/src/__tests__/scorer.test.ts</files>
  <read_first>
    - packages/core/src/__tests__/scorer.test.ts (lines 340-440 — duplication gate tests and page score tests)
  </read_first>
  <action>
Update all existing scorePerPage test assertions:

1. Change `toBeLessThanOrEqual(100)` to `toBeLessThanOrEqual(75)` in the "duplication gate does not apply" test (line ~393)
2. Change `toBeLessThanOrEqual(35)` to `toBeLessThanOrEqual(26)` in all 3 duplication gate tests (lines ~368, ~413)
3. Add assertion `expect(pageScores[0].maxScore).toBe(75)` to all scorePerPage tests
4. Add a new test: "page score never exceeds 75" that creates a page with all-perfect signals and verifies score <= 75
5. Add a new test: "PAGE_LEVEL_DIMENSIONS is exported" that imports it and verifies it has 25 entries
  </action>
  <verify>
    <automated>cd packages/core && npx vitest run src/__tests__/scorer.test.ts --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "toBeLessThanOrEqual(75)" packages/core/src/__tests__/scorer.test.ts
    - grep -q "toBeLessThanOrEqual(26)" packages/core/src/__tests__/scorer.test.ts
    - grep -q "maxScore.*75" packages/core/src/__tests__/scorer.test.ts
    - grep -q "PAGE_LEVEL_DIMENSIONS" packages/core/src/__tests__/scorer.test.ts
  </acceptance_criteria>
  <done>All scorer tests pass with 0-75 scale assertions, duplication gate at 26, maxScore verified, PAGE_LEVEL_DIMENSIONS import verified</done>
</task>

</tasks>

<verification>
1. `cd packages/core && npx vitest run` — all core tests pass
2. `cd packages/core && npx vitest run src/__tests__/scorer.test.ts` — scorer tests specifically pass
3. `pnpm run build --filter=@aeorank/core` — core package builds clean
</verification>

<success_criteria>
- scorePerPage returns scores 0-75 with maxScore:75 field
- Duplication gate caps at 26 (35% of 75)
- PAGE_LEVEL_DIMENSIONS and PAGE_SCORE_MAX exported from @aeorank/core
- All existing + new tests pass
- Core package builds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/16-per-page-scoring/16-01-SUMMARY.md`
</output>
