---
phase: 16-per-page-scoring
plan: 02
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - packages/cli/src/commands/scan.ts
  - packages/cli/src/ui/score-display.ts
  - packages/cli/src/__tests__/score-display.test.ts
  - packages/cli/src/__tests__/scan.test.ts
  - apps/web/components/PageScores.tsx
autonomous: true
requirements: [PAGE-02, PAGE-03]

must_haves:
  truths:
    - "CLI --page /about filters output to show only that page's score and dimension breakdown"
    - "CLI --page flag shows per-page dimension table with pass/warn/fail status"
    - "Dashboard PageScores component has expandable rows showing per-page dimension breakdown"
    - "Dashboard shows score out of 75 (not 100) for per-page scores"
  artifacts:
    - path: "packages/cli/src/commands/scan.ts"
      provides: "--page flag option and per-page output logic"
      contains: "--page"
    - path: "packages/cli/src/ui/score-display.ts"
      provides: "renderPageScore function for single-page output"
      contains: "renderPageScore"
    - path: "apps/web/components/PageScores.tsx"
      provides: "Expandable per-page dimension rows"
      contains: "expandedPage"
  key_links:
    - from: "packages/cli/src/commands/scan.ts"
      to: "packages/cli/src/ui/score-display.ts"
      via: "import renderPageScore"
      pattern: "renderPageScore"
    - from: "packages/cli/src/commands/scan.ts"
      to: "@aeorank/core"
      via: "import PAGE_SCORE_MAX"
      pattern: "PAGE_SCORE_MAX"
---

<objective>
Add CLI --page flag for single-page audit and enhance dashboard per-page breakdown with expandable dimension details.

Purpose: PAGE-03 requires CLI single-page audit via --page flag. PAGE-02 requires dashboard per-page dimension table (currently shows score/grade but not dimension breakdown).
Output: CLI --page flag with per-page dimension output, dashboard expandable page rows.
</objective>

<execution_context>
@/Users/vin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-per-page-scoring/16-CONTEXT.md
@.planning/phases/16-per-page-scoring/16-01-SUMMARY.md

@packages/cli/src/commands/scan.ts
@packages/cli/src/ui/score-display.ts
@packages/cli/src/__tests__/score-display.test.ts
@packages/cli/src/__tests__/scan.test.ts
@apps/web/components/PageScores.tsx

<interfaces>
From packages/core/src/types.ts (after Plan 01):
```typescript
export interface PageScore {
  url: string;
  title: string;
  score: number;
  maxScore: number;  // 75
  grade: string;
  dimensions: Pick<DimensionScore, "id" | "score" | "status">[];
}
```

From packages/core/src/constants.ts (after Plan 01):
```typescript
export const PAGE_LEVEL_DIMENSIONS: string[];  // 25 dimension IDs
export const PAGE_SCORE_MAX = 75;
export const DIMENSION_DEFS: DimensionDef[];
export const PILLAR_GROUPS: PillarGroup[];
```

From packages/cli/src/ui/score-display.ts:
```typescript
export function renderScore(result: ScanResult): string;
export function renderDimensionTable(dimensions: DimensionScore[], pillarFilter?: string): string;
export function renderNextSteps(dimensions: DimensionScore[], pillarFilter?: string): string;
```

From apps/web/components/PageScores.tsx:
```typescript
// Client component with sortable table (score/url)
// Shows: page title, pathname, score, grade
// Expandable to show all pages (default: top 10)
// Does NOT currently show per-page dimension breakdown
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CLI --page flag and renderPageScore function</name>
  <files>packages/cli/src/commands/scan.ts, packages/cli/src/ui/score-display.ts, packages/cli/src/__tests__/score-display.test.ts, packages/cli/src/__tests__/scan.test.ts</files>
  <read_first>
    - packages/cli/src/commands/scan.ts (full — scan command with options)
    - packages/cli/src/ui/score-display.ts (full — renderScore, renderDimensionTable)
    - packages/cli/src/__tests__/score-display.test.ts (first 50 lines — test structure)
    - packages/cli/src/__tests__/scan.test.ts (first 80 lines — mock setup)
  </read_first>
  <behavior>
    - Test: renderPageScore renders page title, URL, score/75, grade, and dimension rows
    - Test: renderPageScore shows pass/warn/fail icons for each dimension
    - Test: renderPageScore shows dimension name and score for each page-level dimension
    - Test: --page flag filters result.pageScores to matching page URL
    - Test: --page /about matches pageScore with URL ending in /about
  </behavior>
  <action>
1. **score-display.ts**: Add `renderPageScore(page: PageScore, allDefs: DimensionDef[]): string` function:
   - Header: page title + URL + score/75 + grade (colored like renderScore)
   - Dimension table: for each dimension in page.dimensions, look up name from allDefs, show status icon + name + score/10
   - Sort dimensions by score ascending (worst first) for actionability
   - Import `PAGE_SCORE_MAX` from `@aeorank/core` for the "/75" display

2. **scan.ts**: Add `--page <path>` option:
   - `.option("--page <path>", "Show score for a specific page path (e.g. /about)")`
   - Add `page?: string` to ScanOptions interface
   - After scan completes, if `options.page` is set:
     - Find matching pageScore where URL pathname matches `options.page` (normalize: ensure leading slash)
     - If found: render with `renderPageScore(matchedPage, DIMENSION_DEFS)` — import DIMENSION_DEFS from @aeorank/core
     - If not found: print error "No page found matching {path}. Scanned pages:" + list of pathnames
     - Skip normal renderScore/renderDimensionTable output when --page is set (show only page-specific output)
   - For JSON mode with --page: output only the matching pageScore object

3. **Tests**: Add tests in score-display.test.ts for renderPageScore. Add test in scan.test.ts for --page flag behavior.
  </action>
  <verify>
    <automated>cd packages/cli && npx vitest run --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "\-\-page" packages/cli/src/commands/scan.ts
    - grep -q "renderPageScore" packages/cli/src/ui/score-display.ts
    - grep -q "renderPageScore" packages/cli/src/__tests__/score-display.test.ts
    - grep -q "PAGE_SCORE_MAX" packages/cli/src/ui/score-display.ts
  </acceptance_criteria>
  <done>CLI --page flag filters to single page, renderPageScore shows score/75 + dimension breakdown, all CLI tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard PageScores expandable per-page dimension breakdown</name>
  <files>apps/web/components/PageScores.tsx</files>
  <read_first>
    - apps/web/components/PageScores.tsx (full — current table implementation)
    - packages/core/src/constants.ts (DIMENSION_DEFS for dimension name lookup)
  </read_first>
  <action>
Enhance the PageScores component to show expandable per-page dimension details:

1. Add `expandedPage` state: `useState<string | null>(null)` to track which page row is expanded
2. Add click handler on each table row — clicking toggles expansion for that page URL
3. When a row is expanded, render a sub-table below it showing all page-level dimensions:
   - Column 1: Dimension name (look up from a `DIMENSION_NAMES` map — hardcode the 25 page-level dim id->name pairs to avoid importing @aeorank/core in browser bundle, matching the pattern from Phase 15 ScoreBreakdown)
   - Column 2: Score /10
   - Column 3: Status badge (pass=green, warn=amber, fail=red)
   - Sort by status (fail first, then warn, then pass) for actionability
4. Update the score display to show "/ 75" instead of bare number:
   - Change score cell from `{page.score}` to `{page.score}/75`
5. Add visual expand indicator (chevron SVG) in first column that rotates when expanded
6. Style the expanded sub-table with slightly indented rows and lighter background using existing CSS utility classes (card-inner or similar)
  </action>
  <verify>
    <automated>cd apps/web && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "expandedPage" apps/web/components/PageScores.tsx
    - grep -q "/75" apps/web/components/PageScores.tsx
    - grep -q "DIMENSION_NAMES" apps/web/components/PageScores.tsx
  </acceptance_criteria>
  <done>PageScores shows expandable dimension breakdown per page, scores display as /75, build succeeds</done>
</task>

</tasks>

<verification>
1. `cd packages/cli && npx vitest run` — all CLI tests pass
2. `cd apps/web && npx next build` — dashboard builds clean
3. `pnpm run build` — full monorepo build passes
</verification>

<success_criteria>
- CLI `aeorank scan <url> --page /about` outputs single-page score/75 with dimension breakdown
- CLI --page with JSON mode outputs filtered pageScore object
- CLI --page with non-existent path shows helpful error with available paths
- Dashboard PageScores rows are clickable/expandable showing dimension details
- Dashboard shows scores as /75
- All tests pass, all packages build
</success_criteria>

<output>
After completion, create `.planning/phases/16-per-page-scoring/16-02-SUMMARY.md`
</output>
