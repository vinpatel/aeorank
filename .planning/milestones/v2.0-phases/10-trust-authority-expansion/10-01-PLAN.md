---
phase: 10-trust-authority-expansion
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/scorer/dimensions.ts
  - packages/core/src/constants.ts
  - packages/core/src/scorer/index.ts
  - packages/core/src/__tests__/dimensions.test.ts
  - packages/core/src/__tests__/scorer.test.ts
  - packages/core/src/__tests__/integration.test.ts
  - packages/core/src/__tests__/determinism.test.ts
autonomous: true
requirements: [TRST-01, TRST-02]

must_haves:
  truths:
    - "scan() result includes internal-linking dimension with score 0-10"
    - "scan() result includes author-schema dimension with score 0-10"
    - "A site with rich internal linking scores higher on internal-linking than one with no internal links"
    - "A site with Person schema + credentials scores higher on author-schema than one without"
    - "All existing + new tests pass with zero failures"
    - "Determinism test passes for both new dimensions"
  artifacts:
    - path: "packages/core/src/scorer/dimensions.ts"
      provides: "scoreInternalLinking and scoreAuthorSchema scorer functions"
      exports: ["scoreInternalLinking", "scoreAuthorSchema"]
    - path: "packages/core/src/constants.ts"
      provides: "2 new DIMENSION_DEFS entries (27 total)"
      contains: "internal-linking"
    - path: "packages/core/src/scorer/index.ts"
      provides: "Both dimensions in PAGE_LEVEL_DIMENSIONS"
      contains: "internal-linking"
    - path: "packages/core/src/__tests__/dimensions.test.ts"
      provides: "Unit tests for both new scorers"
      contains: "scoreInternalLinking"
    - path: "packages/core/src/__tests__/determinism.test.ts"
      provides: "Determinism verification for 27 dimensions"
      contains: "internal-linking"
  key_links:
    - from: "packages/core/src/scorer/dimensions.ts"
      to: "packages/core/src/constants.ts"
      via: "DIMENSION_DEFS entries for internal-linking and author-schema"
      pattern: "internal-linking.*author-schema"
    - from: "packages/core/src/scorer/dimensions.ts"
      to: "DIMENSION_SCORERS registry"
      via: "Registry entries mapping IDs to scorer functions"
      pattern: "internal-linking.*scoreInternalLinking"
---

<objective>
Add 2 Trust & Authority scoring dimensions (internal-linking, author-schema) completing the Trust & Authority pillar expansion.

Purpose: Deepen trust signals beyond basic E-E-A-T by analyzing internal link topology and author expertise markup.
Output: 2 new scorer functions registered in dimensions.ts, 27 total DIMENSION_DEFS, all tests passing including determinism.
</objective>

<execution_context>
@/Users/vin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-trust-authority-expansion/10-CONTEXT.md
@SCORING_ROADMAP.md

@.planning/phases/09-content-structure-expansion/09-02-SUMMARY.md

<interfaces>
<!-- Existing types and contracts the executor needs -->

From packages/core/src/types.ts:
```typescript
export interface ScannedPage {
  url: string;
  title: string;
  schemaOrg: object[];
  links: PageLink[];
  authorName: string | null;
  // ... other fields
}

export interface PageLink {
  href: string;
  text: string;
  internal: boolean;
}

export interface DimensionScore {
  id: string;
  name: string;
  score: number;
  maxScore: number;
  weight: "high" | "medium" | "low";
  status: "pass" | "warn" | "fail";
  hint: string;
}
```

From packages/core/src/scorer/dimensions.ts:
```typescript
type DimensionScorer = (pages: ScannedPage[], meta: ScanMeta) => DimensionScore;

// Helper used by most scorers:
function makeDimension(id, name, score, weight, hint): DimensionScore;

// Registry — append new entries here:
export const DIMENSION_SCORERS: Record<string, DimensionScorer> = { ... };
```

From packages/core/src/constants.ts:
```typescript
// Currently 25 entries — append 2 more:
export const DIMENSION_DEFS: DimensionDef[] = [ ... ];
```

From packages/core/src/scorer/index.ts:
```typescript
// Page-level dimensions — append both new IDs:
const PAGE_LEVEL_DIMENSIONS = [ ... ];
```

From packages/core/src/__tests__/dimensions.test.ts:
```typescript
// Test helpers:
function makePage(overrides: Partial<ScannedPage> = {}): ScannedPage;
function makeMeta(overrides: Partial<ScanMeta> = {}): ScanMeta;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement internal-linking and author-schema scorers</name>
  <files>
    packages/core/src/scorer/dimensions.ts
    packages/core/src/constants.ts
    packages/core/src/scorer/index.ts
    packages/core/src/__tests__/dimensions.test.ts
  </files>
  <read_first>
    packages/core/src/scorer/dimensions.ts (lines 1090-1200 for latest scorer pattern + makeDimension + DIMENSION_SCORERS registry)
    packages/core/src/constants.ts (full file — DIMENSION_DEFS array)
    packages/core/src/scorer/index.ts (full file — PAGE_LEVEL_DIMENSIONS array)
    packages/core/src/__tests__/dimensions.test.ts (first 50 lines for makePage/makeMeta helpers, last 100 lines for latest test pattern)
    packages/core/src/types.ts (PageLink interface, ScannedPage interface)
  </read_first>
  <behavior>
    scoreInternalLinking tests:
    - Empty pages array returns score 0 with hint about adding internal links
    - Pages with zero internal links return score 0
    - Pages with 1-2 internal links avg return score 2-3
    - Pages with 3-5 internal links avg return score 5-7
    - Pages with 6+ internal links avg AND BreadcrumbList schema present return score 10
    - Pages with good internal links but no breadcrumbs cap at 8
    - Weight is "medium", id is "internal-linking", name is "Internal Linking"

    scoreAuthorSchema tests:
    - Empty pages array returns score 0 with hint about adding Person schema
    - Pages with no Person schema return score 0
    - Pages with Person schema but no jobTitle/credentials return score 3
    - Pages with Person schema + jobTitle return score 6
    - Pages with Person schema + jobTitle + sameAs array (1+ URLs) return score 10
    - Weight is "low", id is "author-schema", name is "Author & Expert Schema"
  </behavior>
  <action>
    RED phase — write tests first in dimensions.test.ts following the behavior spec above. Import scoreInternalLinking and scoreAuthorSchema (will fail — functions don't exist yet).

    GREEN phase — implement both scorers in dimensions.ts:

    **scoreInternalLinking** (Dimension 26, medium weight):
    1. If pages.length === 0, return makeDimension("internal-linking", "Internal Linking", 0, "medium", hint)
    2. Sort pages by URL for determinism
    3. For each page, count internal links: `page.links.filter(l => l.internal).length`
    4. Calculate average internal links per page
    5. Check for BreadcrumbList in any page's schemaOrg (check @type and @graph arrays, same pattern as scoreSchemaMarkup)
    6. Scoring thresholds:
       - avgInternal >= 5 AND hasBreadcrumbs: score = 10
       - avgInternal >= 5: score = 8
       - avgInternal >= 3: score = 6
       - avgInternal >= 1: score = 3
       - else: score = 0
    7. Hint: "Add internal links between related content pages and implement breadcrumb navigation" (when score < 10)

    **scoreAuthorSchema** (Dimension 27, low weight):
    1. If pages.length === 0, return makeDimension("author-schema", "Author & Expert Schema", 0, "low", hint)
    2. Scan all pages' schemaOrg for Person type (check @type and @graph arrays)
    3. For each Person found, check for:
       - `jobTitle` or `hasCredential` property (expertise signal)
       - `sameAs` array with 1+ URLs (authority signal)
    4. Track: hasPersonSchema, hasCredentials, hasSameAs
    5. Scoring:
       - hasPersonSchema AND hasCredentials AND hasSameAs: score = 10
       - hasPersonSchema AND hasCredentials: score = 6
       - hasPersonSchema: score = 3
       - else: score = 0
    6. Hint: "Add Person schema with jobTitle/credentials and sameAs links to author profiles" (when score < 10)

    Register both in DIMENSION_SCORERS:
    - "internal-linking": scoreInternalLinking
    - "author-schema": scoreAuthorSchema

    Add to DIMENSION_DEFS in constants.ts:
    - { id: "internal-linking", name: "Internal Linking", weight: "medium", maxScore: 10 }
    - { id: "author-schema", name: "Author & Expert Schema", weight: "low", maxScore: 10 }

    Add both IDs to PAGE_LEVEL_DIMENSIONS in scorer/index.ts:
    - "internal-linking" (page-level: each page has its own internal links)
    - "author-schema" (page-level: each page may have Person schema)
  </action>
  <verify>
    <automated>cd /Users/vin/Development/aeorank && pnpm vitest run packages/core/src/__tests__/dimensions.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "scoreInternalLinking" packages/core/src/scorer/dimensions.ts returns >= 2 (function def + registry)
    - grep -c "scoreAuthorSchema" packages/core/src/scorer/dimensions.ts returns >= 2 (function def + registry)
    - grep -c "internal-linking" packages/core/src/constants.ts returns >= 1
    - grep -c "author-schema" packages/core/src/constants.ts returns >= 1
    - grep -c "internal-linking" packages/core/src/scorer/index.ts returns >= 1
    - grep -c "author-schema" packages/core/src/scorer/index.ts returns >= 1
    - grep -c "scoreInternalLinking\|scoreAuthorSchema" packages/core/src/__tests__/dimensions.test.ts returns >= 4
    - vitest run dimensions.test.ts exits 0
  </acceptance_criteria>
  <done>Both scoreInternalLinking and scoreAuthorSchema implemented, registered in DIMENSION_SCORERS + DIMENSION_DEFS + PAGE_LEVEL_DIMENSIONS, with unit tests passing.</done>
</task>

<task type="auto">
  <name>Task 2: Update scorer/integration/determinism tests for 27 dimensions</name>
  <files>
    packages/core/src/__tests__/scorer.test.ts
    packages/core/src/__tests__/integration.test.ts
    packages/core/src/__tests__/determinism.test.ts
  </files>
  <read_first>
    packages/core/src/__tests__/scorer.test.ts (find assertion for dimension count — currently 25)
    packages/core/src/__tests__/integration.test.ts (find assertion for dimension count — currently 25)
    packages/core/src/__tests__/determinism.test.ts (full file — understand HTML fixture, dimension count, dimension ID list)
  </read_first>
  <action>
    **scorer.test.ts:**
    - Update dimension count assertion from 25 to 27
    - If there is a test page fixture used by scorer tests, add internal links and Person schema to it so the new dimensions get non-zero scores

    **integration.test.ts:**
    - Update dimension count assertion from 25 to 27

    **determinism.test.ts:**
    - Update expected dimension count from 25 to 27
    - Add "internal-linking" and "author-schema" to the list of dimension IDs checked
    - Enhance the HTML fixture to include:
      - Several internal anchor links: `<a href="/about">About</a>`, `<a href="/blog">Blog</a>`, `<a href="/contact">Contact</a>`
      - A BreadcrumbList schema in the JSON-LD: `{"@type": "BreadcrumbList", "itemListElement": [...]}`
      - A Person schema with jobTitle and sameAs: `{"@type": "Person", "name": "Dr. Jane Smith", "jobTitle": "Data Scientist", "sameAs": ["https://twitter.com/janesmith", "https://linkedin.com/in/janesmith"]}`
    - This ensures determinism tests verify both new scorers produce consistent non-zero scores

    Run full test suite to confirm zero failures.
  </action>
  <verify>
    <automated>cd /Users/vin/Development/aeorank && pnpm vitest run 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep "27" packages/core/src/__tests__/scorer.test.ts returns a match (dimension count)
    - grep "27" packages/core/src/__tests__/integration.test.ts returns a match (dimension count)
    - grep "27" packages/core/src/__tests__/determinism.test.ts returns a match (dimension count)
    - grep "internal-linking" packages/core/src/__tests__/determinism.test.ts returns a match
    - grep "author-schema" packages/core/src/__tests__/determinism.test.ts returns a match
    - pnpm vitest run exits 0 with all tests passing
  </acceptance_criteria>
  <done>All test files updated for 27 dimensions, determinism verified for internal-linking and author-schema, full test suite passes with zero failures.</done>
</task>

</tasks>

<verification>
1. `pnpm vitest run` — all tests pass (0 failures)
2. `pnpm tsc --noEmit -p packages/core/tsconfig.json` — no type errors
3. DIMENSION_DEFS has exactly 27 entries
4. DIMENSION_SCORERS has exactly 27 entries
5. Determinism test verifies both new dimension IDs produce identical scores across runs
</verification>

<success_criteria>
- scan() result includes internal-linking and author-schema dimension scores (0-10 each)
- Both dimensions registered in DIMENSION_DEFS (27 total), DIMENSION_SCORERS (27 total), PAGE_LEVEL_DIMENSIONS
- A site with internal links + breadcrumbs scores higher on internal-linking than one without
- A site with Person schema + credentials + sameAs scores higher on author-schema than one without
- All existing + new tests pass with zero failures
- Determinism test passes for all 27 dimensions
</success_criteria>

<output>
After completion, create `.planning/phases/10-trust-authority-expansion/10-01-SUMMARY.md`
</output>
