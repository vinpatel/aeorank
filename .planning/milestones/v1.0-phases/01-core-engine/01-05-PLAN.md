---
phase: 01-core-engine
plan: 05
type: execute
wave: 3
depends_on: [02, 03, 04]
files_modified:
  - packages/core/src/index.ts
  - packages/core/src/__tests__/integration.test.ts
  - packages/core/src/__tests__/determinism.test.ts
autonomous: true
requirements: [SCAN-03, SCORE-05]

must_haves:
  truths:
    - "core.scan(url) returns a complete ScanResult with score, grade, dimensions, and files"
    - "core.generateFiles(scanResult) returns all 8 files as strings"
    - "Same mocked URL scanned twice produces identical score (determinism)"
    - "Full pipeline: scan → score → generate works end-to-end"
    - "pnpm build from root compiles all packages with zero errors"
    - "pnpm test from root passes all tests across all packages"
  artifacts:
    - path: "packages/core/src/index.ts"
      provides: "Complete public API: scan, calculateAeoScore, generateFiles"
      exports: ["scan", "scanUrl", "calculateAeoScore", "generateFiles"]
    - path: "packages/core/src/__tests__/integration.test.ts"
      provides: "End-to-end pipeline test"
    - path: "packages/core/src/__tests__/determinism.test.ts"
      provides: "Determinism verification test"
  key_links:
    - from: "packages/core/src/index.ts"
      to: "scanner/index.ts, scorer/index.ts, generators/index.ts"
      via: "imports and public API wiring"
      pattern: "export.*from.*(scanner|scorer|generators)"
---

<objective>
Wire the full scan → score → generate pipeline, build integration tests, and verify determinism across the entire @aeorank/core library.

Purpose: Prove the core library works end-to-end — a URL in, a scored result with 8 generated files out. Verify the critical determinism requirement.
Output: Complete public API with integration and determinism tests passing.
</objective>

<execution_context>
@/Users/vin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-core-engine/01-CONTEXT.md
@.planning/phases/01-core-engine/01-01-SUMMARY.md
@.planning/phases/01-core-engine/01-02-SUMMARY.md
@.planning/phases/01-core-engine/01-03-SUMMARY.md
@.planning/phases/01-core-engine/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire public API and build integration test</name>
  <files>packages/core/src/index.ts, packages/core/src/__tests__/integration.test.ts</files>
  <action>
    Update packages/core/src/index.ts to be the complete public API:
    - Export all types from types.ts
    - Export constants from constants.ts
    - Export utility functions from utils.ts
    - Export scanUrl from scanner/index.ts
    - Export calculateAeoScore from scorer/index.ts
    - Export generateFiles from generators/index.ts
    - Create a convenience function scan(url, config?) that:
      1. Calls scanUrl(url, config) to get pages and meta
      2. Calls calculateAeoScore(pages, meta) to get score, grade, dimensions
      3. Calls generateFiles(scanResult) to get files
      4. Returns complete ScanResult with all fields populated

    Create packages/core/src/__tests__/integration.test.ts:
    - Mock fetch globally to return fixture HTML for different URLs
    - Test the full pipeline: scan('https://example.com') with mocked fetcher
    - Verify result has:
      - url set to the input URL
      - score is a number 0-100
      - grade is a valid letter grade
      - dimensions is an array of exactly 12 items
      - files is an array of exactly 8 items
      - pages is a non-empty array of ScannedPage objects
      - pagesScanned matches pages.length
      - duration is a positive number
      - scannedAt is a valid ISO date string
    - Verify each dimension has: id, name, score (0-10), maxScore, weight, status, hint
    - Verify each file has: name (matching expected names), content (non-empty string)
    - Verify score = weighted average of dimensions (manual calculation in test)
    - Verify grade matches score via getGrade()
  </action>
  <verify>
    <automated>cd packages/core && pnpm test -- --reporter=verbose src/__tests__/integration.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>Integration test passes. Full pipeline produces valid ScanResult with 12 dimensions and 8 files. Score matches weighted calculation.</done>
</task>

<task type="auto">
  <name>Task 2: Build determinism test and final build verification</name>
  <files>packages/core/src/__tests__/determinism.test.ts</files>
  <action>
    Create packages/core/src/__tests__/determinism.test.ts:
    - Test: "same input produces identical output across 10 consecutive runs"
      - Create a fixed mock ScanResult with known pages and meta
      - Run calculateAeoScore(pages, meta) 10 times
      - Assert all 10 results are deeply equal (use JSON.stringify comparison)
      - Specifically check: score, grade, and each dimension's score are identical
    - Test: "same input produces identical files across 10 consecutive runs"
      - Run generateFiles(scanResult) 10 times
      - Assert all 10 results produce identical file content
    - Test: "dimension order is deterministic"
      - Run calculateAeoScore twice
      - Assert dimension IDs appear in same order both times
    - Test: "file order is deterministic"
      - Run generateFiles twice
      - Assert file names appear in same order both times
    - Test: "page ordering doesn't affect score"
      - Create mock pages in two different orders
      - Run calculateAeoScore on both
      - Assert identical scores (proves internal sorting works)

    After tests pass, run from repo root:
    - pnpm build — verify all packages compile
    - pnpm test — verify all tests pass
    - pnpm lint (biome check .) — verify zero warnings
    - pnpm typecheck — verify zero TypeScript errors
  </action>
  <verify>
    <automated>cd /Users/vin/Development/aeorank && pnpm build 2>&1 | tail -10 && pnpm test 2>&1 | tail -20 && npx @biomejs/biome check . 2>&1 | tail -10</automated>
  </verify>
  <done>Determinism tests pass (10 identical runs). Full build succeeds. All tests pass. Biome lint clean. @aeorank/core is complete and ready for CLI consumption in Phase 2.</done>
</task>

</tasks>

<verification>
- pnpm build from root: zero errors
- pnpm test from root: all tests pass
- biome check: zero warnings
- Determinism: 10 consecutive runs produce identical output
- Integration: full scan → score → generate pipeline works
- Public API exports: scan, scanUrl, calculateAeoScore, generateFiles, all types
</verification>

<success_criteria>
- scan() convenience function works end-to-end with mocked HTTP
- Determinism test proves SCORE-05 (same URL = same score)
- All packages compile via Turborepo
- All tests pass across the monorepo
- Biome linting passes with zero warnings
- @aeorank/core is ready for Phase 2 (CLI wrapper)
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-engine/01-05-SUMMARY.md`
</output>
