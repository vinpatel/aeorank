---
phase: 06-retroactive-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/06-retroactive-verification/01-VERIFICATION.md
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03
  - SCAN-01
  - SCAN-02
  - SCAN-03
  - SCAN-04
  - SCORE-01
  - SCORE-02
  - SCORE-03
  - SCORE-04
  - SCORE-05
  - GEN-01
  - GEN-02
  - GEN-03
  - GEN-04
  - GEN-05
  - GEN-06
  - GEN-07
  - GEN-08

must_haves:
  truths:
    - "Every Phase 1 requirement (20 total) has a PASS or GAPS_FOUND verdict with primary evidence"
    - "No requirement is marked PASS based solely on SUMMARY.md — each has source file or test evidence"
    - "SCAN-03 (30-second) is flagged with appropriate confidence caveat per RESEARCH.md pitfall 3"
    - "SCORE-01 (80% structural) interpretation is documented per RESEARCH.md pitfall 4"
    - "INFRA-02 evidence points to packages/core (not packages/config stub) per RESEARCH.md pitfall 5"
  artifacts:
    - path: ".planning/phases/06-retroactive-verification/01-VERIFICATION.md"
      provides: "Phase 1 verification results for all 20 requirements"
      contains: "INFRA-01"
  key_links:
    - from: "01-VERIFICATION.md"
      to: "REQUIREMENTS.md traceability table"
      via: "requirement IDs match exactly"
      pattern: "INFRA-01|SCAN-01|SCORE-01|GEN-01"
---

<objective>
Verify all 20 Phase 1 (Core Engine) requirements against the live source tree and test results, producing 01-VERIFICATION.md with pass/gaps_found status for each requirement.

Purpose: Close the verification gap for Phase 1 requirements (INFRA-01 through GEN-08) that were built but never formally verified against REQUIREMENTS.md.
Output: `.planning/phases/06-retroactive-verification/01-VERIFICATION.md`
</objective>

<execution_context>
@/Users/vin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-retroactive-verification/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run Phase 1 tests and collect infrastructure evidence</name>
  <files>.planning/phases/06-retroactive-verification/01-VERIFICATION.md</files>
  <action>
Execute verification for all 20 Phase 1 requirements. Work through each requirement collecting primary evidence:

**Step 1 — Run tests:**
```bash
pnpm --filter @aeorank/core test 2>&1
pnpm biome check . 2>&1
```
Record: total tests, pass count, any failures.

**Step 2 — Verify each requirement with primary sources (not SUMMARY.md):**

INFRA-01 (monorepo): Confirm pnpm-workspace.yaml exists and lists packages/core, packages/cli, apps/web, apps/marketing, apps/docs. Confirm turbo.json exists with build/test/lint pipelines.

INFRA-02 (shared types): Read packages/core/src/types.ts — confirm exported interfaces (ScanResult, ScoreResult, DimensionScore, etc.). Read packages/core/src/constants.ts — confirm DIMENSION_DEFS, GRADE_THRESHOLDS, DEFAULT_CONFIG, AI_CRAWLERS exports. NOTE: packages/config is a stub (has `echo 'Phase 2 stub'` in build script) — INFRA-02 is satisfied by packages/core, NOT packages/config.

INFRA-03 (Biome): Confirm biome.json at repo root. Run `pnpm biome check .` and record result. Must pass with zero errors.

SCAN-01 (zero-config scan): Confirm packages/cli has bin entry in package.json pointing to scan command. Confirm scan command imports and calls scan() from @aeorank/core.

SCAN-02 (rate limiting): Read packages/core/src/scanner/ — confirm DEFAULT_CONFIG has maxPages:50, concurrency:3. Confirm pLimit usage for rate limiting. Confirm User-Agent string contains "AEOrank".

SCAN-03 (30-second): Read DEFAULT_CONFIG for timeout value. Check integration.test.ts for duration assertion. Per RESEARCH.md Pitfall 3: the timeout is per-request, not per-scan. Mark PASS with MEDIUM confidence caveat: "Implementation uses correct architecture (concurrency:3, per-request timeout:30s) and tracks duration, but no automated benchmark confirms < 30s for a 50-page site under real network conditions."

SCAN-04 (content extraction): Confirm ScannedPage type has fields for content, schema markup, robots.txt, llms.txt, headings, E-E-A-T signals. Confirm parser.ts extracts these.

SCORE-01 (12 dimensions, 80%+ structural): Count dimensions in DIMENSION_DEFS — must be exactly 12. Per RESEARCH.md Pitfall 4: "80% structural/deterministic" means all 12 dimensions are deterministic (no AI/random components), not that 80% of weight is in "structural" dimensions. Mark PASS and document interpretation.

SCORE-02 (letter grades): Confirm getGrade() function exists with A+/A/B/C/D/F thresholds.

SCORE-03 (dimension detail): Confirm DimensionScore type has score, weight, status, hint fields.

SCORE-04 (thresholds): Confirm STATUS_THRESHOLDS: pass>=70, warn>=40, fail<40. Confirm color mapping (green/yellow/red).

SCORE-05 (determinism): Confirm determinism.test.ts exists and tests identical scores across multiple runs.

GEN-01 through GEN-08: For each generator, confirm the source file exists in packages/core/src/generators/:
- GEN-01: llms-txt.ts
- GEN-02: llms-full.ts
- GEN-03: claude-md.ts
- GEN-04: schema-json.ts
- GEN-05: robots-patch.ts
- GEN-06: faq-blocks.ts
- GEN-07: citation-anchors.ts
- GEN-08: sitemap-ai.ts

Also confirm generators.test.ts covers each generator.

**Step 3 — Write 01-VERIFICATION.md:**

Use the VERIFICATION.md format from RESEARCH.md Pattern 1. Include:
- Header with date and verdict (PASS or GAPS_FOUND)
- Requirements table: ID | Description | Evidence | Status
- Evidence must cite specific files, test names, or command output — never just "SUMMARY.md says..."
- Gap Summary section (only if GAPS_FOUND)
- Use current requirement IDs (INFRA-XX, SCAN-XX, etc.) — never old REQ-XX numbering
  </action>
  <verify>
    <automated>test -f .planning/phases/06-retroactive-verification/01-VERIFICATION.md && grep -c "INFRA-01\|INFRA-02\|INFRA-03\|SCAN-01\|SCAN-02\|SCAN-03\|SCAN-04\|SCORE-01\|SCORE-02\|SCORE-03\|SCORE-04\|SCORE-05\|GEN-01\|GEN-02\|GEN-03\|GEN-04\|GEN-05\|GEN-06\|GEN-07\|GEN-08" .planning/phases/06-retroactive-verification/01-VERIFICATION.md</automated>
  </verify>
  <done>01-VERIFICATION.md exists with all 20 requirement IDs, each having an Evidence column with primary source citations (file paths, test names, or command output) and a Status of PASS or GAPS_FOUND. The grep count returns at least 20 (one per requirement ID in the table).</done>
</task>

</tasks>

<verification>
- 01-VERIFICATION.md exists at `.planning/phases/06-retroactive-verification/01-VERIFICATION.md`
- All 20 Phase 1 requirement IDs appear in the document
- Each requirement has primary evidence (not just SUMMARY.md references)
- SCAN-03 has a confidence caveat about per-request vs per-scan timeout
- SCORE-01 documents the "80% structural" interpretation
- INFRA-02 references packages/core, not packages/config
- Test suite output is cited as evidence
</verification>

<success_criteria>
01-VERIFICATION.md contains a requirements table with all 20 Phase 1 IDs, each with primary source evidence and PASS/GAPS_FOUND status. No requirement relies solely on SUMMARY.md for evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/06-retroactive-verification/06-01-SUMMARY.md`
</output>
