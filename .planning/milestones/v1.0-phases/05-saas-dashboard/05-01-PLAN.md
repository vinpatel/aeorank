---
phase: 05-saas-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/package.json
  - apps/web/next.config.ts
  - apps/web/tsconfig.json
  - apps/web/proxy.ts
  - apps/web/app/layout.tsx
  - apps/web/app/(dashboard)/layout.tsx
  - apps/web/app/sign-in/[[...sign-in]]/page.tsx
  - apps/web/app/sign-up/[[...sign-up]]/page.tsx
  - apps/web/lib/supabase.ts
  - apps/web/lib/validate-url.ts
  - apps/web/lib/validate-url.test.ts
  - supabase/schema.sql
autonomous: true
requirements:
  - DASH-01

must_haves:
  truths:
    - "Next.js 16 app boots with `pnpm --filter @aeorank/web dev` and renders a page"
    - "Clerk sign-in/sign-up pages render and redirect correctly"
    - "Protected /dashboard route redirects unauthenticated users to sign-in"
    - "Supabase client factory uses Clerk native accessToken() integration"
    - "SSRF validation blocks private IPs, loopback, and non-HTTP schemes"
  artifacts:
    - path: "apps/web/proxy.ts"
      provides: "Clerk auth middleware for Next.js 16"
      contains: "clerkMiddleware"
    - path: "apps/web/lib/supabase.ts"
      provides: "Server-side Supabase client with Clerk token"
      contains: "accessToken"
    - path: "apps/web/lib/validate-url.ts"
      provides: "SSRF-safe URL validation"
      exports: ["validateScanUrl"]
    - path: "supabase/schema.sql"
      provides: "Database schema for sites, scans, subscriptions"
      contains: "create table"
  key_links:
    - from: "apps/web/proxy.ts"
      to: "@clerk/nextjs/server"
      via: "clerkMiddleware import"
      pattern: "clerkMiddleware"
    - from: "apps/web/lib/supabase.ts"
      to: "@clerk/nextjs/server"
      via: "auth().getToken()"
      pattern: "accessToken"

user_setup:
  - service: clerk
    why: "User authentication and session management"
    env_vars:
      - name: NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY
        source: "Clerk Dashboard -> API Keys"
      - name: CLERK_SECRET_KEY
        source: "Clerk Dashboard -> API Keys"
  - service: supabase
    why: "Database for sites, scans, subscriptions"
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Supabase Dashboard -> Settings -> API -> URL"
      - name: NEXT_PUBLIC_SUPABASE_ANON_KEY
        source: "Supabase Dashboard -> Settings -> API -> anon key"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard -> Settings -> API -> service_role key"
    dashboard_config:
      - task: "Enable Clerk as third-party auth provider"
        location: "Supabase Dashboard -> Auth -> Third-party Auth -> Add Provider -> Clerk"
      - task: "Run schema.sql in SQL Editor"
        location: "Supabase Dashboard -> SQL Editor -> paste supabase/schema.sql"
---

<objective>
Set up the Next.js 16 foundation with Clerk authentication, Supabase database client, SSRF-safe URL validation, and the complete database schema.

Purpose: Every subsequent plan depends on auth, database access, and URL validation. This must land first.
Output: Bootable Next.js 16 app with Clerk sign-in/sign-up, protected dashboard layout, Supabase client factory, SSRF validation utility, and SQL schema file.
</objective>

<execution_context>
@/Users/vin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-saas-dashboard/05-RESEARCH.md

<interfaces>
<!-- Key types from @aeorank/core that the dashboard will consume -->

From packages/core/src/types.ts:
```typescript
export interface ScanResult {
  url: string;
  siteName: string;
  siteDescription: string;
  score: number;
  grade: string;
  dimensions: DimensionScore[];
  files: GeneratedFile[];
  pages: ScannedPage[];
  meta: ScanMeta;
  pagesScanned: number;
  duration: number;
  scannedAt: string;
}

export interface DimensionScore {
  id: string;
  name: string;
  score: number;
  maxScore: number;
  weight: "high" | "medium" | "low";
  status: "pass" | "warn" | "fail";
  hint: string;
}

export interface GeneratedFile {
  name: string;
  content: string;
}
```

From packages/core/src/index.ts:
```typescript
export async function scan(url: string, config?: Partial<ScanConfig>, customFetcher?: FetcherFn): Promise<ScanResult>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Next.js 16 project bootstrap with Clerk auth and Supabase client</name>
  <files>
    apps/web/package.json,
    apps/web/next.config.ts,
    apps/web/tsconfig.json,
    apps/web/proxy.ts,
    apps/web/app/layout.tsx,
    apps/web/app/(dashboard)/layout.tsx,
    apps/web/app/(dashboard)/dashboard/page.tsx,
    apps/web/app/sign-in/[[...sign-in]]/page.tsx,
    apps/web/app/sign-up/[[...sign-up]]/page.tsx,
    apps/web/lib/supabase.ts,
    supabase/schema.sql
  </files>
  <action>
    1. Replace stub `apps/web/package.json` with real Next.js 16 project. Install dependencies:
       `pnpm --filter @aeorank/web add next@^16 react react-dom @clerk/nextjs @supabase/supabase-js zod`
       `pnpm --filter @aeorank/web add -D @types/react @types/react-dom typescript`

    2. Create `apps/web/next.config.ts` with:
       - `outputFileTracingRoot: path.join(__dirname, "../../")` for monorepo @aeorank/core tracing
       - Turbopack is default in Next.js 16, no extra config needed

    3. Create `apps/web/tsconfig.json` extending `../../tsconfig.base.json` with:
       - `"jsx": "preserve"`, `"paths": { "@/*": ["./app/*", "./*"] }`
       - `"module": "esnext"`, `"moduleResolution": "bundler"` (Next.js 16 App Router convention)

    4. Create `apps/web/proxy.ts` (NOT middleware.ts — Next.js 16):
       - Import clerkMiddleware, createRouteMatcher from @clerk/nextjs/server
       - Protect routes: /dashboard(.*)`, /sites(.*)`, /upgrade(.*)`
       - Make public: `/api/scan/process(.*)` (QStash callback), `/api/webhooks/(.*)` (Stripe webhook)
       - Export config.matcher per Clerk docs

    5. Create `apps/web/app/layout.tsx`:
       - Import ClerkProvider from @clerk/nextjs
       - Wrap children in ClerkProvider
       - Add basic html/body structure with Inter font via next/font/google
       - Add metadata: title "AEOrank Dashboard", description

    6. Create `apps/web/app/(dashboard)/layout.tsx`:
       - Import auth from @clerk/nextjs/server
       - Call `await auth()` — if no userId, redirect to /sign-in
       - Render children with minimal sidebar/nav shell (nav with links: Dashboard, Upgrade)

    7. Create `apps/web/app/(dashboard)/dashboard/page.tsx`:
       - Placeholder: "Your sites" heading, "Add a site" text
       - Server Component (no "use client")

    8. Create sign-in and sign-up pages:
       - `apps/web/app/sign-in/[[...sign-in]]/page.tsx` — renders `<SignIn />` from @clerk/nextjs
       - `apps/web/app/sign-up/[[...sign-up]]/page.tsx` — renders `<SignUp />` from @clerk/nextjs

    9. Create `apps/web/lib/supabase.ts`:
       - `createServerSupabaseClient()`: Uses Clerk native integration with `accessToken()` factory
       - `createServiceSupabaseClient()`: Uses SUPABASE_SERVICE_ROLE_KEY for server-to-server (QStash handler)
       - Follow exact pattern from research: `{ async accessToken() { return (await auth()).getToken(); } }`

    10. Create `supabase/schema.sql` with the complete schema from research:
        - `sites` table (id uuid, user_id text, url text, name text, created_at)
        - `scans` table (id uuid, site_id uuid FK, user_id text, status text, score int, grade text, dimensions jsonb, files jsonb, pages_scanned int, duration_ms int, error text, scanned_at timestamptz)
        - `subscriptions` table (id uuid, user_id text unique, stripe_customer_id, stripe_subscription_id, plan text default 'free', status text default 'active', current_period_end timestamptz, updated_at)
        - RLS policies: users_own_sites, users_own_scans, users_own_subscription (select only — webhook updates via service role)
        - Enable uuid-ossp extension

    11. Update `apps/web/package.json` scripts:
        - `"dev": "next dev --turbopack"`, `"build": "next build"`, `"start": "next start"`
        - Add `@aeorank/core` as workspace dependency: `"@aeorank/core": "workspace:*"`

    12. Create `.env.example` in apps/web/ with all required env var placeholders (NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY, CLERK_SECRET_KEY, NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY, NEXT_PUBLIC_APP_URL).
  </action>
  <verify>
    <automated>cd /Users/vin/Development/aeorank && pnpm --filter @aeorank/web build 2>&1 | tail -20</automated>
  </verify>
  <done>Next.js 16 app builds successfully. Clerk sign-in/sign-up pages exist. Protected dashboard layout redirects unauthenticated users. Supabase client factory exports both authenticated and service-role clients. Schema SQL file is complete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SSRF-safe URL validation with tests</name>
  <files>apps/web/lib/validate-url.ts, apps/web/lib/validate-url.test.ts</files>
  <behavior>
    - Test 1: Valid HTTPS URL returns normalized URL string
    - Test 2: Valid HTTP URL returns normalized URL string
    - Test 3: Throws on non-HTTP scheme (ftp://example.com)
    - Test 4: Throws on localhost
    - Test 5: Throws on 127.0.0.1
    - Test 6: Throws on ::1
    - Test 7: Throws on 10.x.x.x private range
    - Test 8: Throws on 172.16.x.x private range
    - Test 9: Throws on 192.168.x.x private range
    - Test 10: Throws on 169.254.x.x (AWS metadata)
    - Test 11: Throws on malformed URL
    - Test 12: Throws on empty string
  </behavior>
  <action>
    Create `apps/web/lib/validate-url.ts` implementing the SSRF validation from the research:
    - Parse with Zod `z.string().url()` + `new URL()`
    - Block non-HTTP/HTTPS schemes
    - Block BLOCKED_HOSTS set: localhost, 127.0.0.1, ::1, 0.0.0.0
    - Block PRIVATE_IP_RANGES: 10.x, 172.16-31.x, 192.168.x, 169.254.x, fc00:, fe80:
    - Return normalized URL string on success, throw descriptive Error on failure

    Create `apps/web/lib/validate-url.test.ts` with all behavior cases above.
    Use vitest (add as dev dependency if not present).
  </action>
  <verify>
    <automated>cd /Users/vin/Development/aeorank && pnpm --filter @aeorank/web exec vitest run lib/validate-url.test.ts 2>&1</automated>
  </verify>
  <done>All 12 SSRF validation tests pass. validateScanUrl() correctly blocks all private/loopback addresses and non-HTTP schemes.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @aeorank/web build` completes without errors
- SSRF validation tests all pass
- proxy.ts exists with clerkMiddleware protecting dashboard routes
- supabase/schema.sql contains all three tables with RLS policies
</verification>

<success_criteria>
Next.js 16 app is bootable, Clerk auth is configured, Supabase client factory works with Clerk native integration, SSRF validation blocks all private IPs, and the complete database schema is ready to deploy to Supabase.
</success_criteria>

<output>
After completion, create `.planning/phases/05-saas-dashboard/05-01-SUMMARY.md`
</output>
