---
phase: 05-saas-dashboard
plan: 04
type: execute
wave: 3
depends_on:
  - 05-02
  - 05-03
files_modified:
  - apps/web/components/ScoreChart.tsx
  - apps/web/app/(dashboard)/sites/[siteId]/page.tsx
  - apps/web/app/api/download/[siteId]/route.ts
  - apps/web/components/DownloadButton.tsx
autonomous: true
requirements:
  - DASH-03
  - DASH-04

must_haves:
  truths:
    - "User can see a 30-day sparkline chart of their site's AEO score history"
    - "User can click Download and receive a ZIP containing all 8 generated files"
    - "Score chart renders even with a single data point (no crash on sparse data)"
    - "ZIP download requires authentication and only serves the user's own files"
  artifacts:
    - path: "apps/web/components/ScoreChart.tsx"
      provides: "Recharts 30-day sparkline chart"
      contains: "use client"
    - path: "apps/web/app/api/download/[siteId]/route.ts"
      provides: "GET endpoint returning ZIP of generated files"
      exports: ["GET"]
    - path: "apps/web/components/DownloadButton.tsx"
      provides: "Client button triggering ZIP download"
      contains: "use client"
  key_links:
    - from: "apps/web/components/ScoreChart.tsx"
      to: "Supabase scans table"
      via: "data prop from server component query"
      pattern: "AreaChart.*data"
    - from: "apps/web/app/api/download/[siteId]/route.ts"
      to: "jszip"
      via: "ZIP generation from JSONB files"
      pattern: "JSZip"
---

<objective>
Add score history visualization (30-day sparkline) and one-click ZIP download of all generated files to the site detail page.

Purpose: Score history shows progress over time (retention driver). ZIP download delivers the core value — the 8 generated files users need for AI visibility.
Output: ScoreChart component with Recharts sparkline, DownloadButton triggering ZIP creation from a server Route Handler.
</objective>

<execution_context>
@/Users/vin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-saas-dashboard/05-RESEARCH.md
@.planning/phases/05-saas-dashboard/05-02-SUMMARY.md

<interfaces>
From apps/web/lib/supabase.ts (Plan 01):
```typescript
export function createServerSupabaseClient(): SupabaseClient;
```

From packages/core/src/types.ts:
```typescript
export interface GeneratedFile { name: string; content: string; }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Score history sparkline chart</name>
  <files>
    apps/web/components/ScoreChart.tsx,
    apps/web/app/(dashboard)/sites/[siteId]/page.tsx
  </files>
  <action>
    1. Install recharts:
       `pnpm --filter @aeorank/web add recharts`

    2. Create `apps/web/components/ScoreChart.tsx` ("use client"):
       - Props: `{ data: Array<{ date: string; score: number }> }`
       - Use Recharts AreaChart with ResponsiveContainer (width 100%, height 80px)
       - XAxis with dataKey="date", hidden (sparkline style)
       - Tooltip showing score and formatted date
       - Area with monotone interpolation, stroke="#111", fill="#111" at 0.1 opacity, no dots
       - Handle edge case: if data has 1 point, render a single dot (not empty chart)
       - Follow exact pattern from research ScoreChart example

    3. Update `apps/web/app/(dashboard)/sites/[siteId]/page.tsx`:
       - Query last 30 days of scans for this site from Supabase: `scans.select("score, scanned_at").eq("site_id", siteId).eq("status", "complete").gte("scanned_at", thirtyDaysAgo).order("scanned_at", { ascending: true })`
       - Transform to `{ date: scan.scanned_at, score: scan.score }[]`
       - Render ScoreChart above ScoreBreakdown
       - Add section heading "Score History (30 days)"
  </action>
  <verify>
    <automated>cd /Users/vin/Development/aeorank && pnpm --filter @aeorank/web build 2>&1 | tail -20</automated>
  </verify>
  <done>ScoreChart renders a 30-day sparkline using Recharts. Site detail page shows score history above the dimension breakdown. Single data points render as a dot.</done>
</task>

<task type="auto">
  <name>Task 2: ZIP download route and button</name>
  <files>
    apps/web/app/api/download/[siteId]/route.ts,
    apps/web/components/DownloadButton.tsx,
    apps/web/app/(dashboard)/sites/[siteId]/page.tsx
  </files>
  <action>
    1. Install jszip:
       `pnpm --filter @aeorank/web add jszip`

    2. Create `apps/web/app/api/download/[siteId]/route.ts` (GET handler):
       - Auth check via Clerk — reject 401 if no userId
       - `const { siteId } = await params` (Next.js 16 async params!)
       - Query Supabase: latest complete scan for this site where user_id matches
       - If no scan found, return 404
       - Parse files from JSONB: `Array<{ name: string, content: string }>`
       - Create JSZip instance, add each file: `zip.file(file.name, file.content)`
       - Generate ZIP buffer: `zip.generateAsync({ type: "nodebuffer" })`
       - Return Response with Content-Type application/zip and Content-Disposition attachment filename="aeorank-{hostname}-files.zip"
       - Follow exact pattern from research ZIP download example

    3. Create `apps/web/components/DownloadButton.tsx` ("use client"):
       - Props: `{ siteId: string, disabled?: boolean }`
       - On click: fetch `/api/download/${siteId}` with credentials
       - Convert response to blob, create object URL, trigger download via hidden anchor click
       - Show loading spinner during download
       - Disable if no completed scan exists
       - Tailwind: black button matching 37signals style

    4. Update `apps/web/app/(dashboard)/sites/[siteId]/page.tsx`:
       - Add DownloadButton below ScoreChart, above ScoreBreakdown
       - Only show if latest scan status is "complete"
       - Label: "Download all files (ZIP)"
  </action>
  <verify>
    <automated>cd /Users/vin/Development/aeorank && pnpm --filter @aeorank/web build 2>&1 | tail -20</automated>
  </verify>
  <done>GET /api/download/[siteId] returns ZIP of all 8 generated files. DownloadButton triggers download on click. Site detail page shows download button when scan is complete.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @aeorank/web build` completes without errors
- ScoreChart component exists with Recharts sparkline
- Download route generates ZIP from JSONB files
- Site detail page shows chart + download button + score breakdown
</verification>

<success_criteria>
Site detail page shows 30-day score history sparkline and a download button that delivers a ZIP of all 8 generated files. Both features require authentication and respect data ownership.
</success_criteria>

<output>
After completion, create `.planning/phases/05-saas-dashboard/05-04-SUMMARY.md`
</output>
