---
phase: 07-marketing-content-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/marketing/src/components/Pricing.astro
  - apps/marketing/src/components/FAQ.astro
  - apps/marketing/src/components/Nav.astro
  - apps/marketing/src/components/Footer.astro
  - apps/marketing/src/components/CTASection.astro
autonomous: true
requirements:
  - SITE-02

must_haves:
  truths:
    - "Pricing section shows Pro, API, and Agency tiers as available with 'Start Free Trial' CTAs linking to https://app.aeorank.dev"
    - "FAQ 'Is AEOrank free?' answer mentions the shipped web dashboard with paid tiers starting at $29/month"
    - "FAQ 'Can I use this in CI?' answer mentions the official GitHub Action (aeorank/action@v1)"
    - "Nav bar contains a 'Dashboard' link pointing to https://app.aeorank.dev"
    - "Footer contains a 'Dashboard' link pointing to https://app.aeorank.dev"
    - "CTA section contains an 'Open Dashboard' tertiary link pointing to https://app.aeorank.dev"
  artifacts:
    - path: "apps/marketing/src/components/Pricing.astro"
      provides: "Available paid tiers with working CTAs"
      contains: "available: true"
    - path: "apps/marketing/src/components/FAQ.astro"
      provides: "Updated FAQ answers reflecting shipped features"
      contains: "aeorank/action@v1"
    - path: "apps/marketing/src/components/Nav.astro"
      provides: "Dashboard nav link"
      contains: "app.aeorank.dev"
    - path: "apps/marketing/src/components/Footer.astro"
      provides: "Dashboard footer link"
      contains: "app.aeorank.dev"
    - path: "apps/marketing/src/components/CTASection.astro"
      provides: "Dashboard tertiary CTA"
      contains: "app.aeorank.dev"
  key_links:
    - from: "Pricing.astro paid tier CTAs"
      to: "https://app.aeorank.dev"
      via: "ctaLink property in tiers array"
      pattern: "https://app\\.aeorank\\.dev"
    - from: "Nav.astro Dashboard link"
      to: "https://app.aeorank.dev"
      via: "anchor href"
      pattern: "app\\.aeorank\\.dev"
---

<objective>
Update five marketing site components to replace stale "Coming Soon" copy with accurate product state — dashboard links, available pricing tiers, and shipped feature references.

Purpose: The marketing site currently says the GitHub Action and dashboard are "coming soon" even though both are shipped. This creates a credibility gap for visitors.
Output: Five updated Astro components with accurate copy and working dashboard links.
</objective>

<execution_context>
@/Users/vin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-marketing-content-deployment/07-CONTEXT.md
@.planning/phases/07-marketing-content-deployment/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Pricing and FAQ data arrays</name>
  <files>apps/marketing/src/components/Pricing.astro, apps/marketing/src/components/FAQ.astro</files>
  <action>
**Pricing.astro** — In the frontmatter `tiers` array, update the three paid tiers (Pro, API, Agency). For each, change exactly three fields:
- `cta: "Coming Soon"` → `cta: "Start Free Trial"`
- `ctaLink: "#"` → `ctaLink: "https://app.aeorank.dev"`
- `available: false` → `available: true`

Do NOT change `highlighted` on any tier (leave it `false` on paid tiers). Do NOT touch the Free tier. Do NOT modify template markup — only data values in the frontmatter array.

**FAQ.astro** — In the frontmatter `faqs` array, update two answers by matching on question text (not array index):

1. Question "Is AEOrank free?" — Replace the answer string. The new answer must include the phrase "The web dashboard has paid tiers starting at $29/month for score history, charts, and file downloads" (per locked decision). Keep the part about the CLI being free and MIT-licensed. Remove "(coming soon)".

2. Question "Can I use this in CI?" — Replace the answer string. The new answer must include the phrase "use the official GitHub Action (aeorank/action@v1) to post scores as Check Runs and PR comments automatically" (per locked decision). Remove "coming soon". Keep the part about `npx aeorank scan --format json`.

Current stale text to find and replace:
- FAQ index 1: `"The CLI is free and MIT-licensed. Run unlimited scans, generate all 8 files, pipe JSON to CI — no account needed. The web dashboard (coming soon) has paid tiers starting at $29/month."`
- FAQ index 5: `"Yes. Run npx aeorank scan --format json in any CI pipeline. A GitHub Action that posts scores as Check Runs and PR comments is coming soon."`
  </action>
  <verify>
    <automated>cd /Users/vin/Development/aeorank && grep -c "Coming Soon" apps/marketing/src/components/Pricing.astro | grep -q "^0$" && grep -c "coming soon" apps/marketing/src/components/FAQ.astro | grep -q "^0$" && echo "PASS: No stale copy found" || echo "FAIL: Stale copy remains"</automated>
  </verify>
  <done>All three paid tiers show available: true with "Start Free Trial" CTA linking to https://app.aeorank.dev. Both FAQ answers reference shipped features with zero "coming soon" text.</done>
</task>

<task type="auto">
  <name>Task 2: Add Dashboard links to Nav, Footer, and CTASection</name>
  <files>apps/marketing/src/components/Nav.astro, apps/marketing/src/components/Footer.astro, apps/marketing/src/components/CTASection.astro</files>
  <action>
**Nav.astro** — Add a "Dashboard" link inside the center nav `<div>` (the one with `class="flex items-center justify-center gap-8"`), after the last existing link. Use the same classes as adjacent nav links:
```html
<a href="https://app.aeorank.dev" class="text-sm font-medium text-text-muted transition-colors hover:text-text">Dashboard</a>
```
Style it as a regular nav item (per user decision: "regular nav item, not a styled button").

**Footer.astro** — Add a "Dashboard" link in the Product column `<ul>` (which currently has Documentation, CLI Reference, Pricing). Add as a new `<li>` at the end, using the same classes as sibling links:
```html
<li><a href="https://app.aeorank.dev" class="text-sm text-text-muted hover:text-text transition-colors">Dashboard</a></li>
```

**CTASection.astro** — Add an "Open Dashboard" tertiary link after the existing two CTA links. Use plain text link styling (not btn-primary or btn-secondary) to maintain visual hierarchy. Place inside the existing button row `<div>`:
```html
<a href="https://app.aeorank.dev" class="text-sm font-medium text-text-muted transition-colors hover:text-text">Open Dashboard →</a>
```

Use the canonical URL `https://app.aeorank.dev` (no trailing slash) consistently across all three files.
  </action>
  <verify>
    <automated>cd /Users/vin/Development/aeorank && grep -c "app.aeorank.dev" apps/marketing/src/components/Nav.astro apps/marketing/src/components/Footer.astro apps/marketing/src/components/CTASection.astro | grep -v ":0$" | wc -l | grep -q "^3$" && pnpm --filter marketing build 2>&1 | tail -5</automated>
  </verify>
  <done>Dashboard link appears in Nav, Footer, and CTASection. All three link to https://app.aeorank.dev. Marketing site builds with zero errors.</done>
</task>

</tasks>

<verification>
1. `grep -r "coming soon" apps/marketing/src/components/ --include="*.astro" -i` returns zero matches
2. `grep -r "Coming Soon" apps/marketing/src/components/ --include="*.astro"` returns zero matches
3. `grep -c "app.aeorank.dev" apps/marketing/src/components/{Nav,Footer,CTASection,Pricing}.astro` — all show at least 1 match
4. `pnpm --filter marketing build` completes with zero errors
</verification>

<success_criteria>
- Zero instances of "coming soon" or "Coming Soon" in any marketing component
- All three paid pricing tiers have available: true, cta: "Start Free Trial", ctaLink: "https://app.aeorank.dev"
- Dashboard link present in Nav, Footer, and CTASection pointing to https://app.aeorank.dev
- Marketing site builds successfully
</success_criteria>

<output>
After completion, create `.planning/phases/07-marketing-content-deployment/07-01-SUMMARY.md`
</output>
