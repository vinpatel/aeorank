---
phase: 09-content-structure-expansion
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/scanner/parser.ts
  - packages/core/src/types.ts
  - packages/core/src/scorer/dimensions.ts
  - packages/core/src/constants.ts
  - packages/core/src/scorer/index.ts
  - packages/core/src/__tests__/dimensions.test.ts
  - packages/core/src/__tests__/scorer.test.ts
  - packages/core/src/__tests__/integration.test.ts
autonomous: true
requirements: [CSTR-01, CSTR-02, CSTR-03]
must_haves:
  truths:
    - "ScannedPage has questionHeadings array with heading text and level"
    - "ScannedPage has tableCount and listCount numeric fields"
    - "scoreQaFormat returns 0-10 based on percentage of question-format headings"
    - "scoreDirectAnswerDensity returns 0-10 based on concise answer paragraphs after question headings"
    - "scoreQueryAnswerAlignment returns 0-10 based on every question heading being followed by a direct answer"
  artifacts:
    - path: "packages/core/src/types.ts"
      provides: "ScannedPage with questionHeadings, tableCount, listCount"
      contains: "questionHeadings"
    - path: "packages/core/src/scorer/dimensions.ts"
      provides: "scoreQaFormat, scoreDirectAnswerDensity, scoreQueryAnswerAlignment"
      exports: ["scoreQaFormat", "scoreDirectAnswerDensity", "scoreQueryAnswerAlignment"]
    - path: "packages/core/src/constants.ts"
      provides: "22 DIMENSION_DEFS entries"
      contains: "qa-format"
  key_links:
    - from: "packages/core/src/scorer/dimensions.ts"
      to: "packages/core/src/types.ts"
      via: "ScannedPage.questionHeadings"
      pattern: "questionHeadings"
    - from: "packages/core/src/constants.ts"
      to: "packages/core/src/scorer/dimensions.ts"
      via: "DIMENSION_SCORERS registry"
      pattern: "qa-format.*direct-answer-density.*query-answer-alignment"
---

<objective>
Add parser extensions for question heading detection, table/list counting, and 3 Q&A-focused Content Structure scorers (qa-format, direct-answer-density, query-answer-alignment).

Purpose: These 3 dimensions measure how well content uses Q&A formatting — the most impactful content structure signal for AI engines that extract direct answers.
Output: ScannedPage extended with questionHeadings/tableCount/listCount, 3 new scorers registered, DIMENSION_DEFS at 22 entries, all tests passing.
</objective>

<execution_context>
@/Users/vin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-answer-readiness/08-01-SUMMARY.md
@.planning/phases/08-answer-readiness/08-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From packages/core/src/types.ts:
```typescript
export interface ScannedPage {
  url: string;
  title: string;
  metaDescription: string;
  headings: Heading[];
  bodyText: string;
  schemaOrg: object[];
  links: PageLink[];
  canonical: string | null;
  robotsMeta: string | null;
  language: string | null;
  wordCount: number;
  hasDatePublished: boolean;
  authorName: string | null;
  paragraphs: string[];
  sentences: string[];
  contentHash: string;
}

export interface Heading {
  level: number;
  text: string;
  id: string | null;
}
```

From packages/core/src/scorer/dimensions.ts:
```typescript
type DimensionScorer = (pages: ScannedPage[], meta: ScanMeta) => DimensionScore;

export const DIMENSION_SCORERS: Record<string, DimensionScorer> = {
  // ... 19 entries currently
};

function makeDimension(id: string, name: string, score: number, weight: "high" | "medium" | "low", hint: string): DimensionScore;
```

From packages/core/src/constants.ts:
```typescript
export const DIMENSION_DEFS: DimensionDef[] = [
  // ... 19 entries currently
];
```

From packages/core/src/scorer/index.ts:
```typescript
const PAGE_LEVEL_DIMENSIONS = [
  "content-structure", "answer-first", "meta-descriptions", "page-freshness",
  "citation-anchors", "schema-markup", "eeat-signals", "fact-density",
  "duplicate-content", "evidence-packaging", "citation-ready-writing",
];
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend parser and ScannedPage with question headings, table/list counts</name>
  <files>packages/core/src/types.ts, packages/core/src/scanner/parser.ts, packages/core/src/__tests__/dimensions.test.ts, packages/core/src/__tests__/scorer.test.ts, packages/core/src/__tests__/integration.test.ts</files>
  <read_first>
    - packages/core/src/types.ts (ScannedPage interface — add new fields)
    - packages/core/src/scanner/parser.ts (parsePage function — add extraction)
    - packages/core/src/__tests__/dimensions.test.ts (makePage helper — add defaults)
    - packages/core/src/__tests__/scorer.test.ts (makePage helper — add defaults)
    - packages/core/src/__tests__/integration.test.ts (check for makePage or hardcoded counts)
  </read_first>
  <behavior>
    - Test: parsePage with HTML containing `<h2>What is AEO?</h2>` produces questionHeadings with `{text: "What is AEO?", level: 2}`
    - Test: parsePage with HTML containing `<h3>How do I start?</h3>` produces questionHeadings entry
    - Test: parsePage with HTML containing `<h2>Getting Started</h2>` (no question word) produces empty questionHeadings
    - Test: parsePage with `<table><thead>...</thead></table>` and `<ul><li>...</li></ul>` produces tableCount=1, listCount=1
    - Test: parsePage with no tables or lists produces tableCount=0, listCount=0
  </behavior>
  <action>
    1. Add 3 new fields to ScannedPage in types.ts:
       - `questionHeadings: { text: string; level: number }[]` — headings starting with What/How/Why/When/Where/Who/Is/Are/Do/Does/Did/Can/Could/Should or containing "?"
       - `tableCount: number` — count of `<table>` elements with at least one `<th>` or `<thead>`
       - `listCount: number` — count of `<ol>` and `<ul>` elements with at least 2 `<li>` children

    2. In parser.ts parsePage(), add extraction BEFORE DOM mutation (same pattern as paragraphs):
       - `extractQuestionHeadings(headings)` — filter the already-extracted headings array for question patterns. Regex: `/^(What|How|Why|When|Where|Who|Is|Are|Do|Does|Did|Can|Could|Should)\b/i` OR heading text contains "?"
       - `countTables($)` — `$("table").filter((_, el) => $(el).find("th, thead").length > 0).length`
       - `countLists($)` — `$("ol, ul").filter((_, el) => $(el).children("li").length >= 2).length`

    3. Update makePage() helper in dimensions.test.ts and scorer.test.ts to include defaults: `questionHeadings: [], tableCount: 0, listCount: 0`

    4. Check integration.test.ts for any makePage or mock page objects — update those too.

    5. Run `pnpm --filter @aeorank/core run typecheck` to verify no type errors.
  </action>
  <verify>
    <automated>cd /Users/vin/Development/aeorank && pnpm --filter @aeorank/core run typecheck && pnpm --filter @aeorank/core run test -- --run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "questionHeadings" packages/core/src/types.ts` returns >= 1
    - `grep -c "tableCount" packages/core/src/types.ts` returns >= 1
    - `grep -c "listCount" packages/core/src/types.ts` returns >= 1
    - `grep -c "questionHeadings" packages/core/src/scanner/parser.ts` returns >= 1
    - `grep -c "tableCount.*0" packages/core/src/__tests__/dimensions.test.ts` returns >= 1
    - All existing tests still pass (148+)
  </acceptance_criteria>
  <done>ScannedPage has questionHeadings, tableCount, listCount fields. Parser extracts them. All existing tests pass with no type errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement 3 Q&A Content Structure scorers (qa-format, direct-answer-density, query-answer-alignment)</name>
  <files>packages/core/src/scorer/dimensions.ts, packages/core/src/constants.ts, packages/core/src/scorer/index.ts, packages/core/src/__tests__/dimensions.test.ts, packages/core/src/__tests__/scorer.test.ts, packages/core/src/__tests__/integration.test.ts</files>
  <read_first>
    - packages/core/src/scorer/dimensions.ts (DIMENSION_SCORERS registry, makeDimension helper, existing scorer patterns)
    - packages/core/src/constants.ts (DIMENSION_DEFS array — append 3 new entries)
    - packages/core/src/scorer/index.ts (PAGE_LEVEL_DIMENSIONS — add new page-level ones)
    - packages/core/src/__tests__/dimensions.test.ts (test patterns for existing scorers)
    - packages/core/src/__tests__/scorer.test.ts (hardcoded dimension count — update to 22)
    - packages/core/src/__tests__/integration.test.ts (hardcoded dimension count — update to 22)
  </read_first>
  <behavior>
    scoreQaFormat:
    - Test: pages with 5/6 question headings -> score >= 8 (high Q&A format)
    - Test: pages with 0 question headings -> score = 0
    - Test: empty pages array -> score = 0

    scoreDirectAnswerDensity:
    - Test: pages where each question heading is followed by a paragraph < 300 chars -> score >= 8
    - Test: pages with no question headings -> score = 0
    - Test: empty pages array -> score = 0

    scoreQueryAnswerAlignment:
    - Test: all question headings have a direct answer paragraph within 2 paragraphs -> score >= 8
    - Test: question headings with no following paragraph -> score = 0
    - Test: empty pages array -> score = 0
  </behavior>
  <action>
    Write failing tests first (RED), then implement (GREEN).

    **scoreQaFormat** (id: "qa-format", name: "Q&A Content Format", weight: "medium"):
    - Count question-format headings across all pages using page.questionHeadings.length
    - Count total headings across all pages using page.headings.length
    - Calculate ratio of question headings to total headings
    - Scoring: ratio > 0.4 = 10, > 0.25 = 7, > 0.1 = 4, > 0 = 2, else 0
    - Hint: "Use question-format headings (What, How, Why) to structure content for AI answer extraction"

    **scoreDirectAnswerDensity** (id: "direct-answer-density", name: "Direct Answer Density", weight: "medium"):
    - For each page, check each question heading: find the first paragraph that appears in page.paragraphs AFTER the question heading's position in the content
    - Approach: For each questionHeading, find paragraphs that follow it by checking if bodyText contains the heading text followed (eventually) by a paragraph. Simpler approach: count questionHeadings that have at least one paragraph <= 300 chars in page.paragraphs (any paragraph on the page). If questionHeadings exist, ratio = concise-paragraph-count / total-paragraph-count, weighted by question heading presence.
    - Actually simplest correct approach: For pages with questionHeadings, check what % of paragraphs are "direct answers" (40-300 chars, starts with capital, declarative sentence). Pages with questionHeadings AND high direct-answer paragraph ratio score well.
    - Scoring: avg direct-answer ratio (across pages with question headings) > 0.5 = 10, > 0.3 = 7, > 0.15 = 4, > 0 = 2, else 0. Pages without question headings are excluded from average. If no pages have question headings, score = 0.
    - Hint: "Add concise answer paragraphs (40-300 chars) immediately after question headings"

    **scoreQueryAnswerAlignment** (id: "query-answer-alignment", name: "Query-Answer Alignment", weight: "low"):
    - For each page, check alignment between question headings and content structure
    - Approach: For each page with questionHeadings, check if page.paragraphs.length >= page.questionHeadings.length (i.e., there are at least as many paragraphs as questions — meaning each question has content). Then check ratio of pages where paragraphs >= questionHeadings to total pages with questionHeadings.
    - Scoring: ratio > 0.7 = 10, > 0.5 = 7, > 0.3 = 4, > 0 = 2, else 0. If no pages have question headings, score = 0.
    - Hint: "Ensure every question heading is followed by a direct answer paragraph"

    Register all 3 in DIMENSION_SCORERS map.
    Add 3 entries to DIMENSION_DEFS in constants.ts (after citation-ready-writing).
    Add "qa-format", "direct-answer-density" to PAGE_LEVEL_DIMENSIONS in scorer/index.ts ("query-answer-alignment" is also page-level, add it too).
    Update hardcoded dimension count in scorer.test.ts and integration.test.ts from 19 to 22.
  </action>
  <verify>
    <automated>cd /Users/vin/Development/aeorank && pnpm --filter @aeorank/core run typecheck && pnpm --filter @aeorank/core run test -- --run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "scoreQaFormat" packages/core/src/scorer/dimensions.ts` returns >= 2 (function + registry)
    - `grep -c "scoreDirectAnswerDensity" packages/core/src/scorer/dimensions.ts` returns >= 2
    - `grep -c "scoreQueryAnswerAlignment" packages/core/src/scorer/dimensions.ts` returns >= 2
    - `grep -c "qa-format" packages/core/src/constants.ts` returns >= 1
    - `grep -c "direct-answer-density" packages/core/src/constants.ts` returns >= 1
    - `grep -c "query-answer-alignment" packages/core/src/constants.ts` returns >= 1
    - `grep "qa-format\|direct-answer-density\|query-answer-alignment" packages/core/src/scorer/index.ts | wc -l` returns >= 3
    - All tests pass (148 existing + 9+ new scorer tests)
  </acceptance_criteria>
  <done>3 Q&A scorers implemented, registered in DIMENSION_SCORERS and DIMENSION_DEFS (22 entries). All tests pass with zero failures.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @aeorank/core run typecheck` — 0 errors
- `pnpm --filter @aeorank/core run test -- --run` — all tests pass
- DIMENSION_DEFS has exactly 22 entries
- DIMENSION_SCORERS has exactly 22 entries
- ScannedPage includes questionHeadings, tableCount, listCount
</verification>

<success_criteria>
- ScannedPage extended with 3 new parser fields
- 3 new Content Structure scorers (qa-format, direct-answer-density, query-answer-alignment) score 0-10
- All existing + new tests pass with zero failures
- DIMENSION_DEFS at 22 entries
</success_criteria>

<output>
After completion, create `.planning/phases/09-content-structure-expansion/09-01-SUMMARY.md`
</output>
