---
phase: 14-new-file-generators
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/generators/ai-txt.ts
  - packages/core/src/generators/llms-full.ts
  - packages/core/src/generators/index.ts
  - packages/core/src/__tests__/generators.test.ts
autonomous: true
requirements:
  - FGEN-01
  - FGEN-02

must_haves:
  truths:
    - "generateFiles() returns 9 files including ai.txt"
    - "ai.txt contains machine-readable content licensing directives for AI crawlers"
    - "llms-full.txt includes Q&A pairs extracted from question headings"
    - "llms-full.txt includes definition blocks extracted from page content"
    - "llms-full.txt includes entity disambiguation section per page"
    - "All existing generator tests still pass"
  artifacts:
    - path: "packages/core/src/generators/ai-txt.ts"
      provides: "ai.txt generator function"
      exports: ["generateAiTxt"]
    - path: "packages/core/src/generators/llms-full.ts"
      provides: "Improved llms-full.txt with Q&A, definitions, entities"
      exports: ["generateLlmsFullTxt"]
    - path: "packages/core/src/generators/index.ts"
      provides: "Updated orchestrator returning 9 files"
      exports: ["generateFiles", "generateAiTxt"]
    - path: "packages/core/src/__tests__/generators.test.ts"
      provides: "Tests for ai.txt and improved llms-full.txt"
  key_links:
    - from: "packages/core/src/generators/index.ts"
      to: "packages/core/src/generators/ai-txt.ts"
      via: "import and call in generateFiles()"
      pattern: "generateAiTxt"
    - from: "packages/core/src/generators/llms-full.ts"
      to: "ScannedPage.questionHeadings"
      via: "reads questionHeadings, sentences, paragraphs from pages"
      pattern: "questionHeadings|sentences|paragraphs"
---

<objective>
Add ai.txt generator and improve llms-full.txt with Q&A pairs, definition blocks, and entity disambiguation. Update generateFiles() to return 9 files.

Purpose: FGEN-01 and FGEN-02 — produce content licensing and richer LLM context files that competitive tools check for.
Output: New ai-txt.ts generator, improved llms-full.ts, updated index.ts, tests for both.
</objective>

<execution_context>
@/Users/vin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

@packages/core/src/generators/index.ts
@packages/core/src/generators/llms-full.ts
@packages/core/src/generators/robots-patch.ts
@packages/core/src/types.ts
@packages/core/src/__tests__/generators.test.ts

<interfaces>
<!-- Key types the executor needs -->

From packages/core/src/types.ts:
```typescript
export interface ScanResult {
  url: string;
  siteName: string;
  siteDescription: string;
  score: number;
  grade: string;
  dimensions: DimensionScore[];
  pageScores: PageScore[];
  files: GeneratedFile[];
  pages: ScannedPage[];
  meta: ScanMeta;
  pagesScanned: number;
  duration: number;
  scannedAt: string;
}

export interface ScannedPage {
  url: string;
  title: string;
  paragraphs: string[];
  sentences: string[];
  questionHeadings: { text: string; level: number }[];
  bodyText: string;
  // ... other fields
}

export interface ScanMeta {
  aiTxt: string | null;  // Contents of /ai.txt file (null if not found)
  // ... other fields
}

export interface GeneratedFile {
  name: string;
  content: string;
}
```

From packages/core/src/generators/index.ts:
```typescript
export function generateFiles(result: ScanResult): GeneratedFile[]
// Currently returns 8 files, must return 9 after this plan
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ai.txt generator and wire into generateFiles</name>
  <files>packages/core/src/generators/ai-txt.ts, packages/core/src/generators/index.ts, packages/core/src/__tests__/generators.test.ts</files>
  <read_first>
    - packages/core/src/generators/index.ts
    - packages/core/src/generators/robots-patch.ts (pattern reference)
    - packages/core/src/types.ts (ScanMeta.aiTxt, ScanResult)
    - packages/core/src/__tests__/generators.test.ts (existing test patterns, makeMockResult)
  </read_first>
  <behavior>
    - Test: generateAiTxt(result) returns string starting with "# ai.txt"
    - Test: output contains "User-Agent: *" directive line
    - Test: output contains "Allow-AI-Training:" directive (set based on existing ai.txt presence or default to "Yes")
    - Test: output contains "Allow-AI-Inference:" directive
    - Test: output contains site URL and site name
    - Test: generateFiles() returns exactly 9 files
    - Test: generateFiles() file names include "ai.txt"
    - Test: all 9 files have non-empty content
  </behavior>
  <action>
    Create `packages/core/src/generators/ai-txt.ts` as a pure function `(result: ScanResult) => string`.

    ai.txt format (similar to robots.txt but for AI content licensing):
    ```
    # ai.txt - AI Content Licensing
    # Generated by AEOrank (https://aeorank.dev)
    #
    # Site: {result.siteName}
    # URL: {result.url}

    User-Agent: *
    Allow-AI-Training: Yes
    Allow-AI-Inference: Yes
    Allow-AI-Summarization: Yes
    Allow-AI-Attribution: Required

    # Content License
    License: CC-BY-4.0
    Attribution: {result.siteName} ({result.url})
    Contact: See {result.url} for contact information
    ```

    If `result.meta.aiTxt` already exists (non-null, non-empty), include a comment at the top: `# Note: Site already has ai.txt — this is a recommended template`.

    Wire into `packages/core/src/generators/index.ts`:
    - Import generateAiTxt
    - Add to exports
    - Add `{ name: "ai.txt", content: generateAiTxt(result) }` to generateFiles() array
    - Update JSDoc comment from "8" to "9"

    Update tests in generators.test.ts:
    - Update "returns exactly 8 files" to "returns exactly 9 files"
    - Update file names assertion to include "ai.txt"
    - Add `describe("generateAiTxt")` block with tests per behavior above
    - Ensure makeMockResult() has `aiTxt: null` in meta (add if missing) and all ScannedPage fields (questionHeadings, paragraphs, sentences, etc.)
  </action>
  <acceptance_criteria>
    - grep -q "generateAiTxt" packages/core/src/generators/ai-txt.ts
    - grep -q "generateAiTxt" packages/core/src/generators/index.ts
    - grep -q "ai.txt" packages/core/src/generators/index.ts
    - grep -q "User-Agent" packages/core/src/generators/ai-txt.ts
    - grep -q "Allow-AI-Training" packages/core/src/generators/ai-txt.ts
    - grep -q "Allow-AI-Inference" packages/core/src/generators/ai-txt.ts
    - grep -q "9 files" packages/core/src/__tests__/generators.test.ts
    - grep -q "generateAiTxt" packages/core/src/__tests__/generators.test.ts
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/vin/Development/aeorank && pnpm --filter @aeorank/core test -- --run src/__tests__/generators.test.ts</automated>
  </verify>
  <done>generateAiTxt produces machine-readable ai.txt with licensing directives. generateFiles() returns 9 files. All generator tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Improve llms-full.txt with Q&A pairs, definitions, and entity disambiguation</name>
  <files>packages/core/src/generators/llms-full.ts, packages/core/src/__tests__/generators.test.ts</files>
  <read_first>
    - packages/core/src/generators/llms-full.ts (current implementation)
    - packages/core/src/types.ts (ScannedPage — questionHeadings, paragraphs, sentences)
    - packages/core/src/__tests__/generators.test.ts (after Task 1 updates)
    - SCORING_ROADMAP.md (Phase 14 requirements)
  </read_first>
  <behavior>
    - Test: llms-full.txt output contains "## Q&A" section when page has questionHeadings
    - Test: Q&A section pairs question headings with following paragraph text
    - Test: llms-full.txt output contains "## Definitions" section when definition patterns found
    - Test: definition patterns match "X is defined as", "X refers to", "X means" sentences
    - Test: llms-full.txt output contains "## Key Entities" section with entity from page title
    - Test: pages without questionHeadings/definitions omit those sections (no empty headers)
    - Test: existing body text content is preserved (regression)
    - Test: page separator "---" still present between pages
  </behavior>
  <action>
    Rewrite `packages/core/src/generators/llms-full.ts` to enrich each page section with structured blocks after the body text.

    For each page (sorted by URL for determinism), output:
    ```
    # {page.title || "Untitled"}
    URL: {page.url}

    {page.bodyText}

    ## Q&A
    (Only if page.questionHeadings.length > 0)
    For each questionHeading, find the first paragraph in page.paragraphs that appears after the heading position (heuristic: search bodyText for the heading text, then find paragraphs that follow). Simple approach: pair each questionHeading with the corresponding paragraph by index from a filtered set.
    Format:
    **Q: {heading.text}**
    A: {first paragraph after heading, truncated to 500 chars}

    ## Definitions
    (Only if definition sentences found)
    Scan page.sentences for definition patterns:
    - /^.{3,50}\s+is defined as\s+/i
    - /^.{3,50}\s+refers to\s+/i
    - /^.{3,50}\s+(?:means|describes)\s+(?:the|a|an)\s+/i
    Format each as: - {sentence}

    ## Key Entities
    (Only if entity terms extractable from title)
    Extract entity terms from page.title: words >= 4 chars, not common stopwords.
    List as: - {term}: Referenced {count} times (count occurrences in bodyText, case-insensitive)
    Only include terms with 2+ occurrences.
    ```

    Omit any section (Q&A, Definitions, Key Entities) entirely if it would be empty for that page.

    Keep the "---" separator between pages. Keep URL-based sort for determinism.

    DEFINITION_PATTERNS constant: reuse the same patterns from Phase 9 scorer (DEFINITION_SENTENCE_PATTERNS). Do NOT import from dimensions.ts — duplicate the regex patterns as constants in llms-full.ts to keep generators dependency-free.

    ENTITY_STOPWORDS: simple set of common English words (the, and, for, with, from, this, that, your, about, more, best, guide, how, what, why, when, where, who). Only include terms >= 4 chars after filtering.

    Add tests to generators.test.ts:
    - Update makeMockResult() pages to include questionHeadings, paragraphs, and sentences with definition patterns so the new sections render.
    - Test Q&A section appears with correct question/answer pairs.
    - Test Definitions section appears with matching definition sentences.
    - Test Key Entities section appears with entity counts.
    - Test that pages without these features omit the sections.
  </action>
  <acceptance_criteria>
    - grep -q "Q&A" packages/core/src/generators/llms-full.ts
    - grep -q "Definitions" packages/core/src/generators/llms-full.ts
    - grep -q "Key Entities" packages/core/src/generators/llms-full.ts
    - grep -q "is defined as" packages/core/src/generators/llms-full.ts
    - grep -q "refers to" packages/core/src/generators/llms-full.ts
    - grep -q "questionHeadings" packages/core/src/generators/llms-full.ts
    - grep -q "Q&A" packages/core/src/__tests__/generators.test.ts
    - grep -q "Definitions" packages/core/src/__tests__/generators.test.ts
    - grep -q "Key Entities" packages/core/src/__tests__/generators.test.ts
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/vin/Development/aeorank && pnpm --filter @aeorank/core test -- --run src/__tests__/generators.test.ts</automated>
  </verify>
  <done>llms-full.txt includes Q&A pairs from questionHeadings, definition blocks from sentence patterns, and entity disambiguation from title terms. All generator tests pass including existing regression tests.</done>
</task>

</tasks>

<verification>
Run full core test suite to confirm no regressions:
```bash
cd /Users/vin/Development/aeorank && pnpm --filter @aeorank/core test -- --run
```

Verify file count:
```bash
cd /Users/vin/Development/aeorank && node -e "
const { generateFiles } = require('./packages/core/dist/generators/index.js');
// Quick smoke test that generateFiles signature exists and returns array
console.log('generateFiles is a function:', typeof generateFiles === 'function');
"
```
</verification>

<success_criteria>
- generateFiles() returns exactly 9 GeneratedFile objects
- ai.txt file contains User-Agent, Allow-AI-Training, Allow-AI-Inference directives
- llms-full.txt contains Q&A, Definitions, Key Entities sections for pages with that data
- All generator tests pass (existing + new)
- Full core test suite passes with zero failures
- Biome lint clean
</success_criteria>

<output>
After completion, create `.planning/phases/14-new-file-generators/14-01-SUMMARY.md`
</output>
