---
phase: 02-cli
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/cli/src/commands/init.ts
  - packages/cli/src/config.ts
  - packages/cli/src/__tests__/init.test.ts
  - packages/cli/src/__tests__/config.test.ts
autonomous: true
requirements: [CLI-03]

must_haves:
  truths:
    - "Running `aeorank init` creates an aeorank.config.js file in the current directory"
    - "The generated config file contains sensible defaults (site URL placeholder, output dir, scanner settings)"
    - "If aeorank.config.js already exists, the command warns and does not overwrite"
    - "Config loading merges user config with DEFAULT_CONFIG from core"
  artifacts:
    - path: "packages/cli/src/commands/init.ts"
      provides: "Init command that creates config file"
      exports: ["initCommand"]
    - path: "packages/cli/src/config.ts"
      provides: "Config loading and merging logic"
      exports: ["loadConfig", "CONFIG_FILENAME"]
  key_links:
    - from: "packages/cli/src/config.ts"
      to: "@aeorank/core"
      via: "import { DEFAULT_CONFIG } from '@aeorank/core'"
      pattern: "DEFAULT_CONFIG"
    - from: "packages/cli/src/commands/init.ts"
      to: "packages/cli/src/config.ts"
      via: "import { CONFIG_FILENAME } from '../config'"
      pattern: "CONFIG_FILENAME"
---

<objective>
Implement the `aeorank init` command and config loading system so users can customize scan behavior via a config file.

Purpose: Users need a way to persist scan settings (site URL, output dir, scanner config) so they don't repeat flags on every run.
Output: Working init command + config loader that merges user config with core defaults.
</objective>

<execution_context>
@/Users/vin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-cli/02-CONTEXT.md
@packages/core/src/types.ts
@packages/core/src/constants.ts

<interfaces>
From packages/core/src/types.ts:
```typescript
interface AeorankConfig {
  site: {
    url: string;
    name?: string;
    description?: string;
  };
  output: {
    dir: string;
  };
  scanner: Partial<ScanConfig>;
}

interface ScanConfig {
  maxPages: number;
  concurrency: number;
  timeout: number;
  userAgent: string;
  respectCrawlDelay: boolean;
}
```

From packages/core/src/constants.ts:
```typescript
const DEFAULT_CONFIG: ScanConfig = {
  maxPages: 50,
  concurrency: 3,
  timeout: 30_000,
  userAgent: "AEOrank/1.0 (+https://aeorank.dev)",
  respectCrawlDelay: true,
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Config loader with merging logic</name>
  <files>
    packages/cli/src/config.ts,
    packages/cli/src/__tests__/config.test.ts
  </files>
  <behavior>
    - loadConfig with no file returns DEFAULT_CONFIG values wrapped in AeorankConfig shape
    - loadConfig with valid config file merges user values over defaults
    - loadConfig with partial scanner config keeps unset fields from DEFAULT_CONFIG
    - loadConfig with explicit --config path loads from that path
    - loadConfig with missing --config path throws error with suggestion
    - CONFIG_FILENAME is "aeorank.config.js"
  </behavior>
  <action>
    1. Create `packages/cli/src/config.ts`:
       - Export `CONFIG_FILENAME = "aeorank.config.js"`
       - Export `async function loadConfig(configPath?: string): Promise<AeorankConfig | null>`
         - If `configPath` provided: resolve to absolute path, try dynamic `import()`, throw if not found
         - If no `configPath`: look for `aeorank.config.js` in process.cwd()
         - If found: dynamic `import()` the file, extract default export
         - If not found: return null (zero-config mode)
         - Validate the loaded config shape loosely (check site.url exists if site provided)
       - Export `function mergeConfig(userConfig: AeorankConfig | null, cliFlags: { maxPages?: number; output?: string }): { scanConfig: Partial<ScanConfig>; outputDir: string; siteUrl?: string }`
         - Start with DEFAULT_CONFIG values
         - Overlay user config scanner settings
         - Overlay CLI flags (--max-pages, --output override everything)
         - Return merged result

    2. Write tests `packages/cli/src/__tests__/config.test.ts`:
       - Test mergeConfig with null config returns defaults
       - Test mergeConfig with partial user config merges correctly
       - Test CLI flags override user config values
       - Test loadConfig returns null when no config file exists (use tmp dir)
       - Use vi.mock for fs operations or create temp files in test
  </action>
  <verify>
    <automated>cd packages/cli && npx vitest run src/__tests__/config.test.ts</automated>
  </verify>
  <done>
    - Config loading handles: no file (null), valid file (merged), explicit path
    - mergeConfig correctly layers: DEFAULT_CONFIG < user config < CLI flags
    - All tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Init command that generates config file</name>
  <files>
    packages/cli/src/commands/init.ts,
    packages/cli/src/index.ts,
    packages/cli/src/__tests__/init.test.ts
  </files>
  <behavior>
    - init command creates aeorank.config.js in current working directory
    - Generated config file is valid JavaScript with export default
    - Generated config contains site.url placeholder, output.dir default, scanner defaults
    - If aeorank.config.js already exists, print warning and exit without overwriting
    - init command prints success message with path to created file
  </behavior>
  <action>
    1. Create `packages/cli/src/commands/init.ts`:
       - Export `initCommand` as a Commander Command
       - Name: "init"
       - Description: "Create an aeorank.config.js configuration file"
       - Action handler:
         a. Check if `aeorank.config.js` exists in cwd
         b. If exists: print warning "aeorank.config.js already exists. Use --overwrite to replace it." and exit 1
         c. Add `--overwrite` option to allow replacing existing config
         d. Generate config file content:
            ```javascript
            /** @type {import('@aeorank/core').AeorankConfig} */
            export default {
              site: {
                url: "https://example.com",
              },
              output: {
                dir: "./aeorank-output",
              },
              scanner: {
                maxPages: 50,
                // concurrency: 3,
                // timeout: 30000,
              },
            };
            ```
         e. Write file to `path.join(process.cwd(), "aeorank.config.js")`
         f. Print success: "Created aeorank.config.js — edit the site URL and run `aeorank scan`"

    2. Wire init command into `packages/cli/src/index.ts`:
       - Import initCommand from ./commands/init.ts
       - Add to program: `program.addCommand(initCommand)`

    3. Write tests `packages/cli/src/__tests__/init.test.ts`:
       - Test: init creates config file in temp directory
       - Test: created file contains "export default" and site.url
       - Test: init with existing file prints warning and does not overwrite
       - Test: init with --overwrite replaces existing file
       - Use os.tmpdir() for isolated test directories
  </action>
  <verify>
    <automated>cd packages/cli && npx vitest run src/__tests__/init.test.ts</automated>
  </verify>
  <done>
    - `aeorank init` creates a valid aeorank.config.js with sensible defaults
    - Existing config is not silently overwritten
    - Success message guides user to next step
    - All tests pass
  </done>
</task>

</tasks>

<verification>
- `cd packages/cli && npx vitest run` — all config and init tests pass
- `cd packages/cli && npx tsc --noEmit` — no TypeScript errors
- Config merging correctly layers defaults < user config < CLI flags
- Init command creates valid JS config with JSDoc type annotation
</verification>

<success_criteria>
- `aeorank init` creates aeorank.config.js with sensible defaults
- Config loader handles zero-config (no file), user config, and CLI flag overrides
- Existing config files are not silently overwritten
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-cli/02-02-SUMMARY.md`
</output>
