---
phase: 01-core-engine
plan: 04
type: execute
wave: 3
depends_on: [02, 03]
files_modified:
  - packages/core/src/generators/index.ts
  - packages/core/src/generators/llms-txt.ts
  - packages/core/src/generators/llms-full.ts
  - packages/core/src/generators/claude-md.ts
  - packages/core/src/generators/schema-json.ts
  - packages/core/src/generators/robots-patch.ts
  - packages/core/src/generators/faq-blocks.ts
  - packages/core/src/generators/citation-anchors.ts
  - packages/core/src/generators/sitemap-ai.ts
  - packages/core/src/__tests__/generators.test.ts
autonomous: true
requirements: [GEN-01, GEN-02, GEN-03, GEN-04, GEN-05, GEN-06, GEN-07, GEN-08]

must_haves:
  truths:
    - "generateFiles(scanResult) returns exactly 8 GeneratedFile items"
    - "Each GeneratedFile has name and content as strings (no disk I/O)"
    - "llms.txt follows llmstxt.org spec: H1 title, blockquote summary, H2 sections with links"
    - "llms-full.txt contains full body text of all crawled pages"
    - "schema.json contains valid Organization + WebSite + FAQPage JSON-LD"
    - "All generators are pure functions — no side effects, no disk writes"
  artifacts:
    - path: "packages/core/src/generators/index.ts"
      provides: "generateFiles() orchestrator"
      exports: ["generateFiles"]
    - path: "packages/core/src/generators/llms-txt.ts"
      provides: "llms.txt generator per llmstxt.org spec"
      exports: ["generateLlmsTxt"]
    - path: "packages/core/src/generators/llms-full.ts"
      provides: "llms-full.txt with all page content"
      exports: ["generateLlmsFullTxt"]
    - path: "packages/core/src/generators/claude-md.ts"
      provides: "CLAUDE.md repo context generator"
      exports: ["generateClaudeMd"]
    - path: "packages/core/src/generators/schema-json.ts"
      provides: "JSON-LD schema.json generator"
      exports: ["generateSchemaJson"]
    - path: "packages/core/src/generators/robots-patch.ts"
      provides: "AI crawler robots directives"
      exports: ["generateRobotsPatch"]
    - path: "packages/core/src/generators/faq-blocks.ts"
      provides: "FAQ schema + speakable HTML"
      exports: ["generateFaqBlocks"]
    - path: "packages/core/src/generators/citation-anchors.ts"
      provides: "Heading anchor markup"
      exports: ["generateCitationAnchors"]
    - path: "packages/core/src/generators/sitemap-ai.ts"
      provides: "AI-optimized sitemap XML"
      exports: ["generateSitemapAi"]
  key_links:
    - from: "packages/core/src/generators/index.ts"
      to: "all 8 generator modules"
      via: "imports and aggregation"
      pattern: "import.*from.*\\./llms"
    - from: "packages/core/src/index.ts"
      to: "generators/index.ts"
      via: "re-export generateFiles"
      pattern: "export.*from.*generators"
---

<objective>
Build all 8 file generators that produce AI-readability files from scan results. Each generator is a pure function returning a string. The orchestrator returns an array of GeneratedFile objects.

Purpose: File generation is AEOrank's primary moat — no competitor generates all 8 files. These are the tangible deliverables users take away from every scan.
Output: `generateFiles()` function exported from @aeorank/core returning all 8 files.
</objective>

<execution_context>
@/Users/vin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-core-engine/01-CONTEXT.md
@.planning/phases/01-core-engine/01-RESEARCH.md
@.planning/phases/01-core-engine/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build llms.txt, llms-full.txt, CLAUDE.md, and schema.json generators</name>
  <files>packages/core/src/generators/llms-txt.ts, packages/core/src/generators/llms-full.ts, packages/core/src/generators/claude-md.ts, packages/core/src/generators/schema-json.ts, packages/core/src/__tests__/generators.test.ts</files>
  <behavior>
    llms.txt:
    - Output starts with "# {siteName}" as H1
    - Second element is "> {siteDescription}" blockquote
    - Pages grouped into H2 sections by path segment (e.g., /blog/* under "## Blog")
    - Each page listed as "- [title](url): description"
    - Pages without meta description use first 160 chars of body text

    llms-full.txt:
    - Each page separated by "---" divider
    - Each page has "# {title}\nURL: {url}\n\n{bodyText}"
    - Pages sorted by URL for determinism

    CLAUDE.md:
    - Contains "# {siteName}" header
    - Lists detected tech stack/platform
    - Lists page count and content structure summary
    - Includes directory-style overview of site sections

    schema.json:
    - Valid JSON with @context: "https://schema.org"
    - Contains Organization type with name and url
    - Contains WebSite type with name, url, and potentialAction SearchAction
    - Contains FAQPage type if Q&A content detected, with Question/Answer pairs
    - Output is pretty-printed JSON (2-space indent)
  </behavior>
  <action>
    Write tests FIRST for each generator using mock ScanResult data.

    Create packages/core/src/generators/llms-txt.ts:
    - generateLlmsTxt(result: ScanResult): string
    - Group pages by first path segment: /blog/*, /docs/*, /about → sections
    - Pages at root (/) go into a "Main" section
    - Sort sections alphabetically, sort pages within sections by URL
    - Follow llmstxt.org spec exactly: H1, blockquote, H2 sections with file lists

    Create packages/core/src/generators/llms-full.ts:
    - generateLlmsFullTxt(result: ScanResult): string
    - Sort pages by URL for determinism
    - Format: "# {title}\nURL: {url}\n\n{bodyText}\n\n---\n\n" for each page
    - No length limit on body text (this is the full version)

    Create packages/core/src/generators/claude-md.ts:
    - generateClaudeMd(result: ScanResult): string
    - Header with site name
    - "## Tech Stack" section with detected platform (from meta.platform)
    - "## Site Structure" section listing top-level URL paths as directories
    - "## Content Summary" with page count, avg word count, content types detected

    Create packages/core/src/generators/schema-json.ts:
    - generateSchemaJson(result: ScanResult): string
    - Build @graph array with Organization, WebSite entities
    - Organization: @type, name, url extracted from scan
    - WebSite: @type, name, url, potentialAction (SearchAction if search page detected)
    - If FAQ content detected (pages with Q&A patterns or existing FAQPage schema): add FAQPage entity
    - Return JSON.stringify(schema, null, 2)
  </action>
  <verify>
    <automated>cd packages/core && pnpm test -- --reporter=verbose src/__tests__/generators.test.ts 2>&1 | tail -30</automated>
  </verify>
  <done>llms.txt follows llmstxt.org format. llms-full.txt contains all page content. CLAUDE.md has site context. schema.json is valid JSON-LD with correct types. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build robots-patch, faq-blocks, citation-anchors, sitemap-ai generators + orchestrator</name>
  <files>packages/core/src/generators/robots-patch.ts, packages/core/src/generators/faq-blocks.ts, packages/core/src/generators/citation-anchors.ts, packages/core/src/generators/sitemap-ai.ts, packages/core/src/generators/index.ts, packages/core/src/index.ts</files>
  <behavior>
    robots-patch.txt:
    - Contains User-agent + Allow/Disallow lines for each AI crawler
    - Recommends allowing all 5 AI crawlers by default
    - If crawler already allowed in existing robots.txt, marks as "# Already configured"

    faq-blocks.html:
    - Contains valid HTML with FAQPage JSON-LD script tag
    - Extracts Q&A pairs from pages with FAQ-like patterns (h2/h3 followed by paragraph)
    - Each Q&A wrapped in itemscope/itemprop attributes for schema.org
    - Includes speakable meta tag in head

    citation-anchors.html:
    - Lists all headings that lack id attributes
    - Generates suggested id attributes (slugified heading text)
    - Output is copy-paste HTML snippet with anchored headings

    sitemap-ai.xml:
    - Valid XML with urlset namespace
    - Each URL has loc, lastmod (if available), and ai:summary (first 200 chars of body text)
    - Custom ai: namespace for AI-specific extensions
    - Sorted by URL for determinism

    generateFiles orchestrator:
    - Returns exactly 8 GeneratedFile items
    - Each has {name: string, content: string}
    - Names: "llms.txt", "llms-full.txt", "CLAUDE.md", "schema.json", "robots-patch.txt", "faq-blocks.html", "citation-anchors.html", "sitemap-ai.xml"
  </behavior>
  <action>
    Add tests FIRST for each generator.

    Create packages/core/src/generators/robots-patch.ts:
    - generateRobotsPatch(result: ScanResult): string
    - For each AI_CRAWLERS entry: if already allowed per meta.robotsTxt, add "# {crawler}: Already allowed"
    - Otherwise: "User-agent: {crawler}\nAllow: /\n"
    - Add header comment explaining the file purpose

    Create packages/core/src/generators/faq-blocks.ts:
    - generateFaqBlocks(result: ScanResult): string
    - Scan pages for FAQ patterns: h2/h3 headings containing "?" or FAQ-like text followed by paragraph content
    - Also extract existing FAQPage schema from scanned pages
    - Generate HTML with:
      - <script type="application/ld+json"> containing FAQPage schema
      - Visible HTML with itemprop="mainEntity" Q&A blocks
      - <meta name="speakable" content="..."> for speakable markup
    - If no Q&A content found, generate template with placeholder

    Create packages/core/src/generators/citation-anchors.ts:
    - generateCitationAnchors(result: ScanResult): string
    - Find all h2/h3 headings across pages that lack id attributes
    - Generate id from slugified heading text
    - Output HTML showing before/after: original heading → heading with id
    - Include a "Copy to clipboard" data attribute for each snippet

    Create packages/core/src/generators/sitemap-ai.ts:
    - generateSitemapAi(result: ScanResult): string
    - Generate valid XML sitemap with custom ai namespace
    - <?xml version="1.0" encoding="UTF-8"?>
    - <urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9" xmlns:ai="https://aeorank.dev/sitemap-ai">
    - Each page: <url><loc>{url}</loc><lastmod>{date}</lastmod><ai:summary>{first 200 chars}</ai:summary></url>
    - Sort URLs alphabetically for determinism

    Create packages/core/src/generators/index.ts:
    - generateFiles(result: ScanResult): GeneratedFile[]
    - Call each generator, return array of 8 GeneratedFile objects
    - Order: llms.txt, llms-full.txt, CLAUDE.md, schema.json, robots-patch.txt, faq-blocks.html, citation-anchors.html, sitemap-ai.xml

    Update packages/core/src/index.ts to re-export generateFiles from generators.

    Run full test suite and build.
  </action>
  <verify>
    <automated>cd packages/core && pnpm test -- --reporter=verbose 2>&1 | tail -40 && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>All 8 generators produce valid output. generateFiles returns exactly 8 items. All file names correct. All tests pass. Build succeeds.</done>
</task>

</tasks>

<verification>
- generateFiles returns exactly 8 GeneratedFile items
- All file names match spec: llms.txt, llms-full.txt, CLAUDE.md, schema.json, robots-patch.txt, faq-blocks.html, citation-anchors.html, sitemap-ai.xml
- llms.txt validates against llmstxt.org format
- schema.json is valid JSON with correct @context and @type values
- sitemap-ai.xml is valid XML
- No generators perform disk I/O — all return strings
- pnpm build succeeds with all generators included
</verification>

<success_criteria>
- All 8 file generators produce correct output from mock ScanResult data
- generateFiles() exported from @aeorank/core
- Pure functions — no side effects, no disk writes
- Output is deterministic (sorted, no timestamps unless from scan data)
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-engine/01-04-SUMMARY.md`
</output>
