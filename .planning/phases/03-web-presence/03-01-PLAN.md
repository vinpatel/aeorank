---
phase: 03-web-presence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/marketing/package.json
  - apps/marketing/astro.config.mjs
  - apps/marketing/tsconfig.json
  - apps/marketing/src/styles/global.css
  - apps/marketing/src/layouts/Base.astro
  - apps/marketing/src/components/Nav.astro
  - apps/marketing/src/components/Footer.astro
  - apps/marketing/src/pages/index.astro
  - apps/marketing/public/CNAME
  - apps/marketing/public/favicon.svg
autonomous: true
requirements: [SITE-01, SITE-03, SITE-04]

must_haves:
  truths:
    - Marketing site builds with zero errors via `pnpm build` from apps/marketing/
    - Homepage renders with warm off-white (#FAF9F7) background, near-black (#111) text, Inter font
    - Zero JavaScript in the built output for the index page (no <script> tags in HTML)
    - Nav bar has logo text, docs link, GitHub link, and CTA button
    - Footer has links and legal text
  artifacts:
    - apps/marketing/astro.config.mjs (Astro 5 + Tailwind 4 + Preact)
    - apps/marketing/src/styles/global.css (Tailwind with theme tokens)
    - apps/marketing/src/layouts/Base.astro (shared layout)
    - apps/marketing/src/pages/index.astro (homepage scaffold)
    - apps/marketing/public/CNAME (aeorank.dev)
  key_links:
    - Tailwind theme tokens define design system consumed by all components
    - Base layout wraps all pages with Nav + Footer
---

<objective>
Set up the marketing site foundation: Astro 5 with Tailwind CSS 4, Preact integration, 37signals-inspired design system, shared layout with nav and footer, and a scaffold homepage.

Purpose: Establish the build pipeline, design tokens, and component structure so Plan 03 can fill in homepage content sections.
Output: A building, styled Astro site at apps/marketing/ with the brand's visual identity.
</objective>

<execution_context>
@/Users/vin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-web-presence/03-CONTEXT.md
@.planning/phases/03-web-presence/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Astro 5 project with Tailwind 4 and Preact</name>
  <files>
    apps/marketing/package.json,
    apps/marketing/astro.config.mjs,
    apps/marketing/tsconfig.json,
    apps/marketing/src/styles/global.css,
    apps/marketing/public/CNAME,
    apps/marketing/public/favicon.svg
  </files>
  <action>
  Replace the stub package.json with a proper Astro 5 project. Do NOT use `create astro` — set it up manually in the existing directory.

  1. Update `apps/marketing/package.json`:
     - name: "@aeorank/marketing"
     - Add dependencies: astro (^5.18.0), @astrojs/preact, preact
     - Add devDependencies: tailwindcss (^4.0.0), @tailwindcss/vite
     - Scripts: "dev": "astro dev", "build": "astro build", "preview": "astro preview"

  2. Create `apps/marketing/astro.config.mjs`:
     ```js
     import { defineConfig } from 'astro/config';
     import tailwindcss from '@tailwindcss/vite';
     import preact from '@astrojs/preact';

     export default defineConfig({
       site: 'https://aeorank.dev',
       integrations: [preact()],
       vite: {
         plugins: [tailwindcss()],
       },
     });
     ```

  3. Create `apps/marketing/tsconfig.json`:
     ```json
     { "extends": "astro/tsconfigs/strict" }
     ```

  4. Create `apps/marketing/src/styles/global.css` with Tailwind 4 theme tokens:
     ```css
     @import "tailwindcss";

     @theme {
       --color-bg: #FAF9F7;
       --color-bg-dark: #F0EFED;
       --color-text: #111111;
       --color-text-muted: #555555;
       --color-accent: #111111;
       --color-border: #E5E5E3;
       --font-sans: "Inter", ui-sans-serif, system-ui, sans-serif;
       --font-mono: "JetBrains Mono", ui-monospace, monospace;
     }
     ```

  5. Create `apps/marketing/public/CNAME` containing: `aeorank.dev`

  6. Create a simple `apps/marketing/public/favicon.svg` — a minimal black "A" letter in a square.

  7. Run `pnpm install` from repo root to install all dependencies.

  **Important:** Use @tailwindcss/vite plugin (NOT the deprecated @astrojs/tailwind integration). Tailwind 4 uses the Vite plugin approach.
  </action>
  <verify>
    <automated>cd apps/marketing && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>Astro project builds successfully with Tailwind 4 and Preact configured. CNAME file exists.</done>
</task>

<task type="auto">
  <name>Task 2: Create Base layout, Nav, Footer, and scaffold homepage</name>
  <files>
    apps/marketing/src/layouts/Base.astro,
    apps/marketing/src/components/Nav.astro,
    apps/marketing/src/components/Footer.astro,
    apps/marketing/src/pages/index.astro
  </files>
  <action>
  Create the shared layout and core components following the 37signals/Gumroad aesthetic.

  1. Create `apps/marketing/src/layouts/Base.astro`:
     - Import global.css
     - HTML with lang="en"
     - Head: charset, viewport, title (prop), meta description (prop), preconnect to Google Fonts for Inter
     - Link to Google Fonts: Inter (400, 500, 600, 700)
     - Use `font-display: swap` via the Google Fonts URL parameter
     - Body with class `bg-bg text-text font-sans`
     - Slot wraps `<Nav />` + `<main>` + `<Footer />`
     - No JavaScript — pure static HTML

  2. Create `apps/marketing/src/components/Nav.astro`:
     - Sticky top nav, white background with subtle bottom border
     - Left: "AEOrank" logo text (bold, black)
     - Right: "Docs" link (to https://docs.aeorank.dev), "GitHub" link (to https://github.com/aeorank/aeorank), "Get Started" solid black button (links to #get-started or docs quick-start)
     - Max width container (max-w-6xl), centered
     - Mobile: hamburger menu NOT needed for v1 — just stack horizontally, wrap on small screens
     - Zero JavaScript

  3. Create `apps/marketing/src/components/Footer.astro`:
     - Background: slightly darker off-white (#F0EFED)
     - Three columns: Product (Docs, GitHub, CLI), Company (About AEO, Blog placeholder), Legal (Privacy, Terms — both as placeholder # links)
     - Bottom bar: "© 2026 AEOrank" + "Built for developers who care about AI visibility"
     - Max width container, centered

  4. Create `apps/marketing/src/pages/index.astro`:
     - Uses Base layout with title="AEOrank — AI Engine Optimization for Every Website"
     - Description: "Scan your site, get an AEO score, and generate all 8 files AI engines need to find you. Free CLI tool."
     - Placeholder sections (will be filled in Plan 03):
       - Hero section: `<section id="hero">` with placeholder heading "Make Your Site Visible to AI"
       - How it works: `<section id="how-it-works">` with placeholder
       - Files: `<section id="files">` with placeholder
       - Scoring: `<section id="scoring">` with placeholder
       - Pricing: `<section id="pricing">` with placeholder
       - FAQ: `<section id="faq">` with placeholder
       - CTA: `<section id="get-started">` with placeholder
     - Each section uses consistent padding (py-20 or py-24), max-w-6xl mx-auto, px-6

  **Design rules:**
  - Solid black buttons with white text, no rounded corners (rounded-none or rounded-sm max)
  - No gradients, no shadows (except subtle on nav), no glassmorphism
  - Large confident headings: text-4xl md:text-5xl font-bold for h1, text-3xl md:text-4xl for h2
  - Generous whitespace between sections
  - All links: black text, underline on hover
  </action>
  <verify>
    <automated>cd apps/marketing && pnpm build 2>&1 | tail -5 && test -f dist/index.html && ! grep -q '<script' dist/index.html && echo "PASS: No JS in output"</automated>
  </verify>
  <done>Base layout renders with Nav + Footer. Homepage has all 7 section scaffolds. Build output has zero JavaScript. Design matches 37signals aesthetic with off-white background and Inter font.</done>
</task>

</tasks>

<verification>
- `pnpm build` from apps/marketing/ succeeds with zero errors
- `dist/index.html` exists and contains no `<script>` tags
- Homepage HTML has sections: hero, how-it-works, files, scoring, pricing, faq, get-started
- CSS includes custom properties for brand colors (#FAF9F7, #111)
- CNAME file contains "aeorank.dev"
</verification>

<success_criteria>
- Marketing site builds as static HTML with Astro 5
- Tailwind CSS 4 is configured via @tailwindcss/vite plugin
- Preact integration is installed (for Plan 03's terminal demo)
- 37signals design system is established through Tailwind theme tokens
- Nav, Footer, and Base layout are reusable components
- Homepage scaffold has all required section anchors
- Zero JavaScript in build output
</success_criteria>

<output>
After completion, create `.planning/phases/03-web-presence/03-01-SUMMARY.md`
</output>
