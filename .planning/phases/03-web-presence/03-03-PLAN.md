---
phase: 03-web-presence
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - apps/marketing/src/pages/index.astro
  - apps/marketing/src/components/Hero.astro
  - apps/marketing/src/components/HowItWorks.astro
  - apps/marketing/src/components/FilesList.astro
  - apps/marketing/src/components/ScoringExplainer.astro
  - apps/marketing/src/components/Pricing.astro
  - apps/marketing/src/components/FAQ.astro
  - apps/marketing/src/components/CTASection.astro
  - apps/marketing/src/components/TerminalDemo.tsx
autonomous: true
requirements: [SITE-02, SITE-04]

must_haves:
  truths:
    - Homepage has all 8 sections: hero, terminal demo, how-it-works, files list, scoring, pricing, FAQ, CTA
    - Terminal demo auto-plays typing animation when scrolled into view
    - Terminal demo is the ONLY JavaScript on the page (loaded via client:visible)
    - Pricing table shows 4 tiers: Free CLI / Pro $29 / API $99 / Agency $499
    - All marketing copy is specific and honest (no buzzwords)
  artifacts:
    - apps/marketing/src/components/Hero.astro
    - apps/marketing/src/components/TerminalDemo.tsx (Preact island)
    - apps/marketing/src/components/Pricing.astro
    - apps/marketing/src/components/FAQ.astro
  key_links:
    - TerminalDemo.tsx is a Preact component hydrated via client:visible
    - Hero.astro embeds TerminalDemo with client:visible directive
    - All sections are Astro components (zero JS) except terminal demo
---

<objective>
Build all homepage content sections and the interactive terminal demo. Fill the scaffold from Plan 01 with production content, specific marketing copy, and the typing-animation terminal component.

Purpose: Create a compelling marketing page that shows developers exactly what AEOrank does with honest, specific copy.
Output: Complete homepage with all 8 sections and an interactive terminal demo.
</objective>

<execution_context>
@/Users/vin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-web-presence/03-CONTEXT.md
@.planning/phases/03-web-presence/03-01-SUMMARY.md

<interfaces>
<!-- 12 dimension names for scoring explainer section -->
llms.txt Presence (high), Schema.org Markup (high), AI Crawler Access (medium),
Content Structure (high), Answer-First Formatting (medium), FAQ & Speakable (medium),
E-E-A-T Signals (medium), Meta Descriptions (medium), Sitemap Presence (low),
HTTPS & Redirects (low), Page Freshness (low), Citation Anchors (medium)

<!-- 8 generated files for files list section -->
llms.txt, llms-full.txt, CLAUDE.md, schema.json, robots-patch.txt,
faq-blocks.html, citation-anchors.html, sitemap-ai.xml

<!-- Pricing tiers from CONTEXT.md -->
Free CLI / Pro $29/mo / API $99/mo / Agency $499/mo
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build static content sections (Hero, How It Works, Files, Scoring, Pricing, FAQ, CTA)</name>
  <files>
    apps/marketing/src/pages/index.astro,
    apps/marketing/src/components/Hero.astro,
    apps/marketing/src/components/HowItWorks.astro,
    apps/marketing/src/components/FilesList.astro,
    apps/marketing/src/components/ScoringExplainer.astro,
    apps/marketing/src/components/Pricing.astro,
    apps/marketing/src/components/FAQ.astro,
    apps/marketing/src/components/CTASection.astro
  </files>
  <action>
  Create all static content sections as Astro components (zero JavaScript). Replace the placeholder sections in index.astro with real components.

  **1. Hero.astro** (section#hero):
  - Headline: "Your Site Is Invisible to AI" (large, bold, text-5xl md:text-6xl)
  - Subheadline: "AI engines power 40% of web discovery. AEOrank scans your site, scores your AI visibility across 12 dimensions, and generates the 8 files that make AI engines find you."
  - Two CTAs: "Get Started Free" (solid black button, links to docs quick-start), "See How It Works" (text link with arrow, scrolls to #how-it-works)
  - Below CTAs: placeholder div for TerminalDemo (added in Task 2)
  - No image/illustration — let the terminal demo be the hero visual

  **2. HowItWorks.astro** (section#how-it-works):
  - Heading: "Three Commands. Eight Files. Zero Config."
  - Three steps in a row (grid, 3 cols on md):
    - Step 1: "Scan" — `npx aeorank scan https://your-site.com` — "Point AEOrank at any URL. It crawls up to 50 pages, analyzes structure, schema, and AI readiness."
    - Step 2: "Score" — "Get an AEO score from 0-100 across 12 dimensions. See exactly what's working and what's not."
    - Step 3: "Generate" — "Get 8 files tailored to your site: llms.txt, schema.json, robots directives, and more. Deploy them and watch your AI visibility improve."
  - Each step: large number (01, 02, 03), title, code snippet or description, body text
  - Clean, minimal — no icons, no illustrations. Numbers + text.

  **3. FilesList.astro** (section#files):
  - Heading: "8 Files. Every One Matters."
  - Subheading: "AEOrank generates the complete set of files AI engines look for."
  - Grid of 8 cards (2 cols on md, 4 cols on lg):
    - llms.txt — "The new robots.txt for AI. Tells language models what your site is about."
    - llms-full.txt — "Full-text version for models that want the complete picture."
    - CLAUDE.md — "Repository context file. Helps AI assistants understand your codebase."
    - schema.json — "Organization + WebSite + FAQ structured data in JSON-LD."
    - robots-patch.txt — "Directives for GPTBot, ClaudeBot, PerplexityBot, and Google-Extended."
    - faq-blocks.html — "Speakable FAQ markup that AI engines love to cite."
    - citation-anchors.html — "Heading anchors that make your content directly linkable by AI."
    - sitemap-ai.xml — "AI-optimized sitemap with content type hints."
  - Each card: file name in monospace, one-line description, subtle border

  **4. ScoringExplainer.astro** (section#scoring):
  - Heading: "12 Dimensions. One Score."
  - Subheading: "AEOrank measures what actually matters for AI engine visibility."
  - Table or grid showing all 12 dimensions grouped by weight:
    - High weight (1.5x): llms.txt Presence, Schema.org Markup, Content Structure
    - Medium weight (1.0x): AI Crawler Access, Answer-First Formatting, FAQ & Speakable, E-E-A-T Signals, Meta Descriptions, Citation Anchors
    - Low weight (0.5x): Sitemap Presence, HTTPS & Redirects, Page Freshness
  - Brief explanation: "Each dimension scored 0-10. Weighted by importance. Final score: 0-100."
  - Grade scale: A+ (95+), A (85+), B (70+), C (55+), D (40+), F (below 40)
  - Link to full docs: "Read the full scoring methodology →"

  **5. Pricing.astro** (section#pricing):
  - Heading: "Simple Pricing"
  - Subheading: "The CLI is free forever. Pay when you need the dashboard."
  - 4 pricing cards in a row (grid):
    - **Free**: CLI tool, unlimited scans, all 8 files, MIT licensed. CTA: "Install Now" (link to docs)
    - **Pro $29/mo**: Everything free + dashboard, score history, 5 sites. CTA: "Coming Soon" (disabled)
    - **API $99/mo**: Everything Pro + REST API, 50 sites, webhook notifications. CTA: "Coming Soon" (disabled)
    - **Agency $499/mo**: Everything API + unlimited sites, white-label reports, priority support. CTA: "Coming Soon" (disabled)
  - Free tier card should be visually distinct (highlighted border)
  - "Coming Soon" tiers have muted styling

  **6. FAQ.astro** (section#faq):
  - Heading: "Questions & Answers"
  - Accordion-style FAQ using HTML `<details>/<summary>` (zero JavaScript):
    - "What is AEO?" — Answer Engine Optimization. The practice of making your website visible and citable by AI engines like ChatGPT, Perplexity, and Claude.
    - "Is AEOrank free?" — The CLI is free and MIT-licensed. Run unlimited scans. The dashboard (coming soon) will have paid tiers.
    - "What are the 8 files?" — Brief list with one-line descriptions.
    - "How is the AEO score calculated?" — 12 dimensions, weighted, normalized to 0-100. Link to scoring docs.
    - "Does this replace SEO?" — No. AEO complements SEO. Traditional search still matters. AEOrank focuses on AI engine visibility specifically.
    - "Can I use this in CI?" — Yes. A GitHub Action is coming (Phase 4). For now, `npx aeorank scan --format json` works in any CI pipeline.
    - "How long does a scan take?" — Under 30 seconds for most sites (up to 50 pages).
  - Use `<details>` with `<summary>` — native HTML accordion, zero JS

  **7. CTASection.astro** (section#get-started):
  - Centered layout
  - Heading: "Ready to See Your Score?"
  - Code block: `npx aeorank scan https://your-site.com`
  - Button: "Read the Quick Start Guide" → docs getting-started
  - Small text: "Free. Open source. No account required."

  **Update index.astro:**
  - Import all components
  - Replace placeholder sections with actual components
  - Keep section IDs for anchor navigation

  **Copy style rules (37signals/Gumroad):**
  - Direct, specific copy — "Generates 8 files in 45 seconds" not "Supercharge your AI presence"
  - Short sentences. No jargon without explanation.
  - Numbers over adjectives: "12 dimensions" not "comprehensive analysis"
  - Honest about what's free vs paid, what's available vs coming soon
  </action>
  <verify>
    <automated>cd apps/marketing && pnpm build 2>&1 | tail -5 && grep -c 'section' dist/index.html | xargs -I{} test {} -ge 7 && echo "PASS: 7+ sections"</automated>
  </verify>
  <done>Homepage has all 7 static sections with production copy. Pricing shows 4 tiers. FAQ uses native HTML details/summary. All 12 dimensions listed in scoring section. All 8 files listed in files section. Zero JavaScript in build output (terminal demo not yet wired).</done>
</task>

<task type="auto">
  <name>Task 2: Build interactive terminal demo (Preact island)</name>
  <files>
    apps/marketing/src/components/TerminalDemo.tsx,
    apps/marketing/src/components/Hero.astro
  </files>
  <action>
  Create the terminal demo as a Preact component and wire it into the Hero section with `client:visible`.

  **TerminalDemo.tsx** (Preact component):
  1. Dark terminal UI:
     - Background: #1a1a2e or similar dark color
     - Top bar: three dots (red/yellow/green) + "Terminal" title
     - Font: monospace, ~14px, light text on dark background
     - Rounded corners, subtle shadow

  2. Typing animation:
     - Use `useState` and `useEffect` from preact/hooks
     - Line-by-line reveal with typing effect (character by character for the command, instant for output lines)
     - Sequence:
       ```
       $ npx aeorank scan https://example.com
       [pause 500ms]

         Scanning https://example.com...
         ✓ Fetched 12 pages in 3.2s
         ✓ Analyzed structure and schema
         ✓ Generated 8 files
       [pause 300ms]

         AEO Score: 42/100 (D)
       [pause 200ms]

         Dimension              Score  Status
         ─────────────────────────────────────
         llms.txt Presence        0    ✗ fail
         Schema.org Markup       65    ⚠ warn
         Content Structure       80    ✓ pass
         AI Crawler Access       30    ✗ fail
         Meta Descriptions       70    ✓ pass
         ...
       [pause 300ms]

         → 8 files written to ./aeo-output/
         → Run again after deploying files to see your score improve
       ```
     - After complete: pause 3s, then restart from beginning (loop)

  3. Auto-play on scroll via `client:visible`:
     - The `client:visible` directive handles the "load when visible" part
     - Animation starts immediately when component hydrates
     - No IntersectionObserver needed — Astro handles this

  4. Component props: none needed (self-contained)

  **Update Hero.astro:**
  - Import TerminalDemo: `import TerminalDemo from './TerminalDemo.tsx';`
  - Add below the CTA buttons: `<TerminalDemo client:visible />`
  - The terminal demo should be centered, max-w-2xl, with some top margin

  **Size budget:**
  - Preact: ~3KB gzipped
  - Component: should be <2KB
  - Total JS: under 5KB gzipped — acceptable for the only interactive element

  **Important:** This is the ONLY JavaScript on the entire marketing site. All other components are pure Astro (server-rendered HTML/CSS).
  </action>
  <verify>
    <automated>cd apps/marketing && pnpm build 2>&1 | tail -5 && ls dist/_astro/*.js 2>/dev/null | head -5 && echo "PASS: JS files exist for island only"</automated>
  </verify>
  <done>Terminal demo renders in hero section. Typing animation plays when scrolled into view. JS is only loaded for this one component via client:visible. Rest of page remains zero-JS static HTML.</done>
</task>

</tasks>

<verification>
- `pnpm build` from apps/marketing/ succeeds
- Homepage HTML contains all section IDs: hero, how-it-works, files, scoring, pricing, faq, get-started
- JavaScript files exist only in _astro/ directory (for the Preact island)
- No `<script>` tags in the HTML except Astro's island hydration script
- Pricing section mentions all 4 tiers
- FAQ section uses `<details>` elements
</verification>

<success_criteria>
- All 8 homepage sections have production content
- Marketing copy is specific and honest (37signals style)
- Terminal demo auto-plays typing animation on scroll
- Terminal demo is the only JS on the page
- Pricing shows correct tiers with correct prices
- FAQ works without JavaScript (native details/summary)
</success_criteria>

<output>
After completion, create `.planning/phases/03-web-presence/03-03-SUMMARY.md`
</output>
