---
phase: 11-technical-foundation-expansion
plan: "02"
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - packages/core/src/scorer/dimensions.ts
  - packages/core/src/constants.ts
  - packages/core/src/scorer/index.ts
  - packages/core/src/__tests__/dimensions.test.ts
  - packages/core/src/__tests__/scorer.test.ts
  - packages/core/src/__tests__/integration.test.ts
  - packages/core/src/__tests__/determinism.test.ts
autonomous: true
requirements: [TECH-04, TECH-05]

must_haves:
  truths:
    - "scan() result includes schema-coverage dimension with score 0-10"
    - "scan() result includes speakable-schema dimension with score 0-10"
    - "A site with schema on inner pages (not just homepage) scores higher on schema-coverage"
    - "A site with SpeakableSpecification markup scores higher on speakable-schema"
    - "All existing + new tests pass with zero failures"
    - "Determinism test passes for all 32 dimensions"
  artifacts:
    - path: "packages/core/src/scorer/dimensions.ts"
      provides: "scoreSchemaCoverage and scoreSpeakableSchema scorer functions"
      exports: ["scoreSchemaCoverage", "scoreSpeakableSchema"]
    - path: "packages/core/src/constants.ts"
      provides: "2 new DIMENSION_DEFS entries (32 total)"
      contains: "schema-coverage"
    - path: "packages/core/src/scorer/index.ts"
      provides: "Both new IDs in PAGE_LEVEL_DIMENSIONS"
      contains: "schema-coverage"
    - path: "packages/core/src/__tests__/determinism.test.ts"
      provides: "Determinism verification for all 32 dimensions"
      contains: "schema-coverage"
  key_links:
    - from: "packages/core/src/scorer/dimensions.ts"
      to: "DIMENSION_SCORERS registry"
      via: "Registry entries mapping IDs to scorer functions"
      pattern: "schema-coverage.*scoreSchemaCoverage"
    - from: "packages/core/src/__tests__/determinism.test.ts"
      to: "packages/core/src/scorer/dimensions.ts"
      via: "Tests verify all 32 dimension IDs produce identical scores across runs"
      pattern: "32"
---

<objective>
Add 2 remaining Technical Foundation scoring dimensions (schema-coverage, speakable-schema) and update all test suites for 32 total dimensions including determinism verification.

Purpose: These 2 dimensions consume existing schemaOrg data from ScannedPage (no parser changes needed). Completing the test suite update and determinism verification closes Phase 11.
Output: 2 new scorer functions, 32 total DIMENSION_DEFS, full test suite passing with determinism for all 32 dimensions.
</objective>

<execution_context>
@/Users/vin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-technical-foundation-expansion/11-CONTEXT.md
@SCORING_ROADMAP.md

@.planning/phases/11-technical-foundation-expansion/11-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — ScannedPage now has these additional fields -->
```typescript
export interface ScannedPage {
  // ... existing fields ...
  semanticElements: { main: number; article: number; nav: number; aside: number; section: number; header: number; footer: number };
  ariaRoleCount: number;
  figureCount: number;
  imgCount: number;
  imgsWithAlt: number;
  avgSentenceLength: number;
}
```

<!-- Existing schemaOrg field structure (used by both new scorers) -->
```typescript
// page.schemaOrg is object[] — each entry may have:
// - "@type": string (e.g., "Article", "FAQPage", "SpeakableSpecification")
// - "@graph": array of typed objects
// - "speakable": object with "@type": "SpeakableSpecification"
```

From packages/core/src/scorer/dimensions.ts:
```typescript
type DimensionScorer = (pages: ScannedPage[], meta: ScanMeta) => DimensionScore;
function makeDimension(id, name, score, weight, hint): DimensionScore;
export const DIMENSION_SCORERS: Record<string, DimensionScorer> = { ... }; // 30 entries after Plan 01
```

From packages/core/src/constants.ts:
```typescript
export const DIMENSION_DEFS: DimensionDef[] = [ ... ]; // 30 entries after Plan 01
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement schema-coverage and speakable-schema scorers</name>
  <files>
    packages/core/src/scorer/dimensions.ts
    packages/core/src/constants.ts
    packages/core/src/scorer/index.ts
    packages/core/src/__tests__/dimensions.test.ts
  </files>
  <read_first>
    packages/core/src/scorer/dimensions.ts (last 120 lines — latest scorer pattern + DIMENSION_SCORERS registry)
    packages/core/src/constants.ts (full file — DIMENSION_DEFS array, should be 30 entries after Plan 01)
    packages/core/src/scorer/index.ts (full file — PAGE_LEVEL_DIMENSIONS array)
    packages/core/src/__tests__/dimensions.test.ts (first 55 lines for makePage/makeMeta helpers, last 100 lines for latest test pattern)
    SCORING_ROADMAP.md (Phase 11 — schema-coverage and speakable-schema specs)
  </read_first>
  <behavior>
    scoreSchemaCoverage tests:
    - Empty pages array returns score 0, weight "low", id "schema-coverage"
    - Single page (homepage only) with schema returns score 3-4 (can't assess inner pages)
    - 5 pages where only homepage has schema returns score 2-3 (poor inner coverage)
    - 5 pages where 4/5 have schema returns score 8-10 (good coverage)
    - 5 pages where all 5 have schema returns score 10
    - Pages with zero schema on any page return score 0

    scoreSpeakableSchema tests:
    - Empty pages array returns score 0, weight "low", id "speakable-schema"
    - Pages with no SpeakableSpecification markup return score 0
    - Pages with SpeakableSpecification @type return score 10
    - Pages with speakable property containing SpeakableSpecification return score 10
    - SpeakableSpecification in @graph array is detected and returns score 10
    - One page with speakable out of 5 returns score 3-5 (partial coverage)
  </behavior>
  <action>
    RED phase — write tests first in dimensions.test.ts.

    GREEN phase — implement in dimensions.ts:

    **scoreSchemaCoverage** (Dimension 31, low weight, id "schema-coverage"):
    1. If pages.length === 0, return makeDimension("schema-coverage", "Schema Coverage", 0, "low", hint)
    2. Sort pages by URL for determinism
    3. For each page, check if schemaOrg array has at least 1 entry with a meaningful @type (not just empty objects):
       - Check direct @type property exists and is a non-empty string
       - Check @graph array items for @type
       - Page "has schema" if at least one typed schema object found
    4. Count pagesWithSchema / total pages
    5. Special case: if only 1 page, and it has schema, score = 5 (can't assess inner page coverage)
    6. Scoring thresholds (for 2+ pages):
       - ratio >= 0.8: score = 10
       - ratio >= 0.6: score = 8
       - ratio >= 0.4: score = 6
       - ratio >= 0.2: score = 3
       - ratio > 0: score = 1
       - else: score = 0
    7. Hint: "Add structured data (schema.org) to inner pages, not just the homepage"

    **scoreSpeakableSchema** (Dimension 32, low weight, id "speakable-schema"):
    1. If pages.length === 0, return makeDimension("speakable-schema", "Speakable Schema", 0, "low", hint)
    2. Sort pages by URL for determinism
    3. For each page, check schemaOrg for SpeakableSpecification:
       - Direct @type === "SpeakableSpecification"
       - In @graph array: any item with @type === "SpeakableSpecification"
       - As nested property: any schema object with `speakable` property where `speakable["@type"] === "SpeakableSpecification"`
    4. Count pagesWithSpeakable / total pages
    5. Scoring thresholds:
       - ratio >= 0.5: score = 10
       - ratio >= 0.3: score = 7
       - ratio >= 0.1: score = 4
       - ratio > 0: score = 2
       - else: score = 0
    6. Hint: "Add SpeakableSpecification markup to indicate content suitable for text-to-speech and voice assistants"

    Register both in DIMENSION_SCORERS:
    - "schema-coverage": scoreSchemaCoverage
    - "speakable-schema": scoreSpeakableSchema

    Add to DIMENSION_DEFS in constants.ts:
    - { id: "schema-coverage", name: "Schema Coverage", weight: "low", maxScore: 10 }
    - { id: "speakable-schema", name: "Speakable Schema", weight: "low", maxScore: 10 }

    Add both IDs to PAGE_LEVEL_DIMENSIONS in scorer/index.ts (page-level: each page has its own schema).
  </action>
  <verify>
    <automated>cd /Users/vin/Development/aeorank && pnpm vitest run packages/core/src/__tests__/dimensions.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "scoreSchemaCoverage" packages/core/src/scorer/dimensions.ts returns >= 2 (function def + registry)
    - grep -c "scoreSpeakableSchema" packages/core/src/scorer/dimensions.ts returns >= 2
    - grep -c "schema-coverage" packages/core/src/constants.ts returns >= 1
    - grep -c "speakable-schema" packages/core/src/constants.ts returns >= 1
    - grep -c "schema-coverage" packages/core/src/scorer/index.ts returns >= 1
    - grep -c "speakable-schema" packages/core/src/scorer/index.ts returns >= 1
    - grep -c "scoreSchemaCoverage\|scoreSpeakableSchema" packages/core/src/__tests__/dimensions.test.ts returns >= 4
    - vitest run dimensions.test.ts exits 0
  </acceptance_criteria>
  <done>Both scoreSchemaCoverage and scoreSpeakableSchema implemented, registered in DIMENSION_SCORERS + DIMENSION_DEFS + PAGE_LEVEL_DIMENSIONS, with unit tests passing. DIMENSION_DEFS count is 32.</done>
</task>

<task type="auto">
  <name>Task 2: Update scorer/integration/determinism tests for 32 dimensions</name>
  <files>
    packages/core/src/__tests__/scorer.test.ts
    packages/core/src/__tests__/integration.test.ts
    packages/core/src/__tests__/determinism.test.ts
  </files>
  <read_first>
    packages/core/src/__tests__/scorer.test.ts (find assertion for dimension count — should be 27, needs 32)
    packages/core/src/__tests__/integration.test.ts (find assertion for dimension count — should be 27, needs 32)
    packages/core/src/__tests__/determinism.test.ts (full file — HTML fixture, dimension count, dimension ID list)
  </read_first>
  <action>
    **scorer.test.ts:**
    - Update dimension count assertion from 27 to 32

    **integration.test.ts:**
    - Update dimension count assertion from 27 to 32

    **determinism.test.ts:**
    - Update expected dimension count from 27 to 32
    - Add all 5 new Phase 11 dimension IDs to the checked list: "semantic-html", "extraction-friction", "image-context", "schema-coverage", "speakable-schema"
    - Enhance the HTML fixture (enhancedPageHtml) to include:
      - Semantic HTML5 elements: `<main>`, `<article>`, `<nav>`, `<aside>` wrapping content
      - `<header>` and `<footer>` elements
      - ARIA role: `role="navigation"` on a nav element
      - Figure with figcaption: `<figure><img src="chart.png" alt="AEO score distribution chart"><figcaption>Figure 1: Score distribution</figcaption></figure>`
      - Additional img with alt: `<img src="logo.png" alt="AEOrank logo">`
      - A SpeakableSpecification in the JSON-LD @graph: `{"@type": "SpeakableSpecification", "cssSelector": [".article-body"]}`
    - This ensures determinism tests verify all 5 new scorers produce consistent non-zero scores

    Run full test suite to confirm zero failures across all test files.
  </action>
  <verify>
    <automated>cd /Users/vin/Development/aeorank && pnpm vitest run 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep "32" packages/core/src/__tests__/scorer.test.ts returns a match (dimension count)
    - grep "32" packages/core/src/__tests__/integration.test.ts returns a match (dimension count)
    - grep "32" packages/core/src/__tests__/determinism.test.ts returns a match (dimension count)
    - grep "semantic-html" packages/core/src/__tests__/determinism.test.ts returns a match
    - grep "extraction-friction" packages/core/src/__tests__/determinism.test.ts returns a match
    - grep "image-context" packages/core/src/__tests__/determinism.test.ts returns a match
    - grep "schema-coverage" packages/core/src/__tests__/determinism.test.ts returns a match
    - grep "speakable-schema" packages/core/src/__tests__/determinism.test.ts returns a match
    - pnpm vitest run exits 0 with all tests passing
  </acceptance_criteria>
  <done>All test files updated for 32 dimensions, determinism verified for all 5 new Phase 11 dimensions, full test suite passes with zero failures.</done>
</task>

</tasks>

<verification>
1. `pnpm vitest run` — all tests pass (0 failures)
2. `pnpm tsc --noEmit -p packages/core/tsconfig.json` — no type errors
3. DIMENSION_DEFS has exactly 32 entries
4. DIMENSION_SCORERS has exactly 32 entries
5. Determinism test verifies all 32 dimension IDs produce identical scores across runs
</verification>

<success_criteria>
- scan() result includes schema-coverage and speakable-schema dimension scores (0-10 each)
- Both dimensions registered in DIMENSION_DEFS (32 total), DIMENSION_SCORERS (32 total), PAGE_LEVEL_DIMENSIONS
- A site with schema on inner pages scores higher on schema-coverage than one with schema only on homepage
- A site with SpeakableSpecification markup scores higher on speakable-schema than one without
- All existing + new tests pass with zero failures
- Determinism test passes for all 32 dimensions
</success_criteria>

<output>
After completion, create `.planning/phases/11-technical-foundation-expansion/11-02-SUMMARY.md`
</output>
