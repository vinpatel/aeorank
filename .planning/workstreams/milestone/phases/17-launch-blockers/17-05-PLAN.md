---
phase: 17-launch-blockers
plan: 05
type: execute
wave: 3
depends_on: ["17-02"]
files_modified:
  - package.json
  - apps/web/package.json
  - apps/docs/package.json
  - packages/core/package.json
  - packages/cli/package.json
  - packages/11ty/package.json
  - packages/astro/package.json
  - packages/docusaurus/package.json
  - packages/gatsby/package.json
  - packages/next/package.json
  - packages/nuxt/package.json
  - packages/remix/package.json
  - packages/shopify/package.json
  - packages/sveltekit/package.json
  - packages/vitepress/package.json
  - pnpm-lock.yaml
autonomous: true
requirements: [SEC-01]

must_haves:
  truths:
    - "Every stale pnpm.overrides floor is raised to at or above the currently required patched version"
    - "vitest is at or above 3.2.6 everywhere it is declared, clearing the last critical outside the Astro tree"
    - "next in apps/web is at or above 16.2.11 and @clerk/nextjs is at or above 7.2.4"
    - "The only critical or high alerts remaining after this lever trace to astro, which plan 17-08 clears"
    - "The alert count after this lever is measured and recorded, attributable to this commit alone"
  artifacts:
    - path: "package.json"
      provides: "Root pnpm.overrides carrying the transitive version floors Dependabot cannot reach"
      contains: "overrides"
    - path: "pnpm-lock.yaml"
      provides: "Resolved tree at the raised floors"
  key_links:
    - from: "package.json pnpm.overrides"
      to: "pnpm-lock.yaml"
      via: "pnpm install re-resolution of transitive versions"
      pattern: "overrides"
---

<objective>
SEC-01 Lever B (D-05 mechanism 2 of 3): raise version floors across roughly 17 packages via
root `pnpm.overrides` and a small number of direct dependency bumps. This is everything that
is a one-line change; the one genuine major migration (Astro) is deliberately excluded and
handled alone in plan 17-08.

Purpose: `pnpm.overrides` is the only lever that reaches transitive dependencies — 217 of the
242 baseline alerts were attributed to the root lockfile. This must be its own commit so its
alert delta is attributable and revertible independently of Lever A and the Astro migration.
Output: one commit raising the floors, with a measured before/after alert count.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/milestone/phases/17-launch-blockers/17-CONTEXT.md
@.planning/workstreams/milestone/phases/17-launch-blockers/17-RESEARCH.md
@.planning/workstreams/milestone/phases/17-launch-blockers/17-02-SUMMARY.md
@.planning/workstreams/milestone/phases/17-launch-blockers/17-03-SUMMARY.md

<interfaces>
<!-- Existing root package.json pnpm.overrides block (27 entries). Three floors are stale
     and one is dangerous. Edit in place; do not rewrite the block wholesale. -->
handlebars >=4.7.9 · lodash >=4.18.0 · node-forge >=1.4.0 · immutable >=3.8.3 ·
liquidjs >=10.25.6 (STALE — critical RCE needs >=10.27.1) · defu >=6.1.7 ·
vite ^7.3.2 (STALE — needs ^7.3.5; also currently crosses a major against astro@5's vite ^6.4.1) ·
picomatch >=4.0.4 · serialize-javascript >=7.0.5 · path-to-regexp >=0.1.13 · axios >=1.15.1 ·
esbuild >=0.25.0 (STALE) · brace-expansion >=5.0.5 (STALE — needs >=5.0.9) · cookie >=0.7.0 ·
tmp >=0.2.5 · markdown-it >=14.1.1 · yaml >=2.8.3 · follow-redirects >=1.16.0 ·
smol-toml >=1.6.1 · file-type >=21.3.1 · h3 >=1.15.9 · srvx >=0.11.13 · unhead >=2.1.13 ·
@sveltejs/kit >=2.57.1 · @parcel/reporter-dev-server >=2.16.4 · webpack >=5.106.2

Root devDependencies: turbo ^2.5.0 · typescript ^5.7.0 · @biomejs/biome ^1.9.0 · vitest ^3.2.0
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Raise the transitive floors and the direct dependency versions</name>
  <files>package.json, apps/web/package.json, apps/docs/package.json, packages/core/package.json, packages/cli/package.json, packages/11ty/package.json, packages/astro/package.json, packages/docusaurus/package.json, packages/gatsby/package.json, packages/next/package.json, packages/nuxt/package.json, packages/remix/package.json, packages/shopify/package.json, packages/sveltekit/package.json, packages/vitepress/package.json, pnpm-lock.yaml</files>
  <read_first>
    - package.json (the full pnpm.overrides block)
    - .planning/workstreams/milestone/phases/17-launch-blockers/17-RESEARCH.md (section "Lever B — the 18 remaining crit+high packages", including the minimum-safe-version table and "Existing stale overrides that must be corrected")
    - .planning/workstreams/milestone/phases/17-launch-blockers/17-03-SUMMARY.md (the exact advisories that failed the CI audit job)
    - apps/web/package.json
    - apps/docs/package.json
  </read_first>
  <action>
Record the current state first: `pnpm audit --json | jq '.metadata.vulnerabilities'` and the
`gh api` severity histogram. These are the before-numbers for the commit body.

**A. Correct the stale overrides in root `package.json`** (edit existing entries in place):
- `liquidjs` `>=10.25.6` -> `>=10.27.1` (the critical RCE floor)
- `brace-expansion` `>=5.0.5` -> `>=5.0.9`
- `esbuild` `>=0.25.0` -> raise to the current patched floor; resolve it with
  `npm view esbuild version` and pin at or above the advisory's `first_patched_version`
- `vite` `^7.3.2` -> `^7.3.5`. Do NOT jump to 8 here: `astro@5.18.1` is still in the tree at
  this wave and an override that crosses a major installs cleanly while failing subtly at
  build time. Plan 17-08 revisits this entry when Astro 7 lands (Astro 7 wants `vite ^8.0.13`)
  and may remove it entirely.

**B. Add the missing overrides** (transitive-only; these have no direct declaration):
- `undici` — 6.x `>=6.27.0`, 7.x `>=7.29.0`
- `fast-uri` `>=3.1.5`
- `js-yaml` — 3.x `>=3.15.1`, 4.x `>=4.3.1`
- `postcss` `>=8.5.18`
- `svgo` — 3.x `>=3.3.4`, 4.x `>=4.0.2`
- `ws` — 7.x `>=7.5.11`, 8.x `>=8.21.0`
- `devalue` `>=5.8.1`
- `sharp` `>=0.35.0` (native binary — a rebuild is expected during install)
Where a package is present at two major lines, express both floors rather than forcing a
single major across the tree. Use pnpm's per-range override key syntax
(e.g. `"undici@6": ">=6.27.0"`) rather than one blanket floor that would drag a major.

**C. Direct dependency bumps:**
- `apps/web`: `next` 16.2.4 -> `^16.3.1` (floor is 16.2.11) and `@clerk/nextjs` -> `^7.7.6`
  (floor is 7.2.4; this pulls `@clerk/backend`, `@clerk/react`, `@clerk/shared`, `js-cookie`)
- `apps/docs`: `sharp` -> `^0.35.3`
- `vitest` -> `^3.2.6` in root `devDependencies` and in every package that declares it
  (packages/core, cli, 11ty, astro, docusaurus, gatsby, next, nuxt, remix, shopify,
  sveltekit, vitepress, apps/web). Use 3.2.6, NOT 4.x — 4 is a major with its own migration
  and 3.2.6 clears the critical with a patch bump. This is the last critical outside the
  Astro tree.

**Do not touch** `apps/marketing/package.json`, `packages/astro`'s `astro` dependency or its
`peerDependencies`, or any `astro`/`@astrojs/*` version. Those belong to plan 17-08 and
splitting them here would destroy the isolation D-03 requires.

Optionally prune override entries whose package no longer resolves in the tree after Lever A
(`pnpm why <pkg>` returns nothing) — `@sveltejs/kit` is the likely candidate. Only do this if
`pnpm install && pnpm audit` stays clean afterwards; it is cleanup, not a requirement.

Then run the gate: `pnpm install && pnpm build && pnpm typecheck && pnpm test`.
  </action>
  <verify>
    <automated>pnpm install && pnpm build && pnpm typecheck && pnpm test && pnpm audit --json | jq -e '.metadata.vulnerabilities.critical == 0'</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm install`, `pnpm build`, `pnpm typecheck`, `pnpm test` each exit 0
    - `pnpm audit --json | jq '.metadata.vulnerabilities.critical'` returns 0
    - `node -e "const p=require('./package.json').pnpm.overrides; process.exit(p.liquidjs.includes('10.27') && p['brace-expansion'].includes('5.0.9') ? 0 : 1)"` exits 0
    - `jq -r '.devDependencies.vitest' package.json` resolves to a range whose floor is at least 3.2.6, and the same holds for every package.json that declares vitest: `grep -rh '"vitest"' package.json apps/web/package.json packages/*/package.json | grep -c '3\.2\.[0-5]"' ` returns 0
    - `jq -r '.dependencies.next' apps/web/package.json` floor is at least 16.2.11
    - `jq -r '.dependencies["@clerk/nextjs"]' apps/web/package.json` floor is at least 7.2.4
    - `git diff --name-only` includes no `astro`-owned file: `git diff --name-only | grep -c 'apps/marketing'` returns 0
    - `grep -c '"astro"' packages/astro/package.json` is unchanged from before this task
    - Any remaining high-severity advisory in `pnpm audit --json` traces to `astro` or an astro-owned subtree — record the list
  </acceptance_criteria>
  <done>All non-Astro floors are raised, criticals are at 0 locally, and the full gate is green.</done>
</task>

<task type="auto">
  <name>Task 2: Commit Lever B alone and re-measure the alert count</name>
  <files>(no file edits — git + measurement)</files>
  <read_first>
    - .planning/workstreams/milestone/phases/17-launch-blockers/17-CONTEXT.md (D-05: measure and report the alert count after each lever independently)
  </read_first>
  <action>
Commit only the Lever B changes as
`chore(deps): raise transitive floors and patch direct deps (SEC-01 lever B)`
with a body recording the before and after `pnpm audit` metadata and the `gh api` histogram.
Do not bundle any Astro change or copy change into this commit.

Push to `main`.

**The gating measurement is local `pnpm audit --json`.** It reflects the pushed lockfile
immediately and its shape is confirmed as `{info, low, moderate, high, critical}`. Criticals
must be 0; that is what this task's `<verify>` asserts.

**The GitHub Dependabot histogram is recorded, not gated.** It only updates after Dependabot
re-scans the pushed lockfile, which can take up to roughly 30 minutes — far longer than any
acceptable feedback latency for a task gate. Fire the query once immediately after the push
and record the result, then fire it again when writing the SUMMARY and record that reading
with its timestamp. If it has not re-scanned by then, write "not yet re-scanned at <time>"
and move on. Do NOT block the task, poll in a loop, or sleep waiting for it. Plan 17-08 takes
the authoritative final histogram once all three levers have landed.

Expected direction: criticals 0, highs reduced to only those attributable to `astro@5.18.1`
(research measured 8 high alerts on astro with no 5.x patch available).

Also check the CI `audit` job on this push. It is expected to still fail while the astro
highs remain — record which advisories it names so plan 17-08 can confirm it clears them.
  </action>
  <verify>
    <automated>pnpm audit --json | jq -e '.metadata.vulnerabilities.critical == 0'</automated>
  </verify>
  <acceptance_criteria>
    - `git show --stat HEAD` lists only package.json files and pnpm-lock.yaml — no `.astro` files, no markdown
    - The commit body contains the before and after `pnpm audit` vulnerability metadata verbatim
    - `pnpm audit --json | jq -e '.metadata.vulnerabilities.critical == 0'` exits 0 — this is the gate
    - The SUMMARY records at least one `gh api "repos/vinpatel/aeorank/dependabot/alerts?state=open&per_page=100" --paginate -q '.[].security_advisory.severity' | sort | uniq -c` reading with its timestamp, or an explicit "not yet re-scanned at &lt;time&gt;" note — recorded observation, not a gate
    - Every remaining high-severity package from `pnpm audit --json | jq -r '.advisories // {} | .[] | select(.severity=="high") | .module_name' | sort -u` (or the equivalent field in the installed pnpm's audit output) is recorded in the summary — this is plan 17-08's target list, and it comes from the local audit so it is available immediately
  </acceptance_criteria>
  <done>Lever B is a standalone commit on main, criticals are 0, and the residual high list is recorded for the Astro plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> pnpm lockfile | Raised floors pull newer published versions of packages already in the tree |
| Override mechanism -> peer/semver checks | `pnpm.overrides` bypasses range validation entirely |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-14 | Tampering | Transitive dependency tree (217 lockfile-attributed alerts) | mitigate | Root `pnpm.overrides` floors raised to advisory `first_patched_version` per package major line; verified by `pnpm audit --json` reporting 0 criticals |
| T-17-15 | Tampering | `pnpm.overrides` crossing a major boundary | mitigate | `vite` held at `^7.3.5` rather than 8 while `astro@5.18.1` is in the tree; the crossing is resolved by upgrading the parent in plan 17-08, not by forcing the override |
| T-17-16 | Denial of Service | `sharp` native rebuild | accept | Native binary bump requires a rebuild on install; caught by the `pnpm build` gate before commit |
| T-17-SC | Tampering | pnpm installs | accept | No new package names are introduced — only version floors on packages already resolved in `pnpm-lock.yaml`. RESEARCH.md `## Package Legitimacy Audit` records zero install candidates for this phase |
</threat_model>

<verification>
- `pnpm install && pnpm build && pnpm typecheck && pnpm test` exits 0
- `pnpm audit --json | jq '.metadata.vulnerabilities.critical'` returns 0
- Remaining highs are enumerated and all trace to astro
</verification>

<success_criteria>
Lever B of ROADMAP success criterion 1 is complete: every critical is cleared and the only
remaining high-severity alerts are the astro ones that require a major migration.
</success_criteria>

<output>
Create `.planning/workstreams/milestone/phases/17-launch-blockers/17-05-SUMMARY.md` when done.
Record the before/after histograms and the explicit list of remaining high-severity packages.
</output>
