---
phase: 17-launch-blockers
plan: 03
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - biome.json
  - .github/workflows/ci.yml
  - scripts/measure-tests.mjs
  - .github/test-count.json
  - apps/marketing/src/data/test-count.json
  - README.md
autonomous: true
requirements: [SEC-04, SEC-02]

must_haves:
  truths:
    - "A CI workflow runs build, typecheck, and the full test suite on every pull request and every push to main, and that job can actually pass"
    - "A separate CI job runs pnpm audit --audit-level=high and fails on any high or critical advisory"
    - "Biome no longer lints generated and planning trees, so the lint signal describes real source code"
    - "The lint job is advisory only and is never proposed as a required status check while the residual error count is above zero"
    - "The authoritative test count is produced by the vitest JSON reporter, never by grep"
    - "One generated artifact carries the count, and both README.md and the marketing site read from it"
  artifacts:
    - path: "biome.json"
      provides: "Ignore list covering the generated and planning trees that produce ~93% of the lint errors"
      contains: ".astro"
    - path: ".github/workflows/ci.yml"
      provides: "The repository's first test gate, audit gate, advisory lint job, and count publisher"
      contains: "pnpm audit --audit-level=high"
    - path: "scripts/measure-tests.mjs"
      provides: "Sums numTotalTests and test-file count from per-package vitest JSON reports"
      contains: "numTotalTests"
    - path: ".github/test-count.json"
      provides: "The single authoritative test count artifact"
      contains: "tests"
    - path: "apps/marketing/src/data/test-count.json"
      provides: "Build-time-importable copy of the same artifact for the Astro marketing site"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "scripts/measure-tests.mjs"
      via: "node invocation in the publish-count job"
      pattern: "measure-tests"
    - from: "README.md"
      to: ".github/test-count.json"
      via: "TESTS_START/TESTS_END marker substitution in CI"
      pattern: "TESTS_START"
---

<objective>
Create the repository's first CI pipeline. It does three things Phase 17 depends on: it runs
the test suite (no workflow currently does), it gates on `pnpm audit --audit-level=high`
(the only mechanism that catches pnpm transitive vulnerabilities — Dependabot cannot, per
D-06), and it emits one authoritative test count from the vitest JSON reporter (per D-07,
never grep).

Purpose: SEC-04 in full, plus the load-bearing half of SEC-02. Every doc and badge that
cites a test count reads from the artifact this plan creates, which is why plans 17-06 and
17-07 depend on it.
Output: a corrected `biome.json`, `.github/workflows/ci.yml`, `scripts/measure-tests.mjs`,
the generated count artifacts, and README marker wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/milestone/phases/17-launch-blockers/17-CONTEXT.md
@.planning/workstreams/milestone/phases/17-launch-blockers/17-RESEARCH.md
@.planning/workstreams/milestone/phases/17-launch-blockers/17-01-SUMMARY.md

<interfaces>
<!-- Existing repo conventions the executor must match. Do not invent a second mechanism. -->

The marker-substitution pattern already in use, from .github/workflows/readme-social-proof.yml:
- Fetches a value, rewrites the block between `<!-- STATS_START -->` and `<!-- STATS_END -->`
  in README.md with a python3 `re.sub(..., flags=re.DOTALL)`, then commits as
  `aeorank-bot <bot@aeorank.dev>` with `[skip ci]` in the message and pushes to main.
- Uses `permissions: contents: write`.

Root package.json scripts (turbo-orchestrated):
  dev, build, test, lint (biome check .), format, typecheck
turbo.json: the `test` task has `dependsOn: ["build"]`; `typecheck` has `dependsOn: ["^build"]`.
packageManager: pnpm@10.32.1

Packages that actually have a vitest suite (13 — the only ones measure-tests.mjs may run):
  packages/11ty, packages/astro, packages/cli, packages/core, packages/docusaurus,
  packages/gatsby, packages/next, packages/nuxt, packages/remix, packages/shopify,
  packages/sveltekit, packages/vitepress, apps/web
Excluded on purpose: packages/config has a stub test script (`echo 'Phase 2 stub'`);
packages/wordpress, apps/docs, apps/marketing have no test script at all. A naive
`pnpm -r exec vitest` fails on packages/config.

<!-- Measured gate state on main after plan 17-01, 2026-08-15. These are facts. -->
pnpm build      -> exit 0
pnpm typecheck  -> exit 0   (green only because plan 17-01 repaired 40 pre-existing errors)
pnpm test       -> exit 0
pnpm lint       -> exit 1, ~5700 errors (drifts — see the breakdown below)

Current biome.json:
  "files": { "ignore": ["**/dist", "**/node_modules", "**/.turbo", "**/*.md"] }

Measured lint breakdown, 2026-08-15:
  With the current ignore list:                    ~5700 errors across 783 files
  With .astro/.next/.vercel/.planning also ignored:  364 errors across 251 files

  The BEFORE number drifts upward every time a `.planning/` document is written, because
  `.planning` is currently linted — it read 5685 at first measurement and 5791 an hour later
  after planning docs were edited. Do not assert a literal before-count; measure your own and
  compare relatively. The AFTER number (364) is stable, because it describes real source only.
  => 5321 of the 5685 errors (93.6%) come from generated or planning trees that are not
     source code. The residual 364 are real, in real source files:
       116 lint/style/noNonNullAssertion
        98 format
        78 organizeImports
        18 lint/a11y/noSvgWithoutTitle
        11 lint/style/noUnusedTemplateLiteral
        10 lint/complexity/useLiteralKeys
         9 lint/suspicious/noExplicitAny
        24 assorted (useKeyWithClickEvents, useTemplate, noDelete, noThenProperty, ...)
     176 of those (format + organizeImports) are safe-autofixable; the other ~188 are rule
     violations spread across roughly 40 source files in every workspace package.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scope biome to real source, and settle what the CI verify job can gate on</name>
  <files>biome.json</files>
  <read_first>
    - biome.json (the `files.ignore` array)
    - package.json (the `lint` script — `biome check .`)
    - .planning/workstreams/milestone/phases/17-launch-blockers/17-01-SUMMARY.md (the typecheck repair — confirm build/typecheck/test are green on main before designing a gate around them)
  </read_first>
  <action>
This task exists because three later plans depend on the CI `verify` job concluding
`success` — Task 3 below, plan 17-09 Task 3, and plan 17-10 Task 2, which makes `verify` a
REQUIRED status check on `main`. A required check that can never pass bricks the repository.
So the job's contents must be decided against measured reality, not assumed.

**A. Extend `biome.json` `files.ignore`.**
Add `"**/.astro"`, `"**/.next"`, `"**/.vercel"`, `".planning"` to the existing array. These
are Astro/Next/Vercel generated output and planning documents — none is hand-written source,
and together they produce roughly 93% of the current errors (5321 of 5685 at first measurement). Keep the four existing entries.
Consider also adding `"**/coverage"` and `"**/.wrangler"` if either exists in the tree; check
before adding, and do not add ignores for paths that do not exist.

Measure before and after and record BOTH numbers:
`pnpm lint 2>&1 | grep -oE 'Found [0-9]+ errors' | tail -1`

**B. Decide the `verify` job contents. The decision is: `verify` does NOT run `pnpm lint`.**
After step A, `pnpm lint` still exits 1 with roughly 364 real errors. Clearing them means a
repo-wide format + organize-imports pass plus ~188 rule fixes across ~40 source files in
every workspace package. That work would collide with the file ownership of plans 17-02,
17-05, 17-06, 17-07, and 17-08 in this same phase, and it is not what SEC-01..SEC-06 ask for.

So `verify` runs `pnpm install --frozen-lockfile`, `pnpm build`, `pnpm typecheck`,
`pnpm test` — all four measured green on main after plan 17-01. Plan 17-09 later inserts
`node scripts/check-claims.mjs` into the same job.

Lint is not dropped, only de-gated: Task 3 adds a separate advisory `lint` job that reports
the residual count without blocking. Record the residual count in the SUMMARY as known,
quantified debt with a named follow-up ("repo-wide biome cleanup"), so the decision reads as
a recorded trade-off rather than a silent omission.

Do NOT relax `biome.json`'s `linter.rules` to make the count smaller. Turning off
`noNonNullAssertion` or `noExplicitAny` to manufacture a green gate is exactly the kind of
unearned claim this phase exists to eliminate. The ignore list may only exclude
non-source trees.

Commit as `chore(lint): exclude generated and planning trees from biome` with the before and
after error counts in the body.
  </action>
  <verify>
    <automated>jq -e '.files.ignore | index("**/.astro") and index("**/.next") and index("**/.vercel") and index(".planning")' biome.json && test "$(pnpm lint 2>&1 | grep -cE '(\.astro|\.next|\.vercel|\.planning)/')" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `jq -e '.files.ignore | index("**/.astro")' biome.json` exits 0, and likewise for `"**/.next"`, `"**/.vercel"`, and `".planning"`
    - `jq -e '.files.ignore | index("**/dist") and index("**/node_modules") and index("**/.turbo") and index("**/*.md")' biome.json` exits 0 — the four pre-existing entries survive
    - `jq -e '.linter.rules.recommended == true' biome.json` exits 0 and `jq -e '.linter.rules | keys | length == 1' biome.json` exits 0 — no rule was disabled to shrink the count
    - `pnpm lint 2>&1 | grep -cE '(\.astro|\.next|\.vercel|\.planning)/'` returns 0 — no diagnostic points at an excluded tree
    - The post-change error count from `pnpm lint 2>&1 | grep -oE 'Found [0-9]+ errors' | tail -1` is recorded verbatim in the commit body and the SUMMARY, and is at least 90% below the recorded pre-change count
    - `pnpm build && pnpm typecheck && pnpm test` all exit 0 on the current tree — confirming the four commands the `verify` job will run are achievable BEFORE the workflow is written
  </acceptance_criteria>
  <done>Biome lints only real source, the residual count is measured and recorded, and the commands `verify` will run are confirmed green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the authoritative test-count measurement script and generate its artifacts</name>
  <files>scripts/measure-tests.mjs, .github/test-count.json, apps/marketing/src/data/test-count.json, README.md</files>
  <read_first>
    - .github/workflows/readme-social-proof.yml (the marker-substitution pattern to extend, not replace)
    - README.md lines 15-30 (the existing STATS_START / STATS_END block — the new markers go beside it)
    - .planning/workstreams/milestone/phases/17-launch-blockers/17-RESEARCH.md (section "Measuring the authoritative test count (the exact method used in this research)")
    - turbo.json
    - package.json
  </read_first>
  <behavior>
    - Running `node scripts/measure-tests.mjs` from the repo root after `pnpm build` writes both artifacts and prints the totals
    - The summed `tests` value equals the runtime count from the vitest JSON reporter (measured truth at research time: 691), NOT the static grep count (614)
    - The summed `files` value equals the number of test files (measured truth: 39)
    - Running it twice in a row produces byte-identical artifacts apart from the `measured` date field
    - It exits non-zero if any package's vitest run fails or emits no JSON report, so a broken suite can never silently publish a stale count
  </behavior>
  <action>
Create `scripts/measure-tests.mjs` (Node ESM, no new dependencies — the repo installs
nothing new this phase). It must:

1. Iterate the explicit 13-package list given in `<interfaces>` above. Do not glob the
   workspace; `packages/config` has a stub test script and would break the run, and
   `packages/wordpress`, `apps/docs`, `apps/marketing` have no suite.
2. For each package run vitest with the JSON reporter into a temp file, e.g.
   `pnpm --filter <name> exec vitest run --reporter=json --outputFile=<tmp>/vt-<pkg>.json`.
   Assume `pnpm build` has already run (turbo's `test` task depends on `build`).
3. Sum `numTotalTests` across reports for `tests`, and sum `testResults.length` across
   reports for `files`. Never count with grep — `it.each` and dynamically generated tests
   are invisible to static counting, which is exactly how the published numbers drifted to
   288 / 637 / 675.
4. Fail loudly (non-zero exit, clear message naming the package) if any report is missing,
   unparseable, or reports failures.
5. Write the same object to BOTH paths:
   `.github/test-count.json` and `apps/marketing/src/data/test-count.json`, shaped as
   `{ "tests": <number>, "files": <number>, "measured": "<YYYY-MM-DD>" }`.
   Two paths, one source: the marketing Astro site cannot cleanly import a JSON file from
   outside its own project root, so CI writes an in-`src` copy for it. Plan 17-07 imports
   `../data/test-count.json` from `apps/marketing/src/components/Files.astro`.

Then run it locally and commit the generated artifacts, so the repo has a valid count
before CI ever runs (the marketing build would otherwise fail on a missing import).

Finally add the consumer markers to `README.md`, immediately after the existing
`<!-- STATS_END -->` line, following the identical pattern:
`<!-- TESTS_START -->` / `<!-- TESTS_END -->` wrapping a shields.io badge of the form
`![tests](https://img.shields.io/badge/tests-<N>%20passing-brightgreen)`.
Populate it now with the measured value; CI keeps it current from here on.
Do NOT hand-edit any other test number in README.md in this plan — the full claim sweep is
plan 17-09 and owns those lines.
  </action>
  <verify>
    <automated>pnpm build && node scripts/measure-tests.mjs && jq -e '.tests > 600 and .files > 30' .github/test-count.json && diff <(jq -S 'del(.measured)' .github/test-count.json) <(jq -S 'del(.measured)' apps/marketing/src/data/test-count.json)</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/measure-tests.mjs` exits 0 and prints a tests total and a files total
    - `jq -e '.tests and .files and .measured' .github/test-count.json` exits 0
    - The two artifacts are identical apart from `measured`: the `diff` in the verify command produces no output
    - `jq -r '.tests' .github/test-count.json` equals the number reported by a fresh manual vitest run and is NOT 614 (the static grep count)
    - `grep -c 'TESTS_START' README.md` returns 1 and `grep -c 'TESTS_END' README.md` returns 1
    - The badge number inside the README markers equals `jq -r '.tests' .github/test-count.json`
    - `grep -c "grep" scripts/measure-tests.mjs` returns 0 — the count must never be derived statically
  </acceptance_criteria>
  <done>One script produces the count from the vitest JSON reporter, both artifacts exist and agree, and README carries the consumer markers.</done>
</task>

<task type="auto">
  <name>Task 3: Create .github/workflows/ci.yml with the test gate, the audit gate, the advisory lint job, and the count publisher</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/readme-social-proof.yml (bot identity, permissions, [skip ci] convention)
    - .github/workflows/deploy-web.yml (existing node/pnpm setup conventions in this repo)
    - biome.json (as corrected in Task 1)
    - scripts/measure-tests.mjs (created in Task 2)
    - .planning/workstreams/milestone/phases/17-launch-blockers/17-RESEARCH.md (section "The missing ci.yml" and "Pattern 3: CI as the recurrence gate")
  </read_first>
  <action>
Create `.github/workflows/ci.yml` named `CI`, triggered on `pull_request`, on `push` to
`main`, on `workflow_dispatch`, and on a weekly `schedule` (`0 9 * * 1`) so transitive drift
is caught even with no commits.

Four jobs, deliberately separate so each can be required — or deliberately not required —
independently by the branch protection ruleset in plan 17-10:

1. `verify` — `actions/checkout@v5`, `pnpm/action-setup@v4` with version `10.32.1`,
   `actions/setup-node@v4` with `node-version: 22` and `cache: pnpm`, then
   `pnpm install --frozen-lockfile`, `pnpm build`, `pnpm typecheck`, `pnpm test`.
   **`pnpm lint` is deliberately NOT in this job**, per the Task 1 decision: it exits 1 with
   roughly 364 real errors that this phase is not scoped to fix, and `verify` becomes a
   required check in plan 17-10, so a permanently-failing step here would make the
   repository unmergeable. Put a comment in the workflow file stating this explicitly with
   the recorded error count, so the omission reads as a decision and not an oversight.
2. `audit` — same setup, then `pnpm audit --audit-level=high`. This is the load-bearing
   piece of SEC-02: Dependabot cannot update pnpm transitive dependencies at all, and 217
   of the 242 baseline alerts were transitive, so only a failing build prevents silent
   re-accumulation. Keep it a separate job. It is EXPECTED to fail until plans 17-05
   (overrides/bumps) and 17-08 (Astro major) land — that is the point, and it is why plan
   17-10 requires it only after it goes green.
3. `lint` — same setup, then `pnpm lint`, with `continue-on-error: true` at the JOB level so
   it reports without blocking. Append the error count to `$GITHUB_STEP_SUMMARY` so the debt
   stays visible on every run rather than silently accumulating. This job MUST NOT be
   proposed as a required status check; plan 17-10 requires `verify` and `audit` only.
4. `publish-count` — runs only on `push` to `main` and only if `verify` succeeded
   (`needs: verify`). Re-runs `pnpm build` then `node scripts/measure-tests.mjs`,
   substitutes the count into the README block between `<!-- TESTS_START -->` and
   `<!-- TESTS_END -->` using the same python3 `re.sub(..., flags=re.DOTALL)` approach as
   `readme-social-proof.yml`, then commits `.github/test-count.json`,
   `apps/marketing/src/data/test-count.json`, and `README.md` as
   `aeorank-bot <bot@aeorank.dev>` with `[skip ci]` in the message and pushes. Use
   `git diff --staged --quiet || git commit ...` so a no-op run does not fail.

Set top-level `permissions: contents: read`; grant `contents: write` only on the
`publish-count` job. Do not add a CodeQL workflow — CodeQL default setup is already
configured (weekly, `remote` threat model) and a workflow file would duplicate it. Do not
put deploy steps in this file; `deploy-*.yml` stay separate.

After committing and pushing, trigger a run and confirm `verify` passes. Block on completion
with `gh run watch <id> --interval 15` rather than a fixed `sleep`: a hard-coded sleep either
wastes time or reports on an unfinished run. Do NOT pass `--exit-status` to `gh run watch` —
the `audit` job is expected to fail at this wave and `--exit-status` would abort the task on
a failure that is the designed outcome. Read the per-job conclusions instead.
  </action>
  <verify>
    <automated>gh workflow run ci.yml && sleep 10 && RID=$(gh run list --workflow=ci.yml --limit 1 --json databaseId -q '.[0].databaseId') && gh run watch "$RID" --interval 15 >/dev/null; gh run view "$RID" --json jobs -q '.jobs[] | select(.name=="verify") | .conclusion' | grep -qx success</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` exists and `gh api repos/vinpatel/aeorank/actions/workflows --jq '.workflows[].path'` includes `.github/workflows/ci.yml`
    - `grep -v '^\s*#' .github/workflows/ci.yml | grep -c 'pnpm audit --audit-level=high'` returns 1
    - `grep -v '^\s*#' .github/workflows/ci.yml | grep -c 'measure-tests'` returns at least 1
    - `grep -v '^\s*#' .github/workflows/ci.yml | grep -c 'pnpm lint'` returns 1, and that occurrence is inside the `lint` job — the `verify` job contains no `pnpm lint` step
    - `grep -v '^\s*#' .github/workflows/ci.yml | grep -c 'continue-on-error: true'` returns 1 — scoped to the `lint` job only
    - For the run triggered by this task, `gh run view <id> --json jobs -q '.jobs[] | select(.name=="verify") | .conclusion'` prints `success`
    - The `audit` job exists in that same run (its conclusion may be `failure` at this wave — record which advisories caused it as input for plan 17-05)
    - The `lint` job exists in that same run and its step summary records the error count
    - A push to main produces a `publish-count` run that either commits an updated count or exits cleanly with no diff
    - `grep -c 'codeql' .github/workflows/ci.yml` returns 0 — default setup is already enabled and must not be duplicated
  </acceptance_criteria>
  <done>CI exists, `verify` is green, `audit` runs and reports honestly, `lint` reports without blocking, and the count publisher works end to end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pull request from a fork -> CI runner | Untrusted code executes in the `verify` job |
| CI runner -> `main` (publish-count job) | A workflow writes to the default branch with `contents: write` |
| npm advisory database -> build outcome | External data determines whether the build passes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-07 | Elevation of Privilege | `.github/workflows/ci.yml` | mitigate | Top-level `permissions: contents: read`; `contents: write` scoped to the `publish-count` job, which runs only on `push` to main (never on `pull_request`, so fork PRs cannot obtain write) |
| T-17-08 | Tampering | Dependency tree (transitive) | mitigate | `pnpm audit --audit-level=high` as a dedicated job, required by the ruleset in plan 17-10 — the only gate that reaches pnpm transitives |
| T-17-09 | Repudiation | Published test count | mitigate | Count is emitted by the vitest JSON reporter in a clean CI environment and committed by the bot; hand-edited counts are detectable because they diverge from `.github/test-count.json` |
| T-17-10 | Spoofing | `publish-count` git identity | accept | Commits as `aeorank-bot` using the default `GITHUB_TOKEN`; same trust model as the three existing bot workflows |
| T-17-10b | Repudiation | Advisory `lint` job | accept | Lint runs but does not gate, and 364 real violations remain. Accepted for this phase and recorded with an exact count in the SUMMARY; no document, badge, or copy line claims the codebase is lint-clean, so the debt is visible rather than misrepresented |
| T-17-SC | Tampering | GitHub Actions used | mitigate | Only first-party/widely-used actions (`actions/checkout`, `actions/setup-node`, `pnpm/action-setup`) at major-version tags; no npm packages are installed by this plan |
</threat_model>

<verification>
- `pnpm lint` reports zero diagnostics from `.astro`, `.next`, `.vercel`, or `.planning`
- `node scripts/measure-tests.mjs` exits 0 and both artifacts agree
- The `verify` job of the latest CI run concludes `success`
- The `audit` job exists and reports the true audit state
- The `lint` job exists, reports its count, and blocks nothing
- README carries `TESTS_START`/`TESTS_END` and CI keeps them current
</verification>

<success_criteria>
ROADMAP success criterion 4 is satisfied at the mechanism level: CI emits one authoritative
test count and the README badge reads from it. Half of criterion 2 is in place — the audit
gate that catches transitive drift exists (repo settings and branch protection complete it
in plan 17-10).
</success_criteria>

<output>
Create `.planning/workstreams/milestone/phases/17-launch-blockers/17-03-SUMMARY.md` when done.
Record the lint error count before and after the biome ignore change, the residual real-source
count as named debt, the measured test/file counts, and the exact list of advisories that
caused the `audit` job to fail, if it failed — plan 17-05 consumes that list.
</output>
