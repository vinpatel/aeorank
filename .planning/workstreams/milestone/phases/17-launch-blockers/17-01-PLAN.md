---
phase: 17-launch-blockers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/vercel.json
  - supabase/schema.sql
  - supabase/migrations/0001_sites_unique_user_id_url.sql
  - apps/web/app/api/cron/keep-alive/route.ts
  - apps/web/app/api/cron/keep-alive/route.test.ts
  - apps/docs/.astro/content-modules.mjs
  - apps/marketing/.astro/types.d.ts
  - packages/nuxt/package.json
  - packages/nuxt/src/runtime/server.ts
  - packages/cli/package.json
  - packages/cli/src/commands/scan.ts
  - packages/cli/src/commands/compare.ts
  - packages/cli/src/errors.ts
  - packages/next/src/__tests__/config.test.ts
  - pnpm-lock.yaml
autonomous: false
requirements: [SEC-05]
user_setup:
  - service: supabase
    why: "The migration issues DELETE FROM sites; a snapshot and the pre-flight duplicate count require DB credentials no agent holds."
    dashboard_config:
      - task: "Take a point-in-time snapshot / backup before running the migration"
        location: "Supabase Dashboard -> Project -> Database -> Backups"
      - task: "Run the pre-flight duplicate count, then apply supabase/migrations/0001_sites_unique_user_id_url.sql as the privileged SQL Editor role (bypasses RLS)"
        location: "Supabase Dashboard -> SQL Editor"
  - service: vercel
    why: "CRON_SECRET is a server-side env var; both cron routes fail closed with 500 without it."
    env_vars:
      - name: CRON_SECRET
        source: "Vercel Dashboard -> aeorank project -> Settings -> Environment Variables"

must_haves:
  truths:
    - "fix/scan-callback-url is an ancestor of origin/main"
    - "The sites table has a unique constraint on (user_id, url) so the scan upsert's onConflict resolves"
    - "apps/web/vercel.json declares both the keep-alive and rescan daily crons"
    - "The keep-alive route is deployed and returns 200 when invoked with the CRON_SECRET bearer token"
    - "pnpm typecheck exits 0 across all 17 workspace packages, so every downstream plan's gate and the CI verify job are achievable"
  artifacts:
    - path: "supabase/migrations/0001_sites_unique_user_id_url.sql"
      provides: "Retrofit of the (user_id, url) unique constraint onto an existing database, dedupe-safe"
      contains: "sites_user_id_url_key"
    - path: "apps/web/app/api/cron/keep-alive/route.ts"
      provides: "Daily Supabase keep-alive so the free-tier project does not auto-pause"
    - path: "apps/web/app/api/cron/keep-alive/route.test.ts"
      provides: "4 tests covering auth-required, missing-secret, success, and failure paths"
    - path: "apps/web/vercel.json"
      provides: "Cron schedule registration"
      contains: "keep-alive"
    - path: "packages/nuxt/package.json"
      provides: "Declaration of h3, which src/runtime/server.ts imports but the manifest never declared"
      contains: "h3"
    - path: "packages/cli/package.json"
      provides: "Declaration of @types/node, without which 21 of the CLI's 34 typecheck errors are unresolvable"
      contains: "@types/node"
  key_links:
    - from: "apps/web/vercel.json"
      to: "apps/web/app/api/cron/keep-alive/route.ts"
      via: "cron path registration"
      pattern: "api/cron/keep-alive"
    - from: "supabase/schema.sql"
      to: "supabase/migrations/0001_sites_unique_user_id_url.sql"
      via: "shared constraint name, fresh-install DDL vs retrofit"
      pattern: "sites_user_id_url_key"
    - from: "packages/nuxt/src/runtime/server.ts"
      to: "h3"
      via: "runtime import that must be declared for tsc to resolve it"
      pattern: "from \"h3\""
---

<objective>
Land the outstanding `fix/scan-callback-url` branch on `main`, including the Supabase
unique-constraint migration and the Vercel keep-alive cron route, so the scan upsert stops
failing with SQLSTATE 42P10 and the Supabase project stops auto-pausing. Then repair the
three pre-existing `pnpm typecheck` failures that every later gate in this phase depends on.

Purpose: SEC-05. This is Wave 1 because every other plan in Phase 17 modifies files on
`main`; leaving 8 unmerged commits and an untracked migration in the working tree would
force conflicts into every later wave. The typecheck repair also belongs here: `pnpm typecheck`
currently exits 2, and plans 17-02, 17-05, and 17-08 all gate on it, while plans 17-03,
17-09, and 17-10 depend on a green CI `verify` job that runs it.
Output: a merged `main` containing the keep-alive route, the cron registration, the
migration file, the applied database constraint, and a green `pnpm typecheck`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/milestone/ROADMAP.md
@.planning/workstreams/milestone/phases/17-launch-blockers/17-CONTEXT.md
@.planning/workstreams/milestone/phases/17-launch-blockers/17-RESEARCH.md

<interfaces>
<!-- Measured baseline of the repo gate, 2026-08-15, on the current working tree.
     These are facts, not predictions. Re-measure before assuming they still hold. -->

pnpm build      -> exit 0
pnpm test       -> exit 0  (30/30 turbo tasks successful)
pnpm typecheck  -> exit 2  (12/15 successful; 3 packages fail with 40 errors total)
pnpm lint       -> exit 1  (~5700 errors, 93% of them from generated/planning trees
                            biome does not ignore — scoped and de-gated in plan 17-03, NOT here)

The three typecheck failures, measured with `pnpm exec turbo typecheck --continue`:

  @aeorank/nuxt (2 errors)
    src/runtime/server.ts(1,55): TS2307 Cannot find module 'h3' or its type declarations
    src/runtime/server.ts(8,29): TS7006 Parameter 'event' implicitly has an 'any' type
    Cause: `h3` is imported by a real source file but is NOT declared in
    packages/nuxt/package.json (devDeps: @nuxt/kit, nuxt, tsup, typescript, vitest;
    deps: @nuxt/kit). It is an undeclared dependency. The TS7006 is downstream of the
    TS2307 — once h3's types resolve, defineEventHandler types `event` and it clears.

  @aeorank/next (4 errors)
    src/__tests__/config.test.ts(36,17), (42,34), (55,34), (68,34):
      TS2339 Property 'headers' does not exist on type '{}'
    Cause: `withAeorank(cfg)` returns `<T extends Record<string, unknown>>(c: T) => T`.
    The test calls `wrapper({})`, so T infers as `{}` and `.headers` is absent. This is a
    test-side inference defect, not a defect in src/config.ts.

  aeorank-cli (34 errors)
    21 x TS2307/TS2580/TS2503: cannot find 'node:fs' / 'node:path' / 'node:url',
      cannot find name 'process', cannot find namespace 'NodeJS'
      Cause: packages/cli/package.json declares no @types/node
      (devDeps: typescript, vitest, tsup). packages/next declares @types/node ^22.19.15.
    12 x TS2454 "Variable 'before'/'after' is used before being assigned" in
      src/commands/compare.ts — these are DOWNSTREAM of the missing @types/node:
      the catch block ends in `process.exit(1)`, which only narrows to `never` when
      @types/node is present. Expect them to clear with the declaration alone.
     1 x TS2345 src/commands/scan.ts(115,34): 'PageScore | undefined' is not assignable
      to parameter of type 'PageScore'. LIKELY the same downstream class as the TS2454s:
      scan.ts:88-110 ALREADY contains `if (!matchedPage) { …console.error…; process.exit(1); }`,
      so `matchedPage` is genuinely narrowed at line 115 — tsc only fails to see it because
      `process.exit` cannot be typed `never` without @types/node. Re-measure after step B's
      declaration before writing any code for it.

Root pnpm.overrides already pins `h3 >=1.15.9`, so declaring h3 introduces no new
resolution and no new package name into the tree.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit the outstanding branch work and rebase onto origin/main</name>
  <files>apps/web/vercel.json, supabase/schema.sql, supabase/migrations/0001_sites_unique_user_id_url.sql, apps/web/app/api/cron/keep-alive/route.ts, apps/web/app/api/cron/keep-alive/route.test.ts, apps/docs/.astro/content-modules.mjs, apps/marketing/.astro/types.d.ts</files>
  <read_first>
    - supabase/migrations/0001_sites_unique_user_id_url.sql
    - supabase/schema.sql
    - apps/web/vercel.json
    - apps/web/app/api/cron/keep-alive/route.ts
    - apps/web/app/api/cron/keep-alive/route.test.ts
    - .planning/workstreams/milestone/phases/17-launch-blockers/17-RESEARCH.md (section "SEC-05: Landing the branch safely")
  </read_first>
  <action>
Stage and commit exactly these paths on the current `fix/scan-callback-url` branch:
`apps/web/vercel.json`, `supabase/schema.sql`,
`supabase/migrations/0001_sites_unique_user_id_url.sql`,
`apps/web/app/api/cron/keep-alive/` (route.ts and route.test.ts),
`apps/docs/.astro/content-modules.mjs`, `apps/marketing/.astro/types.d.ts`.

Explicitly DO NOT commit `LAUNCH.md` — it carries the false "675 tests, 0 Dependabot /
CodeQL alerts" claim and is corrected and committed by plan 17-09. Explicitly DO NOT
commit `aeorank-social-preview.png` at the repo root — it is a byte-identical duplicate of
`.github/social-preview.png` (both 77807 bytes); delete the root copy instead.

The two `.astro/` files are Astro-generated churn; commit them in a separate commit from
the functional change so the diff reads cleanly.

Then rebase onto `origin/main` (branch is 8 ahead / 1 behind; the behind commit is
`chore: update social proof stats [skip ci]`, which touches `README.md` only — this plan
touches no README, so expect no conflict). Run `git fetch origin` first so `origin/main`
is current before you rebase or compare against it.

Run the gate before considering the task done:
`pnpm install --frozen-lockfile && pnpm build && pnpm test`.

`pnpm typecheck` is DELIBERATELY excluded from this task's gate. It exits 2 today on three
pre-existing defects that have nothing to do with this branch (see `<interfaces>` above);
Task 2 repairs them and restores the full gate. Do not attempt to work around a typecheck
failure here by editing branch files.
  </action>
  <verify>
    <automated>test -f supabase/migrations/0001_sites_unique_user_id_url.sql && test -f apps/web/app/api/cron/keep-alive/route.ts && grep -q 'keep-alive' apps/web/vercel.json && grep -q 'sites_user_id_url_key' supabase/schema.sql && git ls-tree -r HEAD --name-only | grep -q 'supabase/migrations/0001_sites_unique_user_id_url.sql' && git ls-tree -r HEAD --name-only | grep -q 'keep-alive/route.ts' && test "$(git status --porcelain | grep -cvE '^\?\? LAUNCH\.md$')" -eq 0 && pnpm --filter @aeorank/web test</automated>
  </verify>
  <acceptance_criteria>
    - `test "$(git status --porcelain | grep -cvE '^\?\? LAUNCH\.md$')" -eq 0` exits 0 — nothing modified or staged, and the only remaining untracked entry is `LAUNCH.md`
    - **Positive existence assertions (these are what make the gate discriminating — a clean `git status` alone cannot distinguish "committed correctly" from "the files were never here").** All four must hold on disk: `supabase/migrations/0001_sites_unique_user_id_url.sql`, `apps/web/app/api/cron/keep-alive/route.ts`, `keep-alive` present in `apps/web/vercel.json`, `sites_user_id_url_key` present in `supabase/schema.sql`
    - **And the same artefacts must be in `HEAD`, not merely on disk:** `git ls-tree -r HEAD --name-only` contains both `supabase/migrations/0001_sites_unique_user_id_url.sql` and `keep-alive/route.ts`. If any of these fail, STOP — do not re-author the migration. It contains a `DELETE FROM sites` and re-authoring it from scratch immediately before an irreversible apply against live data is the single worst failure mode in this phase. Report the missing artefacts and halt.
    - `aeorank-social-preview.png` no longer exists at the repository root: `test ! -f aeorank-social-preview.png`
    - `git log --oneline origin/main..HEAD` shows the keep-alive/migration commit and a separate generated-files commit
    - `git rev-list --left-right --count origin/main...HEAD` reports `0` on the left (no longer behind)
    - `pnpm --filter @aeorank/web test` exits 0 and the 4 tests in `apps/web/app/api/cron/keep-alive/route.test.ts` pass
    - `pnpm build && pnpm test` both exit 0
    - `grep -c 'keep-alive' apps/web/vercel.json` returns at least 1
  </acceptance_criteria>
  <done>Branch is rebased, clean, fully committed except LAUNCH.md, and the build/test gate is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Repair the three pre-existing pnpm typecheck failures</name>
  <files>packages/nuxt/package.json, packages/nuxt/src/runtime/server.ts, packages/cli/package.json, packages/cli/src/commands/scan.ts, packages/cli/src/commands/compare.ts, packages/cli/src/errors.ts, packages/next/src/__tests__/config.test.ts, pnpm-lock.yaml</files>
  <!-- runtime/server.ts, compare.ts, and errors.ts are listed because they MAY need edits if
       the declaration-only fixes do not clear their errors; the expectation is that they do.
       No other plan in this phase owns any of these paths, in any wave. -->
  <read_first>
    - packages/nuxt/src/runtime/server.ts (the h3 import at line 1 and the untyped `event` at line 8)
    - packages/nuxt/package.json (note @nuxt/kit appears in BOTH dependencies and devDependencies; h3 appears in neither)
    - packages/cli/package.json and packages/cli/tsconfig.json (tsconfig sets no `types` array; tsconfig.base.json sets `strict: true`, `moduleResolution: NodeNext`)
    - packages/cli/src/commands/scan.ts lines 88-115 — read the WHOLE existing `if (!matchedPage) { ...; process.exit(1); }` guard at 88-110 BEFORE touching line 115; it already handles the miss
    - packages/next/src/config.ts (the generic signature `<T extends Record<string, unknown>>(nextConfig: T): T`)
    - packages/next/src/__tests__/config.test.ts (the four `wrapper({})` call sites)
    - package.json (root pnpm.overrides — `h3 >=1.15.9` is already pinned there)
  </read_first>
  <behavior>
    - `pnpm exec turbo typecheck --continue` reports 15/15 tasks successful and exits 0
    - `pnpm typecheck` exits 0
    - `pnpm test` still exits 0 and the total test count is unchanged from Task 1's run
    - `pnpm build` still exits 0 and every package still emits its `dist/`
    - No new package NAME enters the tree that is absent from `pnpm-lock.yaml` today apart from `h3` and `@types/node`, both of which already resolve transitively
  </behavior>
  <action>
Fix the three failures in the order given. Re-measure after each so the attribution is
clear; record the residual error count after each step in the summary.

**A. `@aeorank/nuxt` — declare the undeclared `h3` dependency.**
`packages/nuxt/src/runtime/server.ts:1` does `import { defineEventHandler, setResponseHeader } from "h3"`,
and `packages/nuxt/src/module.ts:46` emits the same import inside a Nitro virtual-module
template. Neither is satisfied by the manifest. Add `h3` to `packages/nuxt`
`dependencies` (not devDependencies — `src/runtime/server.ts` is shipped runtime code that a
consumer's Nuxt app loads) at a floor consistent with the root override, i.e. `^1.15.9`.
Resolve the exact current version with `npm view h3 version` before pinning; do not invent one.

Adding a `dependencies` entry to a published package is a consumer-contract change. It is the
correct one: the package already required `h3` at runtime and merely failed to say so. State
this explicitly in the commit body.

Do NOT silence the error with `// @ts-ignore`, a `declare module "h3"` shim, or by adding
`"skipLibCheck"`-style escapes. The TS7006 on `event` is downstream of the missing types and
should clear on its own once `h3` resolves — if it does not, type the parameter from h3's own
exported `H3Event`, never with `any`.

**B. `aeorank-cli` — declare `@types/node`, then fix the one real narrowing defect.**
Add `@types/node` to `packages/cli` devDependencies at the same major already used elsewhere
in the workspace (`packages/next` declares `^22.19.15`). Consistency matters more than
recency here; the root `engines.node` is `>=20` and must not change.

Then re-run `pnpm --filter aeorank-cli exec tsc --noEmit`. Expect the 21 TS2307/TS2580/TS2503
errors AND the 12 TS2454 "used before being assigned" errors in `src/commands/compare.ts` to
clear together — the TS2454s exist only because `process.exit(1)` cannot be typed `never`
without `@types/node`. If any TS2454 survives, fix it by restructuring the try/catch so the
assignment is unambiguous; do not add a non-null assertion.

`src/commands/scan.ts(115,34)` is expected to clear the same way. **Re-measure it after
adding `@types/node`; only if TS2345 survives, narrow explicitly.** Do NOT write a guard
before re-measuring: `scan.ts:88-110` already holds an
`if (!matchedPage) { ... }` block that builds an availablePaths list, prints
"Error: No page found matching <path>." (or the JSON equivalent), and ends in
`process.exit(1)`, so the miss is already handled and `matchedPage` is already narrowed at line 115. The reason
tsc does not see it is identical to the reason the 12 TS2454s exist — `process.exit(1)` cannot
be typed `never` without `@types/node`. Adding a second guard would add unreachable dead code
to a file this phase is meant to be cleaning up.

If and only if TS2345 survives the declaration, narrow it explicitly at the use site (for
example by binding the narrowed value to a `const` inside the branch that follows the existing
guard). Do NOT use `!`; Biome's `noNonNullAssertion` already reports 116 violations in this
repo and this phase must not add a 117th. Record in the summary which of the two outcomes
actually occurred.

**C. `@aeorank/next` — fix the test-side generic inference.**
`withAeorank` returns `<T extends Record<string, unknown>>(nextConfig: T): T`, so
`wrapper({})` infers `T = {}` and `result.headers` does not exist. Fix the FOUR call sites in
`packages/next/src/__tests__/config.test.ts` by giving the argument a type that carries the
property — e.g. pass a value typed `Record<string, unknown>` — so `T` widens correctly.
Do NOT change `packages/next/src/config.ts`: its signature is the published contract and the
defect is in the test, not the source.

Then run the full restored gate:
`pnpm install && pnpm build && pnpm typecheck && pnpm test`.

Commit as a single standalone commit,
`fix(types): repair pre-existing typecheck failures in nuxt, cli, and next`,
with a body listing the three causes and the error count before (40) and after (0). Keep it
separate from the Task 1 branch commits so it is revertible alone. Commit the updated
`pnpm-lock.yaml` with it.
  </action>
  <verify>
    <automated>pnpm install && pnpm build && pnpm typecheck && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm typecheck` exits 0
    - `pnpm exec turbo typecheck --continue 2>&1 | grep -c 'error TS'` returns 0
    - `pnpm build` and `pnpm test` both still exit 0
    - `jq -e '.dependencies.h3 != null' packages/nuxt/package.json` exits 0
    - `jq -e '.devDependencies["@types/node"] != null' packages/cli/package.json` exits 0
    - `git diff HEAD~1 --name-only | grep -c 'packages/next/src/config.ts'` returns 0 — the published source signature was not touched
    - `! grep -qE '@ts-ignore|@ts-expect-error|@ts-nocheck' packages/nuxt/src/runtime/server.ts packages/cli/src/commands/scan.ts packages/next/src/__tests__/config.test.ts` exits 0 — no escape hatch in any of the three files. (`grep -c` across multiple files prints one `file:count` line PER FILE and can never itself equal 0; use the quiet form.)
    - `grep -c 'declare module' packages/nuxt/src/runtime/server.ts` returns 0 — h3 is declared in the manifest, not shimmed
    - `jq -e '.engines.node == ">=20"' package.json` exits 0 — the root Node floor is unchanged
  </acceptance_criteria>
  <done>`pnpm build && pnpm typecheck && pnpm test` is green end to end, so every later plan's gate and the CI verify job are achievable.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Apply the Supabase migration and set CRON_SECRET (credential-gated)</name>
  <files>(no file edits — the migration runs in the Supabase SQL Editor and CRON_SECRET is a Vercel env var)</files>
  <read_first>
    - supabase/migrations/0001_sites_unique_user_id_url.sql
    - .planning/workstreams/milestone/phases/17-launch-blockers/17-RESEARCH.md (section "Migration review — it is correct", incl. "Residual risks the plan must handle")
  </read_first>
  <what-built>
Task 1 committed a dedupe-safe migration that adds the `sites_user_id_url_key` unique
constraint on `(user_id, url)`. It re-points `scans.site_id` to the surviving row BEFORE
deleting duplicates (`scans` is the only foreign key referencing `sites`), and is guarded
by an `if not exists (select 1 from pg_constraint where conname = 'sites_user_id_url_key')`
check so it is idempotent. It contains a `DELETE FROM sites`, so it cannot be applied by an
agent without credentials or a backup. Task 2 additionally restored `pnpm typecheck` to green.
  </what-built>
  <how-to-verify>
1. Supabase Dashboard -> Project -> Database -> Backups: take a point-in-time snapshot.
   This is non-negotiable; the migration deletes rows.
2. Supabase Dashboard -> SQL Editor, run the pre-flight blast-radius query and record the
   row count in your reply:
   `select user_id, url, count(*) from sites group by 1,2 having count(*) > 1;`
3. In the same SQL Editor session (privileged role — bypasses RLS; do NOT run this through
   the anon or authenticated client), paste and run the full contents of
   `supabase/migrations/0001_sites_unique_user_id_url.sql`.
4. Confirm the constraint now exists:
   `select conname from pg_constraint where conname = 'sites_user_id_url_key';`
   Expect exactly one row.
5. Re-run the same migration script a second time. It must complete without error
   (idempotency check).
6. Vercel Dashboard -> aeorank project -> Settings -> Environment Variables: confirm
   `CRON_SECRET` is set for the Production environment. If it is absent, set it now —
   without it both `/api/cron/keep-alive` and `/api/cron/rescan` return 500 on every
   invocation and Supabase pauses anyway, which is the exact failure the keep-alive exists
   to prevent.
7. Supabase Dashboard -> Project home: confirm the project status is Active, not Paused.
   If paused, resume it.
  </how-to-verify>
  <acceptance_criteria>
    - User reports the pre-flight duplicate row count as a number (including 0)
    - User confirms a backup/snapshot was taken before the migration ran
    - User confirms `select conname from pg_constraint where conname = 'sites_user_id_url_key'` returns one row
    - User confirms the second run of the migration completed without error
    - User confirms `CRON_SECRET` is present in Vercel Production env vars
    - User confirms the Supabase project status is Active
  </acceptance_criteria>
  <action>Pause execution and present the verification steps above to the user. Do not attempt the
Supabase migration or the Vercel env var yourself — both are credential-gated. When the user
replies, record the pre-flight duplicate count verbatim in the plan summary before resuming
with Task 4.</action>
  <verify>
    <human-check>User confirms backup taken, constraint present, second run clean, CRON_SECRET set, project Active</human-check>
  </verify>
  <done>User has reported the pre-flight duplicate count and confirmed the migration applied idempotently, with CRON_SECRET set and the Supabase project Active.</done>
  <resume-signal>Reply with the pre-flight duplicate count and "applied" — or describe the failure.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Merge to main and confirm the deploy</name>
  <files>(no file edits — git operation)</files>
  <read_first>
    - .github/workflows/deploy-web.yml
  </read_first>
  <action>
**Before merging, record the rollback point.** Run `git fetch origin && git rev-parse origin/main`
and write the resulting SHA into `17-01-SUMMARY.md` as "pre-merge origin/main SHA". This is the
only one of the phase's four irreversible actions whose pre-state is not otherwise captured —
the Supabase apply has a snapshot and pre-flight count, and both repo-settings changes in 17-10
record their prior values. Without this line the rollback point is reflog-dependent, which does
not survive a fresh clone or another machine.

With the constraint applied in the database (Task 3 confirmed), merge `fix/scan-callback-url`
into `main` and push. Use a fast-forward or a merge commit — no squash, so the separate
generated-files commit and the separate typecheck-repair commit stay legible and individually
revertible.

`main` is currently unprotected (branch protection is added later, in plan 17-10), so a
direct push is expected to succeed. Do not enable protection here.

Run `git fetch origin` before every ancestry comparison in this task — `origin/main` is a
remote-tracking ref and a stale one makes `git merge-base --is-ancestor` report a false
negative immediately after your own push.

After the push, wait for `deploy-web.yml` to complete and confirm it succeeded via
`gh run list --workflow=deploy-web.yml --limit 1`. Then confirm the deployed dashboard
still answers: `curl -s -o /dev/null -w "%{http_code}" https://app.aeorank.dev` returns 200.
Do not attempt to invoke the cron route directly — it requires the CRON_SECRET bearer
token, which is server-side only; its live verification belongs to plan 17-04.
  </action>
  <verify>
    <automated>git fetch origin && git merge-base --is-ancestor fix/scan-callback-url origin/main && gh run list --workflow=deploy-web.yml --limit 1 --json conclusion -q '.[0].conclusion' | grep -qx success</automated>
  </verify>
  <acceptance_criteria>
    - `git fetch origin && git merge-base --is-ancestor fix/scan-callback-url origin/main` exits 0
    - `git log origin/main --oneline -- supabase/migrations/0001_sites_unique_user_id_url.sql` returns at least one commit
    - `git log origin/main --oneline -- apps/web/app/api/cron/keep-alive/route.ts` returns at least one commit
    - `git log origin/main --oneline -- packages/cli/package.json` includes the typecheck-repair commit
    - `gh run list --workflow=deploy-web.yml --limit 1 --json conclusion -q '.[0].conclusion'` prints `success`
    - `curl -s -o /dev/null -w "%{http_code}" https://app.aeorank.dev` prints `200`
    - `pnpm typecheck` still exits 0 on `main` after the merge
  </acceptance_criteria>
  <done>`main` contains the branch and the typecheck repair; the web deploy succeeded and app.aeorank.dev returns 200.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vercel Cron -> `/api/cron/keep-alive` | Unauthenticated internet traffic can reach the route; only the shared secret distinguishes the scheduler |
| Application -> Supabase (`sites`, `scans`) | Row-level security separates users; the migration runs as a privileged role that bypasses RLS |
| Undeclared dependency -> published package consumers | `packages/nuxt` shipped runtime code importing `h3` without declaring it; consumers resolved it by accident of hoisting |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Spoofing | `apps/web/app/api/cron/keep-alive/route.ts` | mitigate | Route requires `Authorization: Bearer $CRON_SECRET` and fails closed with 500 when the secret is unset; already covered by 4 tests in `route.test.ts` |
| T-17-02 | Tampering | `supabase/migrations/0001_sites_unique_user_id_url.sql` | mitigate | Migration issues `DELETE FROM sites`; blocking human checkpoint requires a snapshot and a pre-flight count before it runs, and re-points `scans.site_id` before deleting so the cascade never fires |
| T-17-03 | Elevation of Privilege | Supabase SQL Editor session | accept | Migration must run as the privileged role to bypass RLS; scoped to a single one-off DDL statement executed by the repo owner |
| T-17-04 | Information Disclosure | `CRON_SECRET` | mitigate | Stored only as a Vercel environment variable, never committed; the checkpoint asks the user to confirm presence, never to paste the value |
| T-17-04b | Tampering | Undeclared `h3` in `packages/nuxt` | mitigate | Phantom dependency resolved only by hoisting; declaring it in `dependencies` at `^1.15.9` (consistent with the existing root override) makes the resolution explicit and lockfile-pinned instead of ambient |
| T-17-SC | Tampering | npm/pnpm installs | mitigate | Two package names are newly DECLARED (`h3`, `@types/node`) but neither is new to the tree — both already resolve transitively in `pnpm-lock.yaml`, and `h3` is already pinned by the root override at `>=1.15.9`. RESEARCH.md `## Package Legitimacy Audit` records zero net-new package names for this phase; verify both resolve to the existing lockfile entries rather than pulling a new publisher |
</threat_model>

<verification>
- `git fetch origin && git merge-base --is-ancestor fix/scan-callback-url origin/main` exits 0
- `pnpm build && pnpm typecheck && pnpm test` all exit 0
- The `sites_user_id_url_key` constraint exists in the production database (human-confirmed)
- `CRON_SECRET` is set in Vercel Production (human-confirmed)
</verification>

<success_criteria>
ROADMAP success criterion 5 is satisfied: `fix/scan-callback-url` is merged to `main` with
`supabase/migrations/` applied and `apps/web/app/api/cron/keep-alive/` deployed — and the
repository-wide typecheck gate that plans 17-02, 17-03, 17-05, 17-08, 17-09, and 17-10 all
depend on is green for the first time.
</success_criteria>

<output>
Create `.planning/workstreams/milestone/phases/17-launch-blockers/17-01-SUMMARY.md` when done.
Record the pre-flight duplicate row count reported by the user — it is the only measurement
of production data shape this phase obtains. Also record the typecheck error count after each
of Task 2's three steps, and the exact version `h3` and `@types/node` resolved to.
</output>
