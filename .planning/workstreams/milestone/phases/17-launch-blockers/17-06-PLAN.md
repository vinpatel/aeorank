---
phase: 17-launch-blockers
plan: 06
type: execute
wave: 3
depends_on: ["17-01"]
files_modified:
  - apps/marketing/src/components/Pricing.astro
  - apps/marketing/src/components/FAQ.astro
  - apps/marketing/src/components/OpenSource.astro
  - apps/marketing/astro.config.mjs
autonomous: true
requirements: [SEC-03]

must_haves:
  truths:
    - "The marketing pricing tiers are Free $0, Pro $29, and API $99 — the exact set enforced in apps/web/lib/stripe.ts — with Agency $499 shown as roadmap, not purchasable"
    - "No tier name appears on the marketing site that does not exist as a key in the PLANS object"
    - "The admin tier never appears on any public surface"
    - "PDF export and REST API are visibly labelled as roadmap rather than presented as purchasable today"
    - "No competitor price or cheapness multiplier is stated without a measured source and a measurement date"
  artifacts:
    - path: "apps/marketing/src/components/Pricing.astro"
      provides: "Tier cards matching the canonical pricing in PROJECT.md and the PLANS object"
      contains: "$29"
    - path: "apps/marketing/astro.config.mjs"
      provides: "FAQ answers free of the 14x-cheaper claim and the nonexistent $99 Agency tier"
  key_links:
    - from: "apps/marketing/src/components/Pricing.astro"
      to: "apps/web/lib/stripe.ts"
      via: "tier names and quotas must match the PLANS object"
      pattern: "free|pro|api"
---

<objective>
Reconcile every pricing, tier, and competitor claim on the marketing site to a measured
source: PROJECT.md for canonical pricing (D-01), `apps/web/lib/stripe.ts` for what actually
exists in code, and `.planning/COMPETITIVE-PARITY.md` for competitor pricing measured
2026-08-15.

Purpose: SEC-03. The site currently sells an "Agency $99" tier that does not exist in code,
at a price PROJECT.md assigns to a different tier, bundling two features that are not built
(PDF export is Phase 22, the REST API is Phase 18), against four competitor prices of which
three are wrong. This is the single highest-risk cluster of false claims — it is a
chargeback exposure, not just a credibility one.
Output: corrected pricing, FAQ, and open-source copy on aeorank.dev.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/COMPETITIVE-PARITY.md
@.planning/workstreams/milestone/phases/17-launch-blockers/17-CONTEXT.md
@.planning/workstreams/milestone/phases/17-launch-blockers/17-RESEARCH.md

<interfaces>
<!-- The plan tiers that actually exist, from apps/web/lib/stripe.ts. Marketing must not
     name a tier outside this set, and must never show `admin`. -->
export const PLANS = {
  free:  { name: "Free",  scansPerMonth: 3,        maxSites: 1 },
  pro:   { name: "Pro",   scansPerMonth: 50,       maxSites: 5,  priceId: STRIPE_PRO_PRICE_ID },
  api:   { name: "API",   scansPerMonth: 500,      maxSites: 50, priceId: STRIPE_API_PRICE_ID },
  admin: { name: "Admin", scansPerMonth: Infinity, maxSites: Infinity },  // INTERNAL — never public
} as const;

Canonical pricing (PROJECT.md, D-01): Free / Pro $29 / API $99 / Agency $499.

Competitor pricing measured 2026-08-15 (.planning/COMPETITIVE-PARITY.md §1):
  Profound   entry $99/mo Starter   (top self-serve $399 Growth)
  Scrunch    undisclosed (7-day trial, sales-led)
  Otterly    entry $29/mo Lite      (top self-serve $489 Premium)
  Peec       undisclosed (4 tiers)
  AthenaHQ   FREE (300 credits), $295/mo Starter above it
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile the pricing tiers to the canonical set and label unbuilt features</name>
  <files>apps/marketing/src/components/Pricing.astro</files>
  <read_first>
    - apps/marketing/src/components/Pricing.astro (the full `tiers` array at lines 5-72 and the `competitors` array at 74-79)
    - apps/web/lib/stripe.ts (the PLANS object — the source of truth for what exists)
    - .planning/PROJECT.md (Constraints -> Payments line: canonical pricing)
    - .planning/workstreams/milestone/phases/17-launch-blockers/17-CONTEXT.md (D-01, D-02)
  </read_first>
  <action>
Rewrite the `tiers` array in `Pricing.astro` to the canonical set (D-01). Do not invent
tiers or prices beyond this set.

1. `starter` -> id `free`, name **Free**, price **$0**. Keep the measured quotas: 1 site,
   3 dashboard scans/mo. These match `PLANS.free` exactly.
2. `pro` stays name **Pro**, price **$29**, 5 sites, 50 scans/mo — matches `PLANS.pro`.
   In its `scored` list, `PDF export` currently reads `included` and is FALSE: PDF export is
   RPT-03, shipping in Phase 22. Change it to a roadmap state — keep the row visible per
   D-02 (do not delete it), give it a distinct third state such as `v: "roadmap"` rendered
   with a visible "coming soon" indicator, and make sure it does not render inside the
   included/checked style. Add whatever minimal CSS/markup the existing tier card needs to
   render that third state; do not restyle the component (37signals aesthetic is a locked
   project constraint).
3. `agency` at $99 is the false one. The $99 tier in code is `api` (`PLANS.api`: 50 sites,
   500 scans/mo). Rename it to id `api`, name **API**, price **$99**, and keep the measured
   quotas. Every line in its `scored` list that names an unbuilt capability — `REST API`,
   `bulk scan endpoints`, `webhooks`, `client-ready PDFs` — must carry the same roadmap
   indicator, because the public API is API-01..07 in Phase 18 and PDF export is Phase 22.
   The tier is real and its quotas are real; the feature list is not, yet.
4. Add a fourth card for **Agency $499** (canonical per PROJECT.md) in the roadmap state:
   visibly not purchasable, no checkout CTA, labelled "coming soon". Do not wire a Stripe
   CTA to it — there is no `agency` key in `PLANS` and no price ID.
5. `admin` must never appear.
6. Fix the pro tier's `note`: "Roughly 7–14× cheaper than Profound, Scrunch, and Peec" is
   false. Profound's entry tier is $99/mo, making it ~3.4× at $29; Scrunch and Peec do not
   publish prices at all, so no multiplier against them is computable. Replace the
   multiplier with a claim that survives inspection — e.g. that the scoring engine, the 36
   criteria, the 9 generated files, and the CLI are MIT-licensed and ungated on every tier,
   which is a fact about this repo rather than about someone else's price page.
7. The `competitors` array (Profound $399, Scrunch $250, Otterly $189, Semrush $129) has
   three wrong values. Prefer removing the hardcoded price comparison entirely: competitor
   pricing drifts and any number committed here becomes the next false claim. If a
   comparison is retained, it must use only the values measured in COMPETITIVE-PARITY.md
   (Profound $99 entry, Otterly $29 Lite, Scrunch and Peec undisclosed, AthenaHQ free) and
   must carry a visible "measured 2026-08-15" stamp beside it.
8. Update the section meta counter (`3 plans`) to match the number of cards actually
   rendered, and the "$29 instead of $299" line in the section intro — no competitor is
   priced at $299; AthenaHQ was $295 in April and now has a free tier.
  </action>
  <verify>
    <automated>cd apps/marketing && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @aeorank/marketing build` exits 0 (use the package's actual name from apps/marketing/package.json)
    - `grep -c 'Agency' apps/marketing/src/components/Pricing.astro` returns at least 1 and every occurrence is in the roadmap/coming-soon card, never beside `$99`
    - `grep -c '\$99' apps/marketing/src/components/Pricing.astro` returns at least 1 and every `$99` occurrence sits in the tier named `API`
    - `grep -c 'admin' apps/marketing/src/components/Pricing.astro` returns 0
    - `grep -c '7–14\|7-14\|14×\|14x' apps/marketing/src/components/Pricing.astro` returns 0
    - `grep -c '\$399\|\$250\|\$189\|\$299' apps/marketing/src/components/Pricing.astro` returns 0
    - Every tier `id` in the file is one of `free`, `pro`, `api`, `agency`, and the first three match keys in `apps/web/lib/stripe.ts`
    - The rendered output marks PDF export and REST API as roadmap: `grep -Eci 'coming soon|roadmap' apps/marketing/src/components/Pricing.astro` returns at least 1
  </acceptance_criteria>
  <done>Pricing cards match the canonical tier set, unbuilt features are visibly roadmap, and no false competitor price remains in the component.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the FAQ and open-source competitor claims</name>
  <files>apps/marketing/astro.config.mjs, apps/marketing/src/components/FAQ.astro, apps/marketing/src/components/OpenSource.astro</files>
  <read_first>
    - apps/marketing/astro.config.mjs (the FAQ answers at lines 39, 43, and 51)
    - apps/marketing/src/components/FAQ.astro (line 26 — the "$89–$295/mo floor prices" claim)
    - apps/marketing/src/components/OpenSource.astro (line 25 — the "$99–$499/mo floor" claim)
    - .planning/COMPETITIVE-PARITY.md (section 1 and "What changed since April")
    - packages/core/src/constants.ts (DEFAULT_CONFIG.maxPages — the real crawl limit)
  </read_first>
  <action>
Three files, five specific corrections. Every replacement number must come from a source
named in `<interfaces>` above.

`apps/marketing/astro.config.mjs`:
- Line ~39: "we're just **14x cheaper** and open source" is false — Profound's entry tier is
  $99/mo, so the entry-point ratio is ~3.4×. Drop the multiplier. The differentiation claim
  that IS true and defensible is the one already in the same answer: Profound monitors what
  AI says about you; AEOrank scores technical AI readiness deterministically and generates
  the 9 files. Keep that, delete the price multiplier. Also correct "Profound ($99-399/mo)"
  only if it disagrees with COMPETITIVE-PARITY.md — as measured it is correct, so leave it
  and add the measurement date.
- Line ~43: the pricing answer says "Agency ($99/mo) scales to 50 sites with API access" and
  credits Pro with "PDF exports". Rewrite to the canonical tiers: Free (1 site, 3 scans),
  Pro $29 (5 sites, 50 scans, score history, auto-rescan), API $99 (50 sites, 500 scans).
  Any mention of PDF export or REST API must be marked as roadmap, matching Task 1. Do not
  name an Agency tier at $99 anywhere.
- Line ~51: "crawls up to 50 pages" understates the product and is untraceable —
  `DEFAULT_CONFIG.maxPages` in `packages/core/src/constants.ts` is **200**; 50 is only the
  value written into the `aeorank init` template at `packages/cli/src/commands/init.ts:16`.
  State the real default, or state it as "configurable, defaults to 200". Read the constant
  before writing the number; do not copy 200 from this plan without confirming it.

`apps/marketing/src/components/FAQ.astro` line ~26: "Floor prices there run $89–$295/mo" is
stale — AthenaHQ now has a free tier and Otterly Lite is $29. Remove the price range rather
than replacing it with a new one that will also go stale; make the point qualitatively
(closed-source dashboards, sales-led pricing, no on-page generation) and, if any range is
kept, stamp it "measured 2026-08-15".

`apps/marketing/src/components/OpenSource.astro` line ~25: "$99–$499/mo floor" has the same
problem — AthenaHQ's floor is now $0. Apply the same treatment. Note this line also claims
"11 framework plugins" under MIT; that claim is TRUE of the repository (11 plugin packages
exist) and only becomes false when it describes npm (10 published, `@aeorank/wordpress`
returns 404). Since the sentence is about the license of code in the repo, leave the number
at 11 but make the surface explicit — "11 framework plugins in the repo" — per D-10.
  </action>
  <verify>
    <automated>cd apps/marketing && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - The marketing build exits 0
    - `grep -c '14x cheaper\|14× cheaper' apps/marketing/astro.config.mjs` returns 0
    - `grep -c 'Agency (\$99' apps/marketing/astro.config.mjs` returns 0
    - `grep -c '50 pages' apps/marketing/astro.config.mjs` returns 0, and the replacement number equals `node -e "import('./packages/core/dist/index.js').then(m=>console.log(m.DEFAULT_CONFIG.maxPages))"` run from the repo root
    - `grep -c '89–\$295\|89-\$295' apps/marketing/src/components/FAQ.astro` returns 0
    - `grep -c '99–\$499\|99-\$499' apps/marketing/src/components/OpenSource.astro` returns 0
    - Any surviving competitor price in these three files appears verbatim in `.planning/COMPETITIVE-PARITY.md` and is accompanied by a measurement date
    - `grep -c '11 framework plugins' apps/marketing/src/components/OpenSource.astro` returns at most 1, and if present it is qualified with "in the repo"
  </acceptance_criteria>
  <done>No competitor price, cheapness multiplier, tier name, or crawl limit on the marketing site is stated without a measured source.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Marketing copy -> prospective customer | Public commercial statements; a false capability claim on a paid tier is a refund/chargeback exposure |
| Marketing copy -> billing code | The tiers named on the site must be the tiers `apps/web/lib/stripe.ts` can actually sell |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-17 | Spoofing | `apps/marketing/src/components/Pricing.astro` | mitigate | Every tier name and price is reconciled to the `PLANS` object and PROJECT.md; the nonexistent "Agency $99" checkout path is removed so no customer can be charged for a tier the system cannot provision |
| T-17-18 | Repudiation | Competitor price claims | mitigate | Hardcoded competitor prices removed, or reduced to values traceable to `.planning/COMPETITIVE-PARITY.md` with a visible measurement date |
| T-17-19 | Information Disclosure | `admin` tier | mitigate | The internal admin tier is asserted absent from all public copy by an explicit grep gate |
| T-17-SC | Tampering | npm/pnpm installs | accept | Copy-only change; no dependency is added or modified by this plan |
</threat_model>

<verification>
- The marketing site builds
- No tier name outside `free`/`pro`/`api`/`agency` appears, and `admin` appears nowhere
- No competitor price appears that is not in COMPETITIVE-PARITY.md with a date
</verification>

<success_criteria>
The pricing half of ROADMAP success criterion 3 holds: every pricing, tier, and competitor
claim on `apps/marketing/` traces to PROJECT.md, `apps/web/lib/stripe.ts`, or
`.planning/COMPETITIVE-PARITY.md`, and no unbuilt feature is presented as purchasable.
</success_criteria>

<output>
Create `.planning/workstreams/milestone/phases/17-launch-blockers/17-06-SUMMARY.md` when done.
List every claim changed with its before value, its after value, and the source file the new
value came from.
</output>
