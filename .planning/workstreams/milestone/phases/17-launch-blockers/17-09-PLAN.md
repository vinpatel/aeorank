---
phase: 17-launch-blockers
plan: 09
type: execute
wave: 5
depends_on: ["17-04", "17-06", "17-07", "17-08"]
files_modified:
  - README.md
  - LAUNCH.md
  - .planning/PROJECT.md
  - scripts/check-claims.mjs
  - .github/workflows/ci.yml
autonomous: true
requirements: [SEC-03, SEC-04]

must_haves:
  truths:
    - "The five pillar weights in README match the values computed from @aeorank/core"
    - "No document states a test count that differs from .github/test-count.json"
    - "LAUNCH.md states something true and specific about the security posture instead of '0 Dependabot / CodeQL alerts'"
    - "Every plugin and package count is qualified by the surface it describes — repo versus npm"
    - "A CI check fails the build if any of these claims drifts again"
  artifacts:
    - path: "scripts/check-claims.mjs"
      provides: "Automated drift guard comparing published claims against measured sources"
      contains: "test-count.json"
    - path: "LAUNCH.md"
      provides: "Show HN draft with no false numeric claim"
    - path: "README.md"
      provides: "Pillar table, counts, and comparison rows traceable to measured sources"
  key_links:
    - from: "scripts/check-claims.mjs"
      to: ".github/test-count.json"
      via: "comparison of every published count against the generated artifact"
      pattern: "test-count"
    - from: ".github/workflows/ci.yml"
      to: "scripts/check-claims.mjs"
      via: "verify job step that fails the build on drift"
      pattern: "check-claims"
---

<objective>
Correct every remaining false or untraceable claim in `README.md`, `LAUNCH.md`, and
`.planning/PROJECT.md`, then make the corrections permanent with a CI drift guard.

Purpose: SEC-03 and the documentation half of SEC-04. This plan runs last among the copy
plans because two of its claims only become true once earlier waves land: the test count
comes from the artifact plan 17-03 generates, and the security posture line can only be
written honestly after plan 17-08 drives critical and high alerts to zero.
Output: three corrected documents, a drift-guard script, and a CI step that runs it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/COMPETITIVE-PARITY.md
@.planning/workstreams/milestone/phases/17-launch-blockers/17-CONTEXT.md
@.planning/workstreams/milestone/phases/17-launch-blockers/17-RESEARCH.md
@.planning/workstreams/milestone/phases/17-launch-blockers/17-04-SUMMARY.md
@.planning/workstreams/milestone/phases/17-launch-blockers/17-07-SUMMARY.md
@.planning/workstreams/milestone/phases/17-launch-blockers/17-08-SUMMARY.md

<interfaces>
<!-- Measured sources this plan must read from. Never type a number that is not in one of
     these. -->
.github/test-count.json                  -> { tests, files, measured }   (plan 17-03)
apps/marketing/src/data/pillars.json     -> [{ id, name, weightPct, dimensionCount }] (plan 17-07)
scripts/verify-deploys.sh                -> published npm package count + which are absent (plan 17-04)
packages/core/src/constants.ts           -> DEFAULT_CONFIG.maxPages (200), 36 dimensions, 5 pillars
.planning/COMPETITIVE-PARITY.md §1       -> competitor pricing measured 2026-08-15
gh api .../dependabot/alerts?state=open  -> live severity histogram
gh api .../code-scanning/alerts?state=open -> 4 open js/request-forgery findings

<!-- The specific false claims, with locations measured at research time -->
README.md:129-133  pillar weights 30/21/16/14/19          -> wrong, all five
README.md:56       "Crawls up to 50 pages"                 -> DEFAULT_CONFIG.maxPages is 200
README.md:74       Scrunch "$499+/mo", Semrush "$129+/mo"  -> Scrunch pricing is undisclosed;
                                                          Semrush is not one of the five
                                                          competitors measured in
                                                          COMPETITIVE-PARITY.md, so $129+/mo
                                                          has NO measured source either
README.md:78       "11 packages + 2 manual guides"         -> 10 plugins published to npm, 12 npm packages total
README.md:50,256   "$299/mo monitoring subscription"       -> no competitor is priced at $299
LAUNCH.md:45       "675 tests, 0 Dependabot / CodeQL alerts" -> both halves false
LAUNCH.md:161      "13 npm packages"                        -> 12 published
LAUNCH.md:169      "the ChatGPT conversion stat (15.9%)"    -> no citation anywhere in the repo
LAUNCH.md:214      "11 framework integrations. 675 tests. 0 vulnerabilities." -> false
PROJECT.md         "Deploy Status: DNS pending / credentials needed / Marketplace pending" -> stale, all live
PROJECT.md:37-38,43 "288 core tests / 77 CLI tests / 637 tests" -> superseded by the artifact
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct README.md against the measured sources</name>
  <files>README.md</files>
  <read_first>
    - README.md (read the whole file — the claim locations listed in `<interfaces>` are from research and line numbers will have shifted)
    - apps/marketing/src/data/pillars.json (the derived weights — the ONLY acceptable source for the pillar table)
    - .github/test-count.json
    - .planning/workstreams/milestone/phases/17-launch-blockers/17-04-SUMMARY.md (the measured npm published-package count)
    - packages/core/src/constants.ts (DEFAULT_CONFIG.maxPages)
    - .planning/COMPETITIVE-PARITY.md (section 1)
  </read_first>
  <action>
Correct each claim to its measured value. Run `node scripts/derive-pillars.mjs` and
`node scripts/measure-tests.mjs` first and read the outputs — do not copy numbers out of
this plan or out of RESEARCH.md.

1. **Pillar weight table** — replace all five percentages with the values in
   `apps/marketing/src/data/pillars.json`, in the pillar order the table already uses. Both
   the old and new sets sum to 100, which is why this went unnoticed; the drift guard in
   Task 3 makes that undetectable-by-eye failure detectable by CI.
2. **"Crawls up to 50 pages"** — `DEFAULT_CONFIG.maxPages` is 200; 50 is only the value in
   the `aeorank init` template at `packages/cli/src/commands/init.ts:16`. State the real
   default, or state both explicitly ("defaults to 200; the generated config template starts
   at 50").
3. **Comparison table competitor prices** (README.md:74 at research time) — the row reads
   `| **Price** | **Free / MIT** | $499+/mo | Enterprise | $129+/mo |` across AEOrank,
   Scrunch, Adobe LLM Optimizer, and Semrush AI. Two of those four numbers are unsourced:
   Scrunch does not publish pricing at all (undisclosed, sales-led per
   COMPETITIVE-PARITY.md §1), and **Semrush is not one of the five competitors
   COMPETITIVE-PARITY.md measures**, so `$129+/mo` has no measured source in this repo
   either — it is exactly as unsupported as the Scrunch figure and must get the same
   treatment, not a pass because it looks plausible. Remove every price that is not in
   `.planning/COMPETITIVE-PARITY.md`, and stamp whatever survives with "measured 2026-08-15".
   If that empties the row, say "undisclosed" or "see vendor" rather than inventing a
   replacement.
4. **"$299/mo monitoring subscription" / "$299 dashboard"** — no competitor is priced at
   $299. AthenaHQ was $295 in April and now has a free tier. Remove the number; make the
   point qualitatively.
5. **"11 packages + 2 manual guides"** — per D-10 the resolution is to reword, not to publish
   `@aeorank/wordpress`. Qualify every count by its surface: the repo contains 11 framework
   plugin packages; 10 of them are published to npm; the npm account carries 12 packages in
   total including `aeorank-cli` and `@aeorank/core`. Use the count that plan 17-04's script
   measured, and never state a plugin count without saying whether it describes the repo or
   npm.
6. **Test count** — the `<!-- TESTS_START -->` / `<!-- TESTS_END -->` block added by plan
   17-03 is the only place README may state a test count. Remove or rewrite any other
   sentence that names one so it points at the badge instead.
7. Leave the `<!-- STATS_START -->` block alone — `readme-social-proof.yml` owns it and
   rewrites it weekly by design.
  </action>
  <verify>
    <automated>node -e "const p=require('./apps/marketing/src/data/pillars.json'); const r=require('fs').readFileSync('README.md','utf8'); for(const x of p){ if(!r.includes(x.weightPct+'%')) throw new Error('README missing derived weight '+x.weightPct); }"</automated>
  </verify>
  <acceptance_criteria>
    - Every `weightPct` in `apps/marketing/src/data/pillars.json` appears in the README pillar table (the verify command asserts this)
    - `grep -c '| 30% \|| 21% \|| 16% \|| 14% \|| 19% ' README.md` returns 0
    - `grep -c '50 pages' README.md` returns 0, or every remaining occurrence is explicitly labelled as the init-template value
    - `grep -c '\$299' README.md` returns 0
    - `grep -c '\$499+/mo' README.md` returns 0
    - `grep -c '\$129' README.md` returns 0 — the Semrush figure has no measured source and must go with the Scrunch one
    - Every remaining `$<number>` competitor price in README.md appears verbatim in `.planning/COMPETITIVE-PARITY.md`: verify by listing them with `grep -oE '\$[0-9]+' README.md | sort -u` and checking each against that file; record the list in the summary even if it is empty
    - Every three-digit test count in README appears only inside the TESTS_START/TESTS_END block: `grep -vn 'TESTS_START\|TESTS_END' README.md | grep -Ec '\b[0-9]{3} tests\b'` returns 0
    - Every plugin or package count in README is adjacent to the words `repo` or `npm`, naming the surface it describes
  </acceptance_criteria>
  <done>Every numeric and comparative claim in README is either derived from a measured artifact or removed.</done>
</task>

<task type="auto">
  <name>Task 2: Correct LAUNCH.md and PROJECT.md, and commit LAUNCH.md for the first time</name>
  <files>LAUNCH.md, .planning/PROJECT.md</files>
  <read_first>
    - LAUNCH.md (currently untracked — plan 17-01 deliberately left it uncommitted because of the false claims; read the whole file)
    - .planning/PROJECT.md (the "Current State" and "Deploy Status" blocks)
    - .github/test-count.json
    - .planning/workstreams/milestone/phases/17-launch-blockers/17-08-SUMMARY.md (the final severity histogram and the residual medium/low alerts)
    - .planning/workstreams/milestone/phases/17-launch-blockers/17-04-SUMMARY.md (deploy verdicts, including whether the GitHub App Check Run was proven)
    - .planning/workstreams/milestone/phases/17-launch-blockers/17-CONTEXT.md (D-08 — the "0 alerts" line is rewritten, never achieved; never suppress or dismiss an alert to make copy true)
  </read_first>
  <action>
**LAUNCH.md.** This is the drafted Show HN body — the single document most likely to be
read adversarially. Correct it, then commit it (it has never been committed).

- The "675 tests, 0 Dependabot / CodeQL alerts" line: per D-08 this is REWRITTEN, not
  achieved. 4 CodeQL `js/request-forgery` findings are intrinsic to a tool whose job is
  fetching user-supplied URLs (`packages/core/src/scanner/fetcher.ts` plus three in
  `apps/web/lib/github-app.ts`), and SEC-01 only requires zero critical and zero high.
  Write something true and specific. The shape research proposed, with the numbers filled
  in from the artifact and the live API rather than from this plan:
  "<N> tests. Zero critical or high severity Dependabot alerts. Test count and alert status
  are emitted by CI — see the badges on the repo."
  Do not write "0 alerts" or "0 vulnerabilities" anywhere. Do NOT dismiss or suppress the
  CodeQL findings to make a copy line work — they stay open and the copy tells the truth
  about them.
- "11 framework integrations. 675 tests. 0 vulnerabilities." — same treatment, plus the
  D-10 surface qualification from Task 1.
- "13 npm packages" — use the count plan 17-04's script measured (12 at research time).
- The ChatGPT conversion statistic (15.9% vs Google organic) has no citation anywhere in the
  repo. Either cite the primary source inline with a link, or delete the sentence. Do not
  leave an uncited third-party statistic in a Show HN body — it is exactly the number a
  commenter will demand a source for.
- Any other test count, alert count, or package count in the file gets the same treatment.

**PROJECT.md.**
- The "Deploy Status" block says DNS pending, credentials needed, Marketplace publication
  pending. All three are stale — plan 17-04 verified the surfaces live. Rewrite it from that
  plan's verification record, including any item that came back `fail`.
- "288 core tests", "77 CLI tests", "637 tests" in the Current State block: replace with the
  value in `.github/test-count.json`, or better, stop citing a raw count in a hand-edited
  document and point at the CI badge. Prefer the latter — a number in a hand-edited file is
  how this drift started.
- Leave the canonical pricing line (Free / Pro $29 / API $99 / Agency $499) exactly as it
  is. It is the source of truth plan 17-06 reconciled the marketing site against.
  </action>
  <verify>
    <automated>test -n "$(git log --oneline -- LAUNCH.md)" && ! grep -qE '0 (Dependabot|vulnerabilities)|675 tests|637 tests' LAUNCH.md .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - `git log --oneline -- LAUNCH.md` returns at least one commit — the file is now tracked
    - `grep -Ec '0 Dependabot|0 vulnerabilities|0 CodeQL' LAUNCH.md` returns 0
    - `grep -Ec '\b(675|637|288|614) tests\b' LAUNCH.md .planning/PROJECT.md` returns 0
    - Any test count remaining in LAUNCH.md equals `jq -r '.tests' .github/test-count.json`
    - `grep -c '13 npm packages' LAUNCH.md` returns 0
    - The 15.9% statistic is either removed (`grep -c '15.9' LAUNCH.md` returns 0) or accompanied by a resolvable source URL on the same line
    - `grep -Eci 'pending|credentials needed' .planning/PROJECT.md` returns 0 within the Deploy Status block
    - `grep -c 'Agency \$499' .planning/PROJECT.md` returns at least 1 — canonical pricing untouched
    - No CodeQL alert was dismissed: `gh api "repos/vinpatel/aeorank/code-scanning/alerts?state=dismissed" --jq 'length'` returns the same value as before this plan ran
  </acceptance_criteria>
  <done>LAUNCH.md is tracked and contains no false or uncited number; PROJECT.md reflects the verified deploy state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build the claim-drift guard and wire it into CI</name>
  <files>scripts/check-claims.mjs, .github/workflows/ci.yml</files>
  <read_first>
    - scripts/measure-tests.mjs and scripts/derive-pillars.mjs (match their style and failure behaviour)
    - .github/workflows/ci.yml (the `verify` job created by plan 17-03)
    - README.md, LAUNCH.md as corrected by Tasks 1 and 2
    - apps/marketing/src/components/Anatomy.astro and Files.astro (already artifact-driven — the guard confirms they stay that way)
  </read_first>
  <behavior>
    - Passes on the corrected tree: `node scripts/check-claims.mjs` exits 0 after Tasks 1 and 2
    - Fails with a message naming the file and the offending value if any of these is true:
      - a three-digit "N tests" literal appears in README.md, LAUNCH.md, or .planning/PROJECT.md outside the TESTS_START/TESTS_END block, and N differs from .github/test-count.json
      - a pillar percentage in README.md's weight table differs from apps/marketing/src/data/pillars.json
      - a hardcoded test count literal appears anywhere under apps/marketing/src
      - the strings "0 Dependabot", "0 vulnerabilities", or "0 CodeQL alerts" appear in any tracked markdown file
      - a tier name appears in apps/marketing/src/components/Pricing.astro that is not a key of PLANS in apps/web/lib/stripe.ts, or the string "admin" appears there
      - a hardcoded competitor price (a `$<number>` literal) appears anywhere under apps/marketing/src/components/ or apps/marketing/src/pages/ that is not one of AEOrank's own canonical prices ($0, $29, $99, $499 from PROJECT.md) and is not present verbatim in .planning/COMPETITIVE-PARITY.md. This is the check that would have caught Footer.astro:24's "$299/mo dashboard", which sat in no plan's file list until it was found by hand
      - the literal string "$299" appears in any tracked file under README.md, LAUNCH.md, or apps/marketing/src
    - Exits non-zero and names the missing input if .github/test-count.json or apps/marketing/src/data/pillars.json is absent, rather than silently passing
  </behavior>
  <action>
Create `scripts/check-claims.mjs` (Node ESM, no new dependencies) implementing exactly the
behaviours above. Structure it as a list of named checks, each returning a list of
violations, so a failure message says which check failed, in which file, with what value and
what was expected.

For the competitor-price check, build the allowlist from two sources rather than hardcoding
it: AEOrank's own prices come from `.planning/PROJECT.md`'s canonical pricing line, and
competitor prices come from `.planning/COMPETITIVE-PARITY.md` §1. Anything else that matches
`\$[0-9]+` in a marketing component or page is a violation, reported with file, line, and the
offending value. Recursive scan — do not enumerate component filenames, because the whole
point is to catch the file nobody listed.

When scanning for test-count literals, read the file, strip the region between
`<!-- TESTS_START -->` and `<!-- TESTS_END -->` (that block is CI-generated and legitimately
contains the number), strip fenced code blocks and HTML comment lines, and only then match.
Do not write a bare `grep -c` gate against an unfiltered file: prose that describes the rule
would match the rule and make the check self-invalidating.

Verify the guard actually guards before wiring it up: temporarily edit a scratch copy of
README to state a wrong pillar weight and confirm the script exits non-zero naming that
file, then discard the scratch edit.

Then add a step to the `verify` job in `.github/workflows/ci.yml` running
`node scripts/check-claims.mjs`, placed after `pnpm build` (the pillar data derives from
built core output) and before `pnpm test`. A drifted claim now fails CI, which — once plan
17-10 makes `verify` a required check — makes re-drift unmergeable.
  </action>
  <verify>
    <automated>pnpm build && node scripts/derive-pillars.mjs && node scripts/measure-tests.mjs && node scripts/check-claims.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/check-claims.mjs` exits 0 on the corrected tree
    - Introducing a wrong pillar weight into README makes it exit non-zero with a message naming README.md and both the found and expected values (demonstrate once, then revert)
    - Introducing the string "0 vulnerabilities" into any tracked markdown makes it exit non-zero (demonstrate once, then revert)
    - Introducing a `$299` literal into any file under `apps/marketing/src/components/` makes it exit non-zero naming that file and the value (demonstrate once, then revert) — this is the regression guard for the Footer.astro class of drift
    - `grep -c 'COMPETITIVE-PARITY' scripts/check-claims.mjs` returns at least 1 — the competitor allowlist is read from the measured source, not hardcoded
    - Deleting `.github/test-count.json` makes it exit non-zero naming that file rather than passing (demonstrate once, then restore)
    - `grep -v '^\s*//' scripts/check-claims.mjs | grep -c 'TESTS_START'` returns at least 1 — the generated block is excluded from the literal scan
    - `grep -v '^\s*#' .github/workflows/ci.yml | grep -c 'check-claims'` returns 1
    - The CI run after pushing this change concludes `success` on the `verify` job
  </acceptance_criteria>
  <done>A single command proves every published claim still matches its measured source, and CI runs it on every pull request.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Published documentation -> hostile technical evaluator | README and LAUNCH.md are the launch-day attack surface for credibility |
| Measured artifacts -> documentation | Documents are hand-edited; only a CI check keeps them honest |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-27 | Repudiation | Numeric claims in README.md / LAUNCH.md / PROJECT.md | mitigate | Every claim rewritten from a measured artifact, and `scripts/check-claims.mjs` runs in the CI `verify` job so drift fails the build |
| T-17-27b | Repudiation | Unsourced competitor prices in marketing components | mitigate | `scripts/check-claims.mjs` recursively scans `apps/marketing/src/components/` and `apps/marketing/src/pages/` for `$<number>` literals and allows only AEOrank's canonical prices plus values present in `.planning/COMPETITIVE-PARITY.md`. Recursive by design: the `$299/mo` claim in `Footer.astro:24` was missed by a filename-enumerated sweep |
| T-17-28 | Spoofing | LAUNCH.md security posture claim | mitigate | Claim rewritten to state zero critical and high specifically, acknowledging the 4 open CodeQL findings; D-08 forbids dismissing them to make the copy true, and an acceptance check asserts the dismissed-alert count is unchanged |
| T-17-29 | Information Disclosure | 4 open CodeQL `js/request-forgery` alerts | accept | Intrinsic to a scanner that fetches user-supplied URLs; mitigated in code by `apps/web/lib/validate-url.ts::validateScanUrl` (12 tests). Left OPEN and described honestly rather than suppressed |
| T-17-30 | Repudiation | Uncited third-party statistic (15.9% ChatGPT conversion) | mitigate | Either given a resolvable primary source inline or deleted — no uncited third-party number ships in the launch copy |
| T-17-SC | Tampering | npm/pnpm installs | accept | Documentation and script changes only; no dependency added |
</threat_model>

<verification>
- `node scripts/check-claims.mjs` exits 0
- `git log -- LAUNCH.md` shows the file is tracked
- The dismissed-CodeQL-alert count is unchanged from before this plan
- The CI `verify` job passes with the new claim-check step
</verification>

<success_criteria>
ROADMAP success criterion 3 is satisfied — every numeric claim in `LAUNCH.md`, `README.md`,
and `apps/marketing/` traces to a command or API call that reproduces it — and criterion 4 is
completed: every doc and badge citing a test count reads from `.github/test-count.json`
rather than from a hand-edit, enforced by CI.
</success_criteria>

<output>
Create `.planning/workstreams/milestone/phases/17-launch-blockers/17-09-SUMMARY.md` when done.
List every claim changed with before value, after value, and the measured source, so the
phase verification can spot-check any row.
</output>
