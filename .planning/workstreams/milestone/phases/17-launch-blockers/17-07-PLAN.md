---
phase: 17-launch-blockers
plan: 07
type: execute
wave: 3
depends_on: ["17-03"]
files_modified:
  - scripts/derive-pillars.mjs
  - apps/marketing/src/data/pillars.json
  - apps/marketing/src/components/Anatomy.astro
  - apps/marketing/src/components/Files.astro
  - apps/marketing/src/components/Leaderboard.astro
  - apps/marketing/src/components/Install.astro
  - apps/marketing/src/components/LiveScan.astro
  - apps/marketing/src/pages/scoreboard.astro
  - .github/workflows/ci.yml
autonomous: true
requirements: [SEC-03, SEC-04]

must_haves:
  truths:
    - "The pillar weights shown on aeorank.dev are computed from PILLAR_GROUPS and DIMENSION_DEFS, not typed by hand"
    - "The test count shown on aeorank.dev comes from the CI-generated artifact, not a literal"
    - "No copy claims a scanning cadence that no scheduled job performs"
    - "Every block of sample scanner output on the site is visibly labelled as an example rather than presented as a literal terminal transcript"
    - "Regenerating the derived data produces no diff, proving the site and the code agree"
  artifacts:
    - path: "scripts/derive-pillars.mjs"
      provides: "Computes per-pillar weight percentages from @aeorank/core and writes the marketing data file"
      contains: "PILLAR_GROUPS"
    - path: "apps/marketing/src/data/pillars.json"
      provides: "Generated pillar weights consumed at build time by Anatomy.astro"
    - path: "apps/marketing/src/components/Anatomy.astro"
      provides: "Pillar diagram rendering the derived weights"
    - path: "apps/marketing/src/components/Install.astro"
      provides: "Terminal sample block labelled as illustrative example output"
    - path: "apps/marketing/src/components/LiveScan.astro"
      provides: "Scan demo footer labelled as illustrative example output"
  key_links:
    - from: "apps/marketing/src/components/Anatomy.astro"
      to: "apps/marketing/src/data/pillars.json"
      via: "build-time JSON import"
      pattern: "data/pillars"
    - from: "apps/marketing/src/components/Files.astro"
      to: "apps/marketing/src/data/test-count.json"
      via: "build-time JSON import"
      pattern: "data/test-count"
    - from: "scripts/derive-pillars.mjs"
      to: "packages/core"
      via: "import of PILLAR_GROUPS and DIMENSION_DEFS"
      pattern: "PILLAR_GROUPS"
---

<objective>
Make every measured number on the marketing site read from the code that produces it. The
published pillar weights (30/21/16/14/19) do not match `packages/core/src/constants.ts`
(26/25/12/25/12) — both sum to 100, which is why the error survived. The site also claims
288 tests when the measured count is 691, and claims a weekly scan of 100 funded startups
that no scheduled job performs.

Purpose: SEC-03 and the marketing half of SEC-04. Per D-09 the weights must be DERIVED from
`DIMENSION_DEFS` / `PILLAR_GROUPS`, never hand-copied — hand-copying is precisely what
produced the current error.
Output: a derivation script, generated data files, and three corrected components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/milestone/phases/17-launch-blockers/17-CONTEXT.md
@.planning/workstreams/milestone/phases/17-launch-blockers/17-RESEARCH.md
@.planning/workstreams/milestone/phases/17-launch-blockers/17-03-SUMMARY.md

<interfaces>
<!-- Source of truth for weights: packages/core/src/constants.ts -->
export const PILLAR_GROUPS: PillarGroup[] = [
  { id: "answer-readiness",      name: "Answer Readiness",      dimensionIds: [...7 ids] },
  { id: "content-structure",     name: "Content Structure",     dimensionIds: [...8 ids] },
  { id: "trust-authority",       name: "Trust & Authority",     dimensionIds: [...3 ids] },
  { id: "technical-foundation",  name: "Technical Foundation",  dimensionIds: [...] },
  { id: "ai-discovery",          name: "AI Discovery",          dimensionIds: [...] },
];
// Each entry of DIMENSION_DEFS carries `id` and `weightPct`.
// A pillar's weight = sum of weightPct over its dimensionIds. All 36 sum to exactly 100.
// Measured at research time: 26 / 25 / 12 / 25 / 12 in the PILLAR_GROUPS order above.
// The site currently shows 30 / 21 / 16 / 14 / 19 — all five values wrong.

<!-- Artifact created by plan 17-03, already committed and importable -->
apps/marketing/src/data/test-count.json  ->  { "tests": <number>, "files": <number>, "measured": "<YYYY-MM-DD>" }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Derive the pillar weights from core and rewire the anatomy diagram</name>
  <files>scripts/derive-pillars.mjs, apps/marketing/src/data/pillars.json, apps/marketing/src/components/Anatomy.astro, .github/workflows/ci.yml</files>
  <read_first>
    - packages/core/src/constants.ts (PILLAR_GROUPS and DIMENSION_DEFS — read both in full before computing anything)
    - apps/marketing/src/components/Anatomy.astro (the `pillars` array at lines 5-41 and the markup that consumes `p.weight`, `p.code`, `p.name`, `p.subs`, `p.line`)
    - scripts/measure-tests.mjs (created by plan 17-03 — match its style, artifact shape, and failure behaviour)
    - .github/workflows/ci.yml (the `publish-count` job created by plan 17-03)
    - .planning/workstreams/milestone/phases/17-launch-blockers/17-CONTEXT.md (D-09)
  </read_first>
  <action>
Create `scripts/derive-pillars.mjs` (Node ESM, no new dependencies). It must import
`PILLAR_GROUPS` and `DIMENSION_DEFS` from the built `packages/core` output, compute each
pillar's weight as the sum of `weightPct` over its `dimensionIds`, and write
`apps/marketing/src/data/pillars.json` as an array of
`{ id, name, weightPct, dimensionCount }` in `PILLAR_GROUPS` order.

It must fail loudly (non-zero exit) if the computed weights do not sum to exactly 100, or if
any `dimensionId` in a pillar has no matching entry in `DIMENSION_DEFS`. That assertion is
the guard that stops this class of drift recurring.

Do not type the numbers 26/25/12/25/12 into the script or the component. Compute them. The
values quoted in this plan and in RESEARCH.md are a research measurement, not the source —
if your computed output disagrees with them, the computed output wins and you record the
discrepancy in the summary.

**Commit `apps/marketing/src/data/pillars.json` as soon as it first generates, BEFORE running
the idempotency check.** `git diff --exit-code <path>` exits 0 for an UNTRACKED file, so on a
freshly generated artifact the idempotency assertion passes without testing anything. The
verify command therefore runs `git ls-files --error-unmatch` first, which fails if the file is
untracked. Order: generate -> `git add` + commit -> re-run the script -> `git diff --exit-code`.

Then rewire `apps/marketing/src/components/Anatomy.astro`:
- Import `../data/pillars.json` in the component frontmatter and build the local `pillars`
  array from it rather than from hardcoded `weight` values.
- Keep the existing presentational fields the markup needs (`code`, `subs`, `line`) as local
  data keyed by pillar `id`; only `weight` and `name` come from the generated file.
- The current array is ordered P1 answer-readiness, P2 content-structure, P3 ai-discovery,
  P4 trust-authority, P5 technical-foundation — an ordering chosen when the weights were
  wrong. Re-derive the display order from the real weights (descending) or keep
  `PILLAR_GROUPS` order, and make sure the `code` labels (P1..P5) stay consistent with
  whichever order renders.
- Do not restyle the component. 37signals aesthetic is a locked project constraint; this is
  a data-source change.

Finally, add `node scripts/derive-pillars.mjs` to the `publish-count` job in
`.github/workflows/ci.yml`, immediately before the count measurement step, and include
`apps/marketing/src/data/pillars.json` in that job's `git add` list. That makes the weights
regenerate on every push to main, so they cannot silently drift from core again.
  </action>
  <verify>
    <automated>pnpm build && node scripts/derive-pillars.mjs && node -e "const p=require('./apps/marketing/src/data/pillars.json'); const s=p.reduce((a,x)=>a+x.weightPct,0); if(s!==100) throw new Error('weights sum to '+s); if(p.length!==5) throw new Error('expected 5 pillars');" && git ls-files --error-unmatch apps/marketing/src/data/pillars.json && node scripts/derive-pillars.mjs && git diff --exit-code apps/marketing/src/data/pillars.json</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/derive-pillars.mjs` exits 0 and the generated weights sum to exactly 100 across exactly 5 pillars
    - `git ls-files --error-unmatch apps/marketing/src/data/pillars.json` exits 0 — the artifact is TRACKED before the idempotency check runs (on an untracked file `git diff --exit-code` exits 0 vacuously and proves nothing)
    - Re-running the script after that commit produces no git diff (`git diff --exit-code apps/marketing/src/data/pillars.json` exits 0) — proving the committed file is the derived file
    - No hand-typed pillar weight survives in the component: `grep -Ec 'weight:\s*(30|21|16|14|19)\b' apps/marketing/src/components/Anatomy.astro` returns 0. (Scoped to the `weight:` key rather than a bare word-boundary number scan, which would also match unrelated CSS values and is therefore not discriminating.)
    - `grep -c 'data/pillars' apps/marketing/src/components/Anatomy.astro` returns at least 1
    - The marketing build exits 0 and the rendered `dist` output contains the derived percentages: `grep -o '[0-9]\+%' apps/marketing/dist/index.html | sort -u` includes each derived weight
    - `grep -v '^\s*#' .github/workflows/ci.yml | grep -c 'derive-pillars'` returns at least 1
    - Deliberately corrupting a weight in a scratch copy of the input makes the script exit non-zero (sum-to-100 assertion works) — confirm once, do not commit the corruption
  </acceptance_criteria>
  <done>The site's pillar weights are computed from core, regenerate deterministically, and CI keeps them current.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the test count to the CI artifact, make the scoreboard cadence true, and label the sample output</name>
  <files>apps/marketing/src/components/Files.astro, apps/marketing/src/components/Leaderboard.astro, apps/marketing/src/pages/scoreboard.astro, apps/marketing/src/components/Install.astro, apps/marketing/src/components/LiveScan.astro</files>
  <read_first>
    - apps/marketing/src/components/Files.astro (line ~287 — the "288 tests pin output to byte-level stability" sentence)
    - apps/marketing/src/data/test-count.json (the artifact committed by plan 17-03)
    - apps/marketing/src/components/Leaderboard.astro (line ~32 — the "every week" claim)
    - apps/marketing/src/pages/scoreboard.astro (line ~192 and the hardcoded company array that backs it)
    - .github/workflows/demo-scan.yml (the ONLY scheduled scan: one site, sumhealth.org, daily)
    - apps/marketing/src/components/Install.astro lines 14-15 (the `code` template literal: "✔ 36 checks in 0.9s", "score: 31/100 · grade F", "failing: 9 · partial: 11 · passing: 16")
    - apps/marketing/src/components/LiveScan.astro line ~89 (the `scan__foot` block: "16 passing · 11 partial · 9 failing", "final: 31/100 · grade F")
  </read_first>
  <action>
**Files.astro** — "288 tests pin output to byte-level stability" is false; the measured
count is 691. Import `../data/test-count.json` in the component frontmatter and interpolate
the value. Do not type a number into the copy. The surrounding sentence stays; only the
source of the number changes.

**Leaderboard.astro line ~32 and scoreboard.astro line ~192** — both claim the public sites
of 100 funded startups are scanned "every week". No such job exists: the data is a hardcoded
array literal in `scoreboard.astro`, and the only scheduled scan in the repo is
`demo-scan.yml`, which scans one site daily. Per CONTEXT ("either make it true or make the
copy true") and because automating the board is DEF-03 in Phase 25, make the copy true:
- Add an exported `measuredOn` constant next to the hardcoded array in `scoreboard.astro`
  holding the date those scores were actually produced. If that date cannot be established
  from git history for the file, use the date this task runs and say so in the summary.
- Replace "every week" in both places with a statement of what actually happened — the board
  was scanned with `aeorank-cli` on that date — and render the date from the constant.
- Keep the derived figures (`totalCompanies`, `globalAvg`, the named examples) since they are
  computed from the same array and are internally consistent; only the cadence claim is
  false.
- Do not delete the scoreboard or its data.

**Install.astro lines ~14-15 and LiveScan.astro line ~89** — both present invented scanner
output as a literal terminal transcript: `✔ 36 checks in 0.9s`, `score: 31/100 · grade F`,
`failing: 9 · partial: 11 · passing: 16`, and the matching `16 passing · 11 partial ·
9 failing` / `final: 31/100 · grade F` in the LiveScan footer. Two of those numbers are true
of the product (36 criteria, and 9+11+16 = 36 is internally consistent); the score, the grade,
the timing, and the pass/partial/fail split are for an unnamed placeholder site and were never
measured. On a page whose whole argument is "deterministic, reproducible output", an
unlabelled transcript that nobody can reproduce is the weakest link a hostile reader will pull.

Do NOT delete these blocks and do NOT invent a real scan to back them — running a live scan to
source them is DEF-03/Phase 25 work. Label them instead: add a visible marker adjacent to each
block ("example output", "illustrative — your numbers will differ", or equivalent) rendered in
the existing label style the component already uses. The 36 remains, because 36 is measured
truth from `DIMENSION_DEFS`; the score, grade, and timing stay as they are but stop claiming to
be a record of anything. Do not restyle either component — 37signals aesthetic is a locked
project constraint and this is a labelling change.

Do not touch the pricing, competitor, or plugin-count copy in any file — plan 17-06 owns
those and runs in the same wave.
  </action>
  <verify>
    <automated>cd apps/marketing && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - The marketing build exits 0
    - `grep -c '288 tests' apps/marketing/src/components/Files.astro` returns 0
    - `grep -c 'data/test-count' apps/marketing/src/components/Files.astro` returns at least 1
    - The built page contains the artifact's value: `grep -c "$(jq -r '.tests' apps/marketing/src/data/test-count.json) tests" apps/marketing/dist/index.html` returns at least 1
    - `grep -c 'every week' apps/marketing/src/components/Leaderboard.astro` returns 0
    - `grep -c 'every week' apps/marketing/src/pages/scoreboard.astro` returns 0
    - `grep -Ec 'measuredOn|last scanned' apps/marketing/src/pages/scoreboard.astro` returns at least 1
    - No hardcoded three-digit test count remains anywhere under `apps/marketing/src`: `grep -rEl '\b(288|614|637|675|691) tests\b' apps/marketing/src | wc -l | tr -d ' '` returns `0`. (A bare `grep -rEc … <dir>` prints one `file:count` line per file — 17 lines on this tree today — and can never itself equal 0. `-l` lists only matching files, so an empty list is a genuine pass.)
    - `grep -Eci 'example output|illustrative|your numbers will differ' apps/marketing/src/components/Install.astro` returns at least 1
    - `grep -Eci 'example output|illustrative|your numbers will differ' apps/marketing/src/components/LiveScan.astro` returns at least 1
    - Both sample blocks still exist — `grep -c '31/100' apps/marketing/src/components/Install.astro` and `grep -c '31/100' apps/marketing/src/components/LiveScan.astro` each return at least 1; the fix is labelling, not deletion
    - `grep -c '36 checks' apps/marketing/src/components/Install.astro` returns at least 1 — 36 is measured truth from `DIMENSION_DEFS` and must survive
  </acceptance_criteria>
  <done>The site's test count comes from the CI artifact and no copy claims a cadence the repo does not run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `@aeorank/core` scoring constants -> public marketing claims | The weights published to prospects must be the weights the scorer applies |
| CI-generated artifacts -> site build | Generated JSON is imported at build time; a stale or missing artifact silently publishes a wrong number |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-20 | Repudiation | Published pillar weights | mitigate | Weights are computed by `scripts/derive-pillars.mjs` from `PILLAR_GROUPS` × `weightPct`, asserted to sum to 100, and regenerated by CI on every push to main |
| T-17-21 | Repudiation | Published test count | mitigate | `Files.astro` imports the CI-generated artifact; a grep gate asserts no literal test count survives under `apps/marketing/src` |
| T-17-22 | Spoofing | Scoreboard cadence claim | mitigate | Cadence claim replaced by a rendered measurement date backed by a constant sitting beside the data it describes |
| T-17-22b | Spoofing | Illustrative scanner output in `Install.astro` and `LiveScan.astro` | mitigate | Invented transcript numbers (score 31/100, grade F, 0.9s, 9/11/16 split) are labelled as example output rather than presented as a literal terminal record; the one measured number in the block, 36 criteria, is retained and is derivable from `DIMENSION_DEFS` |
| T-17-SC | Tampering | npm/pnpm installs | accept | No dependency added; the derivation script imports the already-built local `@aeorank/core` |
</threat_model>

<verification>
- `node scripts/derive-pillars.mjs` is idempotent and produces no diff
- The marketing build exits 0 and renders the derived weights and the artifact test count
- No hardcoded weight or test count literal survives under `apps/marketing/src`
- Every sample-output block on the site carries a visible "example output" label
</verification>

<success_criteria>
The measured-number half of ROADMAP success criterion 3 holds for `apps/marketing/`: every
weight, test count, and cadence claim reproduces from a command — `node
scripts/derive-pillars.mjs`, `node scripts/measure-tests.mjs`, or the data file beside the
copy that cites it.
</success_criteria>

<output>
Create `.planning/workstreams/milestone/phases/17-launch-blockers/17-07-SUMMARY.md` when done.
Record the computed pillar weights verbatim and note any disagreement with the 26/25/12/25/12
research measurement.
</output>
