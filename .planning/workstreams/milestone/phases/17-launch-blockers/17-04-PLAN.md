---
phase: 17-launch-blockers
plan: 04
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - scripts/verify-deploys.sh
  - .planning/workstreams/milestone/phases/17-launch-blockers/17-SEC-06-VERIFICATION.md
autonomous: false
requirements: [SEC-06]
user_setup:
  - service: clerk
    why: "Dashboard end-to-end verification requires an authenticated session; a 200 on / only proves the Next.js app boots."
    dashboard_config:
      - task: "Sign in, add a site, run a scan, confirm a score renders and the ZIP downloads"
        location: "https://app.aeorank.dev"
  - service: stripe
    why: "Checkout completion requires a real or test card and a logged-in session."
    dashboard_config:
      - task: "Run one test-mode checkout on the Pro tier and confirm the plan applies"
        location: "https://app.aeorank.dev billing flow + Stripe Dashboard"
  - service: github
    why: "The GitHub App Check Run claim is prominent in README and marketing and is currently unproven."
    dashboard_config:
      - task: "Install the AEOrank GitHub App on a scratch repo, open a PR, confirm the Check Run appears"
        location: "https://github.com/apps/aeorank"
      - task: "Visually inspect the Marketplace listing — screenshots, description, categories"
        location: "https://github.com/marketplace/actions/aeorank-aeo-scanner"
  - service: vercel
    why: "Cron execution results are only visible in project logs."
    dashboard_config:
      - task: "Confirm the keep-alive cron shows a 200 after its next scheduled run"
        location: "Vercel Dashboard -> aeorank -> Logs / Cron Jobs"

must_haves:
  truths:
    - "aeorank.dev, docs.aeorank.dev, and app.aeorank.dev each return HTTP 200, verified by a re-runnable script"
    - "The GitHub Action resolves on the Marketplace and vinpatel/aeorank-action carries a v1 tag"
    - "The npm publication status of every workspace package is measured, not assumed"
    - "Every surface that cannot be checked without credentials is enumerated with a human verdict recorded against it"
  artifacts:
    - path: "scripts/verify-deploys.sh"
      provides: "Re-runnable liveness check for all agent-verifiable public surfaces"
      contains: "app.aeorank.dev"
    - path: ".planning/workstreams/milestone/phases/17-launch-blockers/17-SEC-06-VERIFICATION.md"
      provides: "Recorded verdicts for both agent-checkable and credential-gated surfaces"
  key_links:
    - from: "scripts/verify-deploys.sh"
      to: "npm registry"
      via: "npm view per workspace package, establishing the published-plugin count"
      pattern: "npm view"
---

<objective>
Verify — not rebuild — that the deploy surfaces are live, and convert everything that is
genuinely credential-gated into an explicit user checklist with recorded verdicts.

Purpose: SEC-06. Per D-11 this requirement is mostly already satisfied: all three domains,
the Marketplace listing, and the GitHub App page returned 200 at research time, and
`aeorank-action` has its `v1` tag. What is missing is (a) a re-runnable check rather than a
one-off curl, and (b) honest verdicts on the authenticated surfaces. Marking SEC-06 done on
the strength of three HTTP 200s would be exactly the kind of unverified claim this phase
exists to eliminate.
Output: a liveness script plus a verification record with a verdict against every line.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/milestone/phases/17-launch-blockers/17-CONTEXT.md
@.planning/workstreams/milestone/phases/17-launch-blockers/17-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write and run the public-surface liveness check</name>
  <files>scripts/verify-deploys.sh</files>
  <read_first>
    - .planning/workstreams/milestone/phases/17-launch-blockers/17-RESEARCH.md (section "SEC-06: What can and cannot be verified by an agent")
    - .planning/PROJECT.md (the "Deploy Status" block, which is stale — all three targets are live)
    - packages/wordpress/package.json (the one workspace package not published to npm)
  </read_first>
  <action>
Create `scripts/verify-deploys.sh` (bash, `set -euo pipefail`, executable) that checks every
surface an agent can actually reach and exits non-zero if any check fails. It must print one
line per check in the form `<status>  <target>` so the output is pasteable into the
verification record.

Checks, exactly these:
1. HTTP 200 from each of `https://aeorank.dev`, `https://docs.aeorank.dev`,
   `https://app.aeorank.dev` using
   `curl -sf -o /dev/null -w "%{http_code}"` with `-L` to follow redirects.
2. HTTP 200 from `https://github.com/marketplace/actions/aeorank-aeo-scanner` and
   `https://github.com/apps/aeorank`.
3. `gh api repos/vinpatel/aeorank-action/git/matching-refs/tags` includes both `v1` and
   `v1.0.0`.
4. npm publication status for every workspace package name: `aeorank-cli`, `@aeorank/core`,
   and the plugin packages. Use `npm view <name> version` and record published vs absent.
   Research measured 12 published packages with `@aeorank/wordpress` absent (404). The
   script must print the published count so the "framework plugins" claim in plans 17-06
   and 17-09 has a measured source rather than a remembered one.
Do NOT use GNU `timeout` — it is unavailable on macOS in this environment.

Do not attempt to hit `/api/cron/keep-alive`; it requires the server-side `CRON_SECRET`
bearer token and belongs to the human checklist.

Run the script and capture its full output verbatim for Task 2.

Decision recorded here for downstream plans (D-10, Claude's discretion): the resolution is
to REWORD the claim, not to publish `@aeorank/wordpress`. Publishing a package is feature
work outside a launch-blocker phase, and CONTEXT lists it as a deferred idea. Downstream
copy must therefore describe plugins by the surface it names — "11 in the repo, 10 published
to npm" — and the script's measured published count is the source for that number.
  </action>
  <verify>
    <automated>bash scripts/verify-deploys.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/verify-deploys.sh` exits 0
    - Its output contains a `200` line for each of aeorank.dev, docs.aeorank.dev, app.aeorank.dev, the Marketplace listing, and the GitHub App page
    - Its output confirms both `v1` and `v1.0.0` refs exist on `vinpatel/aeorank-action`
    - Its output prints an explicit published-package count and names every workspace package that is NOT published
    - `grep -c 'timeout ' scripts/verify-deploys.sh` returns 0
    - `test -x scripts/verify-deploys.sh` succeeds
  </acceptance_criteria>
  <done>A single command re-verifies every agent-checkable SEC-06 surface and reports the measured npm publication count.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Record human verdicts for the credential-gated surfaces</name>
  <files>.planning/workstreams/milestone/phases/17-launch-blockers/17-SEC-06-VERIFICATION.md</files>
  <read_first>
    - .planning/workstreams/milestone/phases/17-launch-blockers/17-RESEARCH.md (table "CANNOT be verified by a coding agent — hand to the user")
    - scripts/verify-deploys.sh output from Task 1
  </read_first>
  <what-built>
Task 1 produced a re-runnable script proving the five public surfaces return 200, the
`aeorank-action` v1 tag exists, and exactly which workspace packages are published to npm.
Everything below requires credentials no agent holds — a Clerk session, a Stripe card, a
GitHub App installation, or the Vercel log view.
  </what-built>
  <how-to-verify>
Work through each item and reply with a verdict (pass / fail / not-applicable) plus one line
of evidence for each:

1. **Dashboard end to end** — sign in at https://app.aeorank.dev, add a site, run a scan.
   Confirm a score renders and the generated-files ZIP downloads.
2. **Credentials valid in production** — the scan in step 1 completing is the proof that
   Clerk, Supabase, and the scan pipeline credentials all work in production.
3. **Stripe checkout** — run one test-mode checkout on the Pro tier and confirm the plan
   applies to the account afterwards.
4. **GitHub App Check Run** — install https://github.com/apps/aeorank on a scratch repo,
   open a PR, and confirm the AEOrank Check Run appears. This backs a prominent README and
   marketing claim and is currently unproven; if it fails, say so — plan 17-09 will have to
   soften the claim rather than leave it false.
5. **Marketplace listing quality** — open
   https://github.com/marketplace/actions/aeorank-aeo-scanner and confirm the screenshots,
   description, and categories render correctly. A 200 is not a well-formed listing.
6. **Keep-alive cron** — in the Vercel dashboard, confirm `/api/cron/keep-alive` shows a 200
   after its next scheduled run (daily at `0 5 * * *`, with Vercel's ±59 min jitter).
7. **Supabase project status** — confirm the project is Active and has not auto-paused.
  </how-to-verify>
  <acceptance_criteria>
    - `.planning/workstreams/milestone/phases/17-launch-blockers/17-SEC-06-VERIFICATION.md` exists and contains a table with one row per surface, each row carrying: target, method (script / human), verdict, and date
    - Every agent-checkable row records the exact status code or ref from the Task 1 script output
    - All seven human items above have a recorded verdict — no row is left blank or "assumed"
    - Any item with a `fail` verdict is written into the record with the failure text, and is called out in the summary as input for plan 17-09's claim wording
    - `grep -c 'assumed\|probably\|should be' .planning/workstreams/milestone/phases/17-launch-blockers/17-SEC-06-VERIFICATION.md` returns 0
  </acceptance_criteria>
  <action>Write `.planning/workstreams/milestone/phases/17-launch-blockers/17-SEC-06-VERIFICATION.md`
containing one table row per surface with columns target, method (script / human), verdict,
and date. Pre-fill every agent-checkable row from the Task 1 script output. Leave the seven
human rows with an explicit `pending` verdict, present the checklist to the user, and fill in
their replies verbatim. Never write `assumed` in any row.</action>
  <verify>
    <human-check>All seven credential-gated items have a recorded pass/fail verdict in 17-SEC-06-VERIFICATION.md</human-check>
  </verify>
  <done>Every surface in the verification record carries a verdict, with agent-checkable rows citing the script output and human rows citing the user's reply.</done>
  <resume-signal>Reply with a verdict line for each of the seven items, or "all pass".</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public internet -> aeorank.dev / app.aeorank.dev | Anonymous requests reach the deployed surfaces |
| GitHub App installation -> customer repositories | The App acts inside third-party repos when installed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-11 | Repudiation | SEC-06 completion claim | mitigate | Verdicts are recorded per-surface with method and date in `17-SEC-06-VERIFICATION.md`; the agent-checkable subset is re-runnable via `scripts/verify-deploys.sh` |
| T-17-12 | Information Disclosure | Credential-gated checks | mitigate | The checkpoint asks the user to confirm outcomes only; no secret value is ever requested, pasted, or recorded |
| T-17-13 | Spoofing | GitHub App Check Run claim | mitigate | Claim is proven by an actual installation on a scratch repo, or the claim is softened in plan 17-09 — it is not asserted on the strength of the App page returning 200 |
| T-17-SC | Tampering | npm/pnpm installs | accept | This plan installs nothing; `npm view` is a read-only registry query |
</threat_model>

<verification>
- `bash scripts/verify-deploys.sh` exits 0
- `17-SEC-06-VERIFICATION.md` has a verdict on every row, agent-checkable and human alike
</verification>

<success_criteria>
ROADMAP success criterion 6 is satisfied and provable on demand: aeorank.dev,
docs.aeorank.dev, and app.aeorank.dev return 200 and the GitHub Action resolves on the
Marketplace — with the authenticated surfaces verified by a human rather than assumed.
</success_criteria>

<output>
Create `.planning/workstreams/milestone/phases/17-launch-blockers/17-04-SUMMARY.md` when done.
Record the measured published-package count and any human verdict that came back `fail`,
because plans 17-06 and 17-09 must word their claims to match.
</output>
