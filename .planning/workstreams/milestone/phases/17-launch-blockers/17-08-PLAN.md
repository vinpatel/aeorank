---
phase: 17-launch-blockers
plan: 08
type: execute
wave: 4
depends_on: ["17-05", "17-06", "17-07"]
files_modified:
  - apps/marketing/package.json
  - apps/docs/package.json
  - packages/astro/package.json
  - apps/marketing/astro.config.mjs
  - apps/docs/astro.config.mjs
  - apps/docs/src/content.config.ts
  - apps/marketing/.astro/types.d.ts
  - apps/docs/.astro/content-modules.mjs
  - package.json
  - .github/workflows/deploy-marketing.yml
  - .github/workflows/deploy-docs.yml
  - pnpm-lock.yaml
autonomous: false
requirements: [SEC-01]

must_haves:
  truths:
    - "astro resolves at 7.2.2 across apps/marketing, apps/docs, and packages/astro"
    - "@astrojs/starlight and every Astro integration resolve within their declared peer range for astro 7"
    - "Both Astro sites build, and the docs site still renders all 36 criteria pages"
    - "Zero open critical and zero open high Dependabot alerts remain after this commit"
    - "The migration is a single revertible commit containing no copy or CI change"
  artifacts:
    - path: "apps/marketing/package.json"
      provides: "astro 7.2.2 and integration versions compatible with it"
      contains: "astro"
    - path: "packages/astro/package.json"
      provides: "Plugin devDependency and widened peerDependency range so consumers on Astro 6/7 do not hit peer errors"
      contains: "peerDependencies"
  key_links:
    - from: "packages/astro/src/index.ts"
      to: "astro"
      via: "import type { AstroIntegration } and the astro:build:done hook (destructures { dir } only)"
      pattern: "astro:build:done"
    - from: "apps/docs/src/content.config.ts"
      to: "@astrojs/starlight"
      via: "Content Layer collection definition"
      pattern: "defineCollection|docsLoader"
---

<objective>
SEC-01 Lever B' (D-05 mechanism 3 of 3, governed by D-03): migrate Astro from 5.18.1 to
**7.2.2**. `astro@5.18.1` carries 8 high-severity alerts with no 5.x patch available, so
this upgrade is unavoidable for SEC-01, and after plans 17-02 and 17-05 it is the only thing
standing between the repo and zero critical/high.

Purpose: this is the highest-risk change in the phase — two major versions across two
deployed sites and a published plugin. D-03 requires it to be its own separately-gated,
revertible commit precisely so a broken peer matrix can be rolled back without losing the
other two levers.
Output: one commit moving the Astro tree to 7.2.2, human-verified visually, with the final
alert count measured.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/milestone/phases/17-launch-blockers/17-CONTEXT.md
@.planning/workstreams/milestone/phases/17-launch-blockers/17-RESEARCH.md
@.planning/workstreams/milestone/phases/17-launch-blockers/17-05-SUMMARY.md

<interfaces>
<!-- Compatibility matrix verified against the npm registry 2026-08-15. RE-VERIFY at
     execution time — these versions move. -->
| Astro          | Requires vite | Compatible Starlight |
| 5.18.1 (from)  | ^6.4.1        | 0.36.0 (astro ^5.5.0) |
| 6.4.8          | ^7.3.2        | 0.40.0 (astro ^6.4.5) |
| 7.2.2 (TARGET) | ^8.0.13       | 0.41.7 (astro ^7.0.2) |

Repo currently declares @astrojs/starlight ^0.34.0 in apps/docs.
Root pnpm.overrides currently pins vite ^7.3.5 (raised by plan 17-05) — this CROSSES a major
against astro 7's ^8.0.13 and must be raised to the vite 8 safe floor (>=8.0.16) or removed.

Astro 6 breaking changes that touch this repo (all still apply when jumping to 7):
- Node >= 22.12.0 required
- vite major bump required
- legacy content collections removed — NO IMPACT, apps/docs already uses src/content.config.ts
- .cjs/.cts config unsupported — NO IMPACT, both apps use astro.config.mjs
- `routes` removed from the astro:build:done hook — packages/astro destructures only { dir }
- <ViewTransitions /> renamed to <ClientRouter /> — grep, not exhaustively verified
- Zod 4 in content schemas — only apps/docs/src/content.config.ts
- Image cropping/upscaling defaults changed — apps/docs depends on sharp; visual regression possible
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight — verify the 7.2.2 peer matrix and grep for removed APIs</name>
  <files>(no file edits — verification only)</files>
  <read_first>
    - .planning/workstreams/milestone/phases/17-launch-blockers/17-RESEARCH.md (section "The Astro Upgrade — the only genuinely risky change", including the full breaking-change table)
    - .planning/workstreams/milestone/phases/17-launch-blockers/17-CONTEXT.md (D-03 — 7.2.2 is locked; verify Starlight and every integration peer range against 7.2.2 BEFORE starting)
    - apps/marketing/package.json and apps/docs/package.json (every @astrojs/* dependency declared)
    - packages/astro/package.json (both the devDependency and the peerDependencies block)
    - packages/astro/src/index.ts (the astro:build:done hook usage)
    - apps/docs/src/content.config.ts
  </read_first>
  <action>
Do not change any file in this task. Establish the facts first, because D-03 makes this the
one plan that must not be discovered to be wrong halfway through.

1. Confirm the target exists and read its constraints:
   `npm view astro@7.2.2 version engines peerDependencies` and
   `npm view astro@7.2.2 dependencies.vite`.
2. For EVERY `@astrojs/*` package declared in `apps/marketing/package.json`,
   `apps/docs/package.json`, and `packages/astro/package.json`, resolve the newest version
   whose `peerDependencies.astro` range admits 7.2.2:
   `npm view <pkg> versions --json` then `npm view <pkg>@<candidate> peerDependencies`.
   Research expects `@astrojs/starlight@0.41.7` (peer `astro ^7.0.2`); verify rather than
   assume, and do the same for `@astrojs/preact`, `@astrojs/sitemap`, and anything else
   declared. Record the resolved version for each in a table.
3. Grep both Astro apps for APIs removed in 6 or 7:
   `grep -rn 'ViewTransitions\|Astro.glob\|astro:build:done\|getEntryBySlug\|legacy.collections' apps/marketing/src apps/docs/src apps/marketing/astro.config.mjs apps/docs/astro.config.mjs packages/astro/src`
   Record every hit with file:line and the replacement it needs (`<ViewTransitions />` ->
   `<ClientRouter />`, `Astro.glob()` -> `import.meta.glob()`, `astro:build:done({ routes })`
   -> `astro:routes:resolved`).
4. Establish the Node floor: `npm view astro@7.2.2 engines.node`. Root `engines.node` is
   currently `">=20"` and PROJECT.md states the CLI runtime is Node 20+, which is a published
   contract for `aeorank-cli`. Resolution (Claude's discretion within D-03): raise
   `engines.node` ONLY in `apps/marketing`, `apps/docs`, and `packages/astro` to whatever
   astro 7.2.2 requires; leave the root and the CLI at `>=20` so the published CLI contract
   does not change. Confirm `.github/workflows/deploy-marketing.yml` and
   `deploy-docs.yml` request a Node version that satisfies the new floor; if they request a
   floating `22`, verify the resolved runner version satisfies it and pin it explicitly if
   it does not.
5. If any integration has NO version compatible with astro 7.2.2, stop and report before
   changing anything — that is the one condition under which the locked 7.2.2 target has to
   go back to the user rather than be silently downgraded to 6.4.8.
  </action>
  <verify>
    <automated>npm view astro@7.2.2 version && npm view @astrojs/starlight@0.41.7 peerDependencies</automated>
  </verify>
  <acceptance_criteria>
    - A resolved-version table exists in the working notes naming every `@astrojs/*` package, the chosen version, and its `peerDependencies.astro` range, with every range admitting 7.2.2
    - `npm view astro@7.2.2 version` prints `7.2.2`
    - The removed-API grep output is recorded in full, including a zero-hit result if that is what it returns
    - The astro 7.2.2 `engines.node` value is recorded verbatim
    - Either every integration has a 7-compatible version, or the incompatible one is named explicitly and execution stops
  </acceptance_criteria>
  <done>Every version, peer range, removed-API hit, and Node floor is measured and written down before a single file changes.</done>
</task>

<task type="auto">
  <name>Task 2: Perform the Astro 7.2.2 migration as one isolated commit and measure the final alert count</name>
  <files>apps/marketing/package.json, apps/docs/package.json, packages/astro/package.json, apps/marketing/astro.config.mjs, apps/docs/astro.config.mjs, apps/docs/src/content.config.ts, apps/marketing/.astro/types.d.ts, apps/docs/.astro/content-modules.mjs, package.json, .github/workflows/deploy-marketing.yml, .github/workflows/deploy-docs.yml, pnpm-lock.yaml</files>
  <read_first>
    - The Task 1 resolved-version table and removed-API grep output
    - package.json (the `vite` override raised to ^7.3.5 by plan 17-05 — it now crosses a major against astro 7)
    - .planning/workstreams/milestone/phases/17-launch-blockers/17-RESEARCH.md (section "Pitfall 2: pnpm.overrides that cross a major boundary" and "Pitfall 7: Astro .astro/ generated files churning the diff")
  </read_first>
  <action>
Move the whole coupled set together — Astro alone will not resolve:
- `astro` -> `^7.2.2` in `apps/marketing`, `apps/docs`, and `packages/astro` (devDependency)
- every `@astrojs/*` package to the version resolved in Task 1 (expect
  `@astrojs/starlight` `^0.41.7`, plus `@astrojs/preact` and `@astrojs/sitemap`)
- `packages/astro` `peerDependencies.astro` widened so downstream consumers on Astro 6 or 7
  do not hit peer errors. Widening the peer range is a published-contract change; state the
  new range explicitly in the commit body.
- root `pnpm.overrides.vite`: astro 7.2.2 depends on vite ^8.0.13 while the override
  currently forces ^7.3.5. Either raise it to the vite 8 safe floor (`>=8.0.16`, the value
  research recorded for the 8.x line) or remove the entry entirely and let astro's own
  declared range resolve. Do NOT leave an override that crosses a major — that is the exact
  defect this phase is fixing, and it installs cleanly while failing subtly at build time.
- `engines.node` raised in `apps/marketing`, `apps/docs`, `packages/astro` only, per the
  Task 1 decision. Root and CLI stay at `>=20`.
- Apply every removed-API replacement the Task 1 grep found. If it found none, say so in the
  summary rather than silently skipping.

Then run the gate, in this order, and do not proceed past a failure:
`pnpm install && pnpm build && pnpm typecheck && pnpm test`
followed by an explicit per-site build: build `apps/docs` and `apps/marketing` and confirm
both emit `dist/`.

The `.astro/` generated files (`apps/marketing/.astro/types.d.ts`,
`apps/docs/.astro/content-modules.mjs`) regenerate wholesale on this bump. Commit the
regenerated versions IN THIS SAME COMMIT so the churn is attributable to the migration and
does not pollute later diffs.

Commit as
`chore(deps): migrate astro 5.18.1 -> 7.2.2 (SEC-01 lever B')`
with a body recording: the resolved version of every `@astrojs/*` package, the new
`packages/astro` peer range, what happened to the `vite` override, and the before/after
alert numbers. This commit must contain no copy change, no CI logic change beyond a Node
version pin, and no dependency change unrelated to the Astro tree — D-03 requires it to be
revertible alone.

Push, then measure: `pnpm audit --json | jq '.metadata.vulnerabilities'` immediately, and
poll `gh api "repos/vinpatel/aeorank/dependabot/alerts?state=open&per_page=100" --paginate
-q '.[].security_advisory.severity' | sort | uniq -c` until Dependabot re-scans. This is the
measurement that closes SEC-01: the target is 0 critical and 0 high. Residual medium and low
alerts are explicitly acceptable (SEC-01 requires crit+high only) and must NOT be chased by
dismissing or suppressing anything (D-08).
  </action>
  <verify>
    <automated>pnpm install && pnpm build && pnpm typecheck && pnpm test && pnpm audit --audit-level=high</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm install`, `pnpm build`, `pnpm typecheck`, `pnpm test` each exit 0
    - `pnpm audit --audit-level=high` exits 0
    - `pnpm why astro -r` shows 7.2.2 and no remaining 5.x resolution
    - `gh api "repos/vinpatel/aeorank/dependabot/alerts?state=open&per_page=100" --paginate -q '.[].security_advisory.severity' | sort | uniq -c` shows zero `critical` and zero `high` lines after Dependabot re-scan
    - `apps/marketing/dist/index.html` and the docs `dist` output both exist and are non-empty after a clean build
    - The docs site still emits a page for every criterion: the built docs page count is greater than or equal to the count before the migration (record both numbers)
    - `node -e "const o=require('./package.json').pnpm.overrides; if(o.vite && !o.vite.includes('8')) throw new Error('vite override still crosses a major')"` exits 0
    - `git show --stat HEAD` contains no `.md` file and no `apps/marketing/src/components` file — the commit is dependency-and-config only
    - The CI `audit` job on this push concludes `success`
  </acceptance_criteria>
  <done>Astro is at 7.2.2 across all three consumers, the whole gate is green, and the Dependabot query returns zero critical and zero high.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visually verify both Astro sites after the two-major jump</name>
  <files>(no file edits — local preview of the built sites)</files>
  <read_first>
    - The Task 2 commit body (resolved versions and any removed-API replacements applied)
    - .planning/workstreams/milestone/phases/17-launch-blockers/17-RESEARCH.md (the Astro breaking-change table — image cropping/upscaling defaults changed, which is the likeliest visual regression)
  </read_first>
  <what-built>
Astro moved from 5.18.1 to 7.2.2 — two major versions — across `apps/marketing`,
`apps/docs`, and the published `packages/astro` plugin, together with Starlight and every
Astro integration. Builds, typecheck, and the full test suite are green, and
`pnpm audit --audit-level=high` now exits 0. What automation cannot check is whether the
pages still LOOK right: Astro changed image cropping and upscaling defaults, and `apps/docs`
renders logos through `sharp`.
  </what-built>
  <how-to-verify>
1. Run `pnpm --filter <marketing-package-name> preview` (or `pnpm dev`) and open the local
   marketing site. Check: the hero, the `/100` anatomy diagram with its pillar percentages,
   the pricing cards, and the scoreboard page all render with no layout breakage and no
   missing images.
2. Run the docs site locally and check: the Starlight sidebar renders, search works, a
   criterion detail page renders its content, and any logo or diagram image is not cropped
   or upscaled oddly.
3. If either deployed site has already updated (deploy-marketing.yml / deploy-docs.yml run
   on push to main), compare https://aeorank.dev and https://docs.aeorank.dev against what
   you saw locally.
4. Report any visual regression with the page and the element. A regression is a reason to
   fix forward or revert THIS commit alone — it is not a reason to touch the Lever A or
   Lever B commits.
  </how-to-verify>
  <acceptance_criteria>
    - User confirms the marketing hero, anatomy diagram, pricing cards, and scoreboard render correctly
    - User confirms the docs sidebar, search, and at least one criterion page render correctly
    - User confirms no image is visibly cropped, stretched, or missing on either site
    - Any reported regression is recorded in the summary with page and element named
  </acceptance_criteria>
  <action>Build both sites locally, start the preview servers, and give the user the exact local URLs
plus the checklist above. Do not judge visual correctness yourself — a passing build proves
nothing about cropping, layout, or missing images. Record any reported regression with page
and element in the summary, and fix forward or revert THIS commit alone; never touch the
Lever A or Lever B commits in response.</action>
  <verify>
    <human-check>User confirms marketing hero, anatomy diagram, pricing cards, scoreboard, docs sidebar, docs search, and a criterion page all render correctly with no image regressions</human-check>
  </verify>
  <done>User has visually approved both Astro sites after the two-major migration, or the regressions are recorded and addressed.</done>
  <resume-signal>Type "approved" or describe the visual regressions you found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> Astro dependency tree | A two-major jump replaces a large transitive subtree in one commit |
| `packages/astro` peerDependencies -> downstream consumer projects | Widening the peer range changes a published contract |
| Built static sites -> public internet | aeorank.dev and docs.aeorank.dev deploy automatically on push to main |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-23 | Tampering | `astro@5.18.1` (8 high-severity advisories, no 5.x patch) | mitigate | Upgrade the parent to 7.2.2 rather than forcing transitive overrides across a major; verified by `pnpm audit --audit-level=high` exiting 0 |
| T-17-24 | Denial of Service | aeorank.dev / docs.aeorank.dev | mitigate | Full build + typecheck + test gate before commit, per-site `dist` assertion, and a blocking human visual check; the isolated commit is revertible alone per D-03 |
| T-17-25 | Tampering | `pnpm.overrides.vite` crossing a major | mitigate | Override raised to the vite 8 floor or removed, asserted by an automated check in the acceptance criteria |
| T-17-26 | Denial of Service | Consumers of the published `@aeorank/astro` plugin | mitigate | `peerDependencies.astro` widened to admit Astro 6 and 7 so downstream installs do not hit peer errors; the new range is recorded in the commit body |
| T-17-SC | Tampering | npm/pnpm installs | mitigate | Every `@astrojs/*` version is resolved in Task 1 directly from the npm registry with its peer range read before installation. No new package NAMES are introduced — only versions of packages already in `pnpm-lock.yaml`, which RESEARCH.md `## Package Legitimacy Audit` records as the phase-wide condition |
</threat_model>

<verification>
- `pnpm install && pnpm build && pnpm typecheck && pnpm test` exits 0
- `pnpm audit --audit-level=high` exits 0
- `gh api` Dependabot query returns zero critical and zero high
- Both sites build and are human-verified visually
</verification>

<success_criteria>
ROADMAP success criterion 1 is fully satisfied:
`gh api repos/vinpatel/aeorank/dependabot/alerts?state=open` returns zero alerts of severity
`critical` and zero of severity `high` — achieved across three separately-gated, individually
revertible commits (plans 17-02, 17-05, and this one), with no alert suppressed or dismissed.
</success_criteria>

<output>
Create `.planning/workstreams/milestone/phases/17-launch-blockers/17-08-SUMMARY.md` when done.
Record the resolved version of every `@astrojs/*` package, the new `packages/astro` peer
range, the fate of the `vite` override, the final severity histogram, and the exact residual
medium/low alerts left open.
</output>
